// Generated by AshTypescript - RPC Actions
// Do not edit this file manually


import type { AgentActivityLogFilterInput, AgentActivityLogResourceSchema, AgentActivityLogSortField, AgentInboxEventFilterInput, AgentInboxEventResourceSchema, AgentInboxEventSortField, AgentSecretFilterInput, AgentSecretResourceSchema, AgentSecretSortField, AshRpcError, BrainFilterInput, BrainPageFilterInput, BrainPageLinkFilterInput, BrainPageLinkResourceSchema, BrainPageLinkSortField, BrainPageResourceSchema, BrainPageSortField, BrainPageSourceFilterInput, BrainPageSourceResourceSchema, BrainPageSourceSortField, BrainResourceSchema, BrainSortField, ConditionalPaginatedResult, ConversationFavoriteFilterInput, ConversationFavoriteResourceSchema, ConversationFavoriteSortField, ConversationFilterInput, ConversationInvitationFilterInput, ConversationInvitationResourceSchema, ConversationInvitationSortField, ConversationInviteLinkFilterInput, ConversationInviteLinkResourceSchema, ConversationInviteLinkSortField, ConversationMemberFilterInput, ConversationMemberResourceSchema, ConversationMemberSortField, ConversationResourceSchema, ConversationShareLinkFilterInput, ConversationShareLinkResourceSchema, ConversationShareLinkSortField, ConversationSortField, CustomAgentFilterInput, CustomAgentResourceSchema, CustomAgentSortField, Decimal, DraftFilterInput, DraftResourceSchema, DraftSortField, FileFilterInput, FileResourceSchema, FileSortField, FolderFilterInput, FolderResourceSchema, FolderSortField, InferResult, JobFilterInput, JobResourceSchema, JobRunFilterInput, JobRunResourceSchema, JobRunSortField, JobSortField, KnowledgeCollectionFilterInput, KnowledgeCollectionResourceSchema, KnowledgeCollectionSortField, KnowledgeSourceFilterInput, KnowledgeSourceResourceSchema, KnowledgeSourceSortField, MCPServerCredentialResourceSchema, MCPServerFilterInput, MCPServerResourceSchema, MCPServerSortField, MagusAgentsSlashCommandInputSchema, MemoryFilterInput, MemoryResourceSchema, MemorySortField, MessageFilterInput, MessageResourceSchema, MessageSortField, ModelFilterInput, ModelProviderFilterInput, ModelProviderResourceSchema, ModelProviderSortField, ModelResourceSchema, ModelSortField, NotificationFilterInput, NotificationResourceSchema, NotificationSortField, OrganizationMemberFilterInput, OrganizationMemberResourceSchema, OrganizationMemberSortField, OrganizationResourceSchema, PromptFavoriteFilterInput, PromptFavoriteResourceSchema, PromptFavoriteSortField, PromptFilterInput, PromptResourceSchema, PromptSortField, SandboxSecretFilterInput, SandboxSecretResourceSchema, SandboxSecretSortField, SkillFavoriteFilterInput, SkillFavoriteResourceSchema, SkillFavoriteSortField, SkillFilterInput, SkillResourceSchema, SkillSortField, SkillTrustFilterInput, SkillTrustResourceSchema, SkillTrustSortField, SortString, TabSessionResourceSchema, TagFilterInput, TagResourceSchema, TagSortField, TaskDependencyFilterInput, TaskDependencyResourceSchema, TaskDependencySortField, TaskEventFilterInput, TaskEventResourceSchema, TaskEventSortField, TaskFilterInput, TaskResourceSchema, TaskSortField, UUID, UUIDv7, UnifiedFieldSelection, UserFolderStateFilterInput, UserFolderStateResourceSchema, UserFolderStateSortField, UserIntegrationFilterInput, UserIntegrationResourceSchema, UserIntegrationSortField, UserModelPreferenceFilterInput, UserModelPreferenceResourceSchema, UserModelPreferenceSortField, UserProfileResourceSchema, UserResourceSchema, UtcDateTimeUsec, WorkspaceFilterInput, WorkspaceMemberFilterInput, WorkspaceMemberResourceSchema, WorkspaceMemberSortField, WorkspaceResourceSchema, WorkspaceSortField, context_windowResourceSchema } from "./ash_types";
export type * from "./ash_types";

// Helper Functions

/**
 * Configuration options for action RPC requests
 */
export interface ActionConfig {
  // Request data
  input?: Record<string, any>;
  identity?: any;
  fields?: Array<string | Record<string, any>>; // Field selection
  filter?: Record<string, any>; // Filter options (for reads)
  sort?: string | string[]; // Sort options
  page?:
    | {
        // Offset-based pagination
        limit?: number;
        offset?: number;
        count?: boolean;
      }
    | {
        // Keyset pagination
        limit?: number;
        after?: string;
        before?: string;
      };

  // Metadata
  metadataFields?: ReadonlyArray<string>;

  // HTTP customization
  headers?: Record<string, string>; // Custom headers
  fetchOptions?: RequestInit; // Fetch options (signal, cache, etc.)
  customFetch?: (
    input: RequestInfo | URL,
    init?: RequestInit,
  ) => Promise<Response>;

  // Multitenancy
  tenant?: string; // Tenant parameter

  // Hook context
  hookCtx?: Record<string, any>;
}




/**
 * Gets the CSRF token from the page's meta tag
 * Returns null if no CSRF token is found
 */
export function getPhoenixCSRFToken(): string | null {
  return document
    ?.querySelector("meta[name='csrf-token']")
    ?.getAttribute("content") || null;
}

/**
 * Builds headers object with CSRF token for Phoenix applications
 * Returns headers object with X-CSRF-Token (if available)
 */
export function buildCSRFHeaders(headers: Record<string, string> = {}): Record<string, string> {
  const csrfToken = getPhoenixCSRFToken();
  if (csrfToken) {
    headers["X-CSRF-Token"] = csrfToken;
  }

  return headers;
}

/**
 * Internal helper function for making action RPC requests
 * Handles hooks, request configuration, fetch execution, and error handling
 * @param config Configuration matching ActionConfig
 */
export async function executeActionRpcRequest<T>(
  payload: Record<string, any>,
  config: ActionConfig
): Promise<T> {
    const processedConfig = config;

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...processedConfig.headers,
    ...config.headers,
  };

  const fetchFunction = config.customFetch || processedConfig.customFetch || fetch;
  const fetchOptions: RequestInit = {
    ...processedConfig.fetchOptions,
    ...config.fetchOptions,
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  };

  const response = await fetchFunction("/rpc/run", fetchOptions);
  const result = response.ok ? await response.json() : null;


  if (!response.ok) {
    return {
      success: false,
      errors: [
        {
          type: "network_error",
          message: `Network request failed: ${response.statusText}`,
          shortMessage: "Network error",
          vars: { statusCode: response.status, statusText: response.statusText },
          fields: [],
          path: [],
          details: { statusCode: response.status }
        }
      ],
    } as T;
  }

  return result as T;
}









export type ChangeUserPasswordInput = {
  currentPassword: string;
  password: string;
  passwordConfirmation: string;
};

export type ChangeUserPasswordFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferChangeUserPasswordResult<
  Fields extends ChangeUserPasswordFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type ChangeUserPasswordResult<Fields extends ChangeUserPasswordFields | undefined = undefined> = | { success: true; data: InferChangeUserPasswordResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function changeUserPassword<Fields extends ChangeUserPasswordFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: ChangeUserPasswordInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ChangeUserPasswordResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "change_user_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ChangeUserPasswordResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CurrentUserFields = UnifiedFieldSelection<UserResourceSchema>[];
export type InferCurrentUserResult<
  Fields extends CurrentUserFields,
> = InferResult<UserResourceSchema, Fields>;

export type CurrentUserResult<Fields extends CurrentUserFields> = | { success: true; data: InferCurrentUserResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function currentUser<Fields extends CurrentUserFields>(
  config: {
  tenant?: string;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CurrentUserResult<Fields>> {
  const payload = {
    action: "current_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CurrentUserResult<Fields>>(
    payload,
    config
  );
}


export type GrantDataRegionConsentInput = {
  region: string;
};

export type GrantDataRegionConsentFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferGrantDataRegionConsentResult<
  Fields extends GrantDataRegionConsentFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type GrantDataRegionConsentResult<Fields extends GrantDataRegionConsentFields | undefined = undefined> = | { success: true; data: InferGrantDataRegionConsentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function grantDataRegionConsent<Fields extends GrantDataRegionConsentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: GrantDataRegionConsentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GrantDataRegionConsentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "grant_data_region_consent",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GrantDataRegionConsentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RequestEmailChangeInput = {
  newEmail: string;
};

export type RequestEmailChangeFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferRequestEmailChangeResult<
  Fields extends RequestEmailChangeFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type RequestEmailChangeResult<Fields extends RequestEmailChangeFields | undefined = undefined> = | { success: true; data: InferRequestEmailChangeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function requestEmailChange<Fields extends RequestEmailChangeFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: RequestEmailChangeInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RequestEmailChangeResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "request_email_change",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RequestEmailChangeResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SelectDefaultImageModelInput = {
  selectedImageModelId?: UUID | null;
};

export type SelectDefaultImageModelFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferSelectDefaultImageModelResult<
  Fields extends SelectDefaultImageModelFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type SelectDefaultImageModelResult<Fields extends SelectDefaultImageModelFields | undefined = undefined> = | { success: true; data: InferSelectDefaultImageModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function selectDefaultImageModel<Fields extends SelectDefaultImageModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SelectDefaultImageModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SelectDefaultImageModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "select_default_image_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SelectDefaultImageModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SelectDefaultModelInput = {
  selectedModelId?: UUID | null;
};

export type SelectDefaultModelFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferSelectDefaultModelResult<
  Fields extends SelectDefaultModelFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type SelectDefaultModelResult<Fields extends SelectDefaultModelFields | undefined = undefined> = | { success: true; data: InferSelectDefaultModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function selectDefaultModel<Fields extends SelectDefaultModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SelectDefaultModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SelectDefaultModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "select_default_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SelectDefaultModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SelectDefaultVideoModelInput = {
  selectedVideoModelId?: UUID | null;
};

export type SelectDefaultVideoModelFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferSelectDefaultVideoModelResult<
  Fields extends SelectDefaultVideoModelFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type SelectDefaultVideoModelResult<Fields extends SelectDefaultVideoModelFields | undefined = undefined> = | { success: true; data: InferSelectDefaultVideoModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function selectDefaultVideoModel<Fields extends SelectDefaultVideoModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SelectDefaultVideoModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SelectDefaultVideoModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "select_default_video_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SelectDefaultVideoModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SelectWorkspaceInput = {
  currentWorkspaceId?: UUID | null;
};

export type SelectWorkspaceFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferSelectWorkspaceResult<
  Fields extends SelectWorkspaceFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type SelectWorkspaceResult<Fields extends SelectWorkspaceFields | undefined = undefined> = | { success: true; data: InferSelectWorkspaceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function selectWorkspace<Fields extends SelectWorkspaceFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SelectWorkspaceInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SelectWorkspaceResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "select_workspace",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SelectWorkspaceResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetUserPasswordInput = {
  password: string;
  passwordConfirmation: string;
};

export type SetUserPasswordFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferSetUserPasswordResult<
  Fields extends SetUserPasswordFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type SetUserPasswordResult<Fields extends SetUserPasswordFields | undefined = undefined> = | { success: true; data: InferSetUserPasswordResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function setUserPassword<Fields extends SetUserPasswordFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: SetUserPasswordInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetUserPasswordResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_user_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetUserPasswordResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateDataRegionPreferenceInput = {
  regions: Array<string>;
};

export type UpdateDataRegionPreferenceFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateDataRegionPreferenceResult<
  Fields extends UpdateDataRegionPreferenceFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateDataRegionPreferenceResult<Fields extends UpdateDataRegionPreferenceFields | undefined = undefined> = | { success: true; data: InferUpdateDataRegionPreferenceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateDataRegionPreference<Fields extends UpdateDataRegionPreferenceFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateDataRegionPreferenceInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateDataRegionPreferenceResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_data_region_preference",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateDataRegionPreferenceResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateGlobalMemorySettingInput = {
  globalMemoryEnabled?: boolean;
};

export type UpdateGlobalMemorySettingFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateGlobalMemorySettingResult<
  Fields extends UpdateGlobalMemorySettingFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateGlobalMemorySettingResult<Fields extends UpdateGlobalMemorySettingFields | undefined = undefined> = | { success: true; data: InferUpdateGlobalMemorySettingResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateGlobalMemorySetting<Fields extends UpdateGlobalMemorySettingFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateGlobalMemorySettingInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateGlobalMemorySettingResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_global_memory_setting",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateGlobalMemorySettingResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateProfileSettingInput = {
  profileEnabled?: boolean;
};

export type UpdateProfileSettingFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateProfileSettingResult<
  Fields extends UpdateProfileSettingFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateProfileSettingResult<Fields extends UpdateProfileSettingFields | undefined = undefined> = | { success: true; data: InferUpdateProfileSettingResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateProfileSetting<Fields extends UpdateProfileSettingFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateProfileSettingInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateProfileSettingResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_profile_setting",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateProfileSettingResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateTimezoneInput = {
  timezone?: string | null;
};

export type UpdateTimezoneFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateTimezoneResult<
  Fields extends UpdateTimezoneFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateTimezoneResult<Fields extends UpdateTimezoneFields | undefined = undefined> = | { success: true; data: InferUpdateTimezoneResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateTimezone<Fields extends UpdateTimezoneFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateTimezoneInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateTimezoneResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_timezone",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateTimezoneResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateUiPreferencesInput = {
  uiPreferences?: Record<string, any>;
};

export type UpdateUiPreferencesFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateUiPreferencesResult<
  Fields extends UpdateUiPreferencesFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateUiPreferencesResult<Fields extends UpdateUiPreferencesFields | undefined = undefined> = | { success: true; data: InferUpdateUiPreferencesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateUiPreferences<Fields extends UpdateUiPreferencesFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateUiPreferencesInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateUiPreferencesResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_ui_preferences",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateUiPreferencesResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateUserSettingsInput = {
  displayName?: string | null;
  language?: "de" | "en";
  name?: string | null;
  contextStrategy?: "compact" | "rolling" | null;
};

export type UpdateUserSettingsFields = UnifiedFieldSelection<UserResourceSchema>[];

export type InferUpdateUserSettingsResult<
  Fields extends UpdateUserSettingsFields | undefined,
> = InferResult<UserResourceSchema, Fields>;

export type UpdateUserSettingsResult<Fields extends UpdateUserSettingsFields | undefined = undefined> = | { success: true; data: InferUpdateUserSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing User
 *
 * @ashActionType :update
 */
export async function updateUserSettings<Fields extends UpdateUserSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateUserSettingsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateUserSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_user_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateUserSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type AgentActivityInput = {
  agentId: UUID;
};

export type AgentActivityFields = UnifiedFieldSelection<AgentActivityLogResourceSchema>[];
export type InferAgentActivityResult<
  Fields extends AgentActivityFields,
> = Array<InferResult<AgentActivityLogResourceSchema, Fields>>;

export type AgentActivityResult<Fields extends AgentActivityFields> = | { success: true; data: InferAgentActivityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AgentActivityLog records
 *
 * @ashActionType :read
 */
export async function agentActivity<Fields extends AgentActivityFields>(
  config: {
  tenant?: string;
  input: AgentActivityInput;
  fields: Fields;
  filter?: AgentActivityLogFilterInput;
  sort?: SortString<AgentActivityLogSortField> | SortString<AgentActivityLogSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentActivityResult<Fields>> {
  const payload = {
    action: "agent_activity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<AgentActivityResult<Fields>>(
    payload,
    config
  );
}


export type AgentInboxEventsInput = {
  agentId: UUID;
};

export type AgentInboxEventsFields = UnifiedFieldSelection<AgentInboxEventResourceSchema>[];
export type InferAgentInboxEventsResult<
  Fields extends AgentInboxEventsFields,
> = Array<InferResult<AgentInboxEventResourceSchema, Fields>>;

export type AgentInboxEventsResult<Fields extends AgentInboxEventsFields> = | { success: true; data: InferAgentInboxEventsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AgentInboxEvent records
 *
 * @ashActionType :read
 */
export async function agentInboxEvents<Fields extends AgentInboxEventsFields>(
  config: {
  tenant?: string;
  input: AgentInboxEventsInput;
  fields: Fields;
  filter?: AgentInboxEventFilterInput;
  sort?: SortString<AgentInboxEventSortField> | SortString<AgentInboxEventSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentInboxEventsResult<Fields>> {
  const payload = {
    action: "agent_inbox_events",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<AgentInboxEventsResult<Fields>>(
    payload,
    config
  );
}


export type DismissInboxEventInput = {
  resolutionNote?: string | null;
};

export type DismissInboxEventFields = UnifiedFieldSelection<AgentInboxEventResourceSchema>[];

export type InferDismissInboxEventResult<
  Fields extends DismissInboxEventFields | undefined,
> = InferResult<AgentInboxEventResourceSchema, Fields>;

export type DismissInboxEventResult<Fields extends DismissInboxEventFields | undefined = undefined> = | { success: true; data: InferDismissInboxEventResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing AgentInboxEvent
 *
 * @ashActionType :update
 */
export async function dismissInboxEvent<Fields extends DismissInboxEventFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: DismissInboxEventInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DismissInboxEventResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "dismiss_inbox_event",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DismissInboxEventResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type AgentSecretsInput = {
  customAgentId: UUID;
};

export type AgentSecretsFields = UnifiedFieldSelection<AgentSecretResourceSchema>[];
export type InferAgentSecretsResult<
  Fields extends AgentSecretsFields,
> = Array<InferResult<AgentSecretResourceSchema, Fields>>;

export type AgentSecretsResult<Fields extends AgentSecretsFields> = | { success: true; data: InferAgentSecretsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AgentSecret records
 *
 * @ashActionType :read
 */
export async function agentSecrets<Fields extends AgentSecretsFields>(
  config: {
  tenant?: string;
  input: AgentSecretsInput;
  fields: Fields;
  filter?: AgentSecretFilterInput;
  sort?: SortString<AgentSecretSortField> | SortString<AgentSecretSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentSecretsResult<Fields>> {
  const payload = {
    action: "agent_secrets",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<AgentSecretsResult<Fields>>(
    payload,
    config
  );
}


export type CreateAgentSecretInput = {
  key: string;
  value: string;
  scope?: "sandbox_env" | "tool_config";
  description?: string | null;
  customAgentId: UUID;
};

export type CreateAgentSecretFields = UnifiedFieldSelection<AgentSecretResourceSchema>[];

export type InferCreateAgentSecretResult<
  Fields extends CreateAgentSecretFields | undefined,
> = InferResult<AgentSecretResourceSchema, Fields>;

export type CreateAgentSecretResult<Fields extends CreateAgentSecretFields | undefined = undefined> = | { success: true; data: InferCreateAgentSecretResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new AgentSecret
 *
 * @ashActionType :create
 */
export async function createAgentSecret<Fields extends CreateAgentSecretFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateAgentSecretInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateAgentSecretResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_agent_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateAgentSecretResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyAgentSecretResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a AgentSecret
 *
 * @ashActionType :destroy
 */
export async function destroyAgentSecret(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyAgentSecretResult> {
  const payload = {
    action: "destroy_agent_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyAgentSecretResult>(
    payload,
    config
  );
}


export type UpdateAgentSecretInput = {
  value?: string;
  description?: string | null;
};

export type UpdateAgentSecretFields = UnifiedFieldSelection<AgentSecretResourceSchema>[];

export type InferUpdateAgentSecretResult<
  Fields extends UpdateAgentSecretFields | undefined,
> = InferResult<AgentSecretResourceSchema, Fields>;

export type UpdateAgentSecretResult<Fields extends UpdateAgentSecretFields | undefined = undefined> = | { success: true; data: InferUpdateAgentSecretResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing AgentSecret
 *
 * @ashActionType :update
 */
export async function updateAgentSecret<Fields extends UpdateAgentSecretFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateAgentSecretInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateAgentSecretResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_agent_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateAgentSecretResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type AddAgentAttachmentInput = {
  agentId: UUID;
  fileId: UUID;
  mode: string;
};

export type InferAddAgentAttachmentResult = Record<string, any>;

export type AddAgentAttachmentResult = | { success: true; data: InferAddAgentAttachmentResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function addAgentAttachment(
  config: {
  tenant?: string;
  input: AddAgentAttachmentInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AddAgentAttachmentResult> {
  const payload = {
    action: "add_agent_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AddAgentAttachmentResult>(
    payload,
    config
  );
}


export type AgentAttachmentsInput = {
  agentId: UUID;
};

export type InferAgentAttachmentsResult = Array<Record<string, any>>;

export type AgentAttachmentsResult = | { success: true; data: InferAgentAttachmentsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function agentAttachments(
  config: {
  tenant?: string;
  input: AgentAttachmentsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentAttachmentsResult> {
  const payload = {
    action: "agent_attachments",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AgentAttachmentsResult>(
    payload,
    config
  );
}


export type AgentIntegrationsInput = {
  agentId: UUID;
};

export type InferAgentIntegrationsResult = Array<Record<string, any>>;

export type AgentIntegrationsResult = | { success: true; data: InferAgentIntegrationsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function agentIntegrations(
  config: {
  tenant?: string;
  input: AgentIntegrationsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentIntegrationsResult> {
  const payload = {
    action: "agent_integrations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AgentIntegrationsResult>(
    payload,
    config
  );
}


export type AgentKnowledgeAccessInput = {
  agentId: UUID;
};

export type InferAgentKnowledgeAccessResult = Record<string, any>;

export type AgentKnowledgeAccessResult = | { success: true; data: InferAgentKnowledgeAccessResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function agentKnowledgeAccess(
  config: {
  tenant?: string;
  input: AgentKnowledgeAccessInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentKnowledgeAccessResult> {
  const payload = {
    action: "agent_knowledge_access",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AgentKnowledgeAccessResult>(
    payload,
    config
  );
}


export type AgentMemoriesInput = {
  agentId: UUID;
};

export type InferAgentMemoriesResult = Array<Record<string, any>>;

export type AgentMemoriesResult = | { success: true; data: InferAgentMemoriesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function agentMemories(
  config: {
  tenant?: string;
  input: AgentMemoriesInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AgentMemoriesResult> {
  const payload = {
    action: "agent_memories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AgentMemoriesResult>(
    payload,
    config
  );
}


export type AvailableIntegrationProvidersInput = {
  agentId: UUID;
};

export type InferAvailableIntegrationProvidersResult = Array<Record<string, any>>;

export type AvailableIntegrationProvidersResult = | { success: true; data: InferAvailableIntegrationProvidersResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function availableIntegrationProviders(
  config: {
  tenant?: string;
  input: AvailableIntegrationProvidersInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AvailableIntegrationProvidersResult> {
  const payload = {
    action: "available_integration_providers",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AvailableIntegrationProvidersResult>(
    payload,
    config
  );
}


export type ConnectAgentIntegrationInput = {
  agentId: UUID;
  providerKey: string;
  credentials?: Record<string, any> | null;
  config?: Record<string, any> | null;
};

export type InferConnectAgentIntegrationResult = Record<string, any>;

export type ConnectAgentIntegrationResult = | { success: true; data: InferConnectAgentIntegrationResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function connectAgentIntegration(
  config: {
  tenant?: string;
  input: ConnectAgentIntegrationInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConnectAgentIntegrationResult> {
  const payload = {
    action: "connect_agent_integration",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ConnectAgentIntegrationResult>(
    payload,
    config
  );
}


export type CreateCustomAgentInput = {
  name: string;
  description?: string | null;
  icon?: string | null;
  imagePath?: string | null;
  instructions?: string | null;
  slashCommands?: Array<MagusAgentsSlashCommandInputSchema> | null;
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation" | null;
  disabledToolCategories?: Array<"code" | "files" | "integrations" | "memory" | "skills" | "tasks" | "web"> | null;
  preLoadedSkills?: Array<string> | null;
  samplingSettings?: Record<string, any> | null;
  maxIterations?: number | null;
  canReadGlobalMemories?: boolean | null;
  canWriteGlobalMemories?: boolean | null;
  canAccessGlobalFiles?: boolean | null;
  canAccessKnowledge?: boolean | null;
  modelId?: UUID | null;
  imageModelId?: UUID | null;
  videoModelId?: UUID | null;
  workspaceId?: UUID | null;
  isPaused?: boolean | null;
  maxDailyRuns?: number | null;
  maxTokensPerRun?: number | null;
  heartbeatEnabled?: boolean | null;
  heartbeatInstructions?: string | null;
  heartbeatDefaultIntervalMinutes?: number | null;
  nextScheduledAt?: UtcDateTimeUsec | null;
};

export type CreateCustomAgentFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];

export type InferCreateCustomAgentResult<
  Fields extends CreateCustomAgentFields | undefined,
> = InferResult<CustomAgentResourceSchema, Fields>;

export type CreateCustomAgentResult<Fields extends CreateCustomAgentFields | undefined = undefined> = | { success: true; data: InferCreateCustomAgentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new CustomAgent
 *
 * @ashActionType :create
 */
export async function createCustomAgent<Fields extends CreateCustomAgentFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateCustomAgentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateCustomAgentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_custom_agent",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateCustomAgentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeleteAgentMemoryInput = {
  memoryId: UUID;
};

export type InferDeleteAgentMemoryResult = Record<string, any>;

export type DeleteAgentMemoryResult = | { success: true; data: InferDeleteAgentMemoryResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function deleteAgentMemory(
  config: {
  tenant?: string;
  input: DeleteAgentMemoryInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteAgentMemoryResult> {
  const payload = {
    action: "delete_agent_memory",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DeleteAgentMemoryResult>(
    payload,
    config
  );
}


export type DestroyCustomAgentResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a CustomAgent
 *
 * @ashActionType :destroy
 */
export async function destroyCustomAgent(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyCustomAgentResult> {
  const payload = {
    action: "destroy_custom_agent",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyCustomAgentResult>(
    payload,
    config
  );
}


export type DisconnectAgentIntegrationInput = {
  integrationId: UUID;
};

export type InferDisconnectAgentIntegrationResult = Record<string, any>;

export type DisconnectAgentIntegrationResult = | { success: true; data: InferDisconnectAgentIntegrationResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function disconnectAgentIntegration(
  config: {
  tenant?: string;
  input: DisconnectAgentIntegrationInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DisconnectAgentIntegrationResult> {
  const payload = {
    action: "disconnect_agent_integration",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DisconnectAgentIntegrationResult>(
    payload,
    config
  );
}


export type GetCustomAgentFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];
export type InferGetCustomAgentResult<
  Fields extends GetCustomAgentFields,
> = InferResult<CustomAgentResourceSchema, Fields>;

export type GetCustomAgentResult<Fields extends GetCustomAgentFields> = | { success: true; data: InferGetCustomAgentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read CustomAgent records
 *
 * @ashActionType :read
 */
export async function getCustomAgent<Fields extends GetCustomAgentFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetCustomAgentResult<Fields>> {
  const payload = {
    action: "get_custom_agent",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetCustomAgentResult<Fields>>(
    payload,
    config
  );
}


export type InferListAvailableSkillsResult = Array<Record<string, any>>;

export type ListAvailableSkillsResult = | { success: true; data: InferListAvailableSkillsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function listAvailableSkills(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAvailableSkillsResult> {
  const payload = {
    action: "list_available_skills",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<ListAvailableSkillsResult>(
    payload,
    config
  );
}


export type MergedSlashCommandsInput = {
  agentId?: UUID | null;
};

export type InferMergedSlashCommandsResult = Array<Record<string, any>>;

export type MergedSlashCommandsResult = | { success: true; data: InferMergedSlashCommandsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function mergedSlashCommands(
  config: {
  tenant?: string;
  input?: MergedSlashCommandsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MergedSlashCommandsResult> {
  const payload = {
    action: "merged_slash_commands",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<MergedSlashCommandsResult>(
    payload,
    config
  );
}


export type MyAgentsFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];
export type InferMyAgentsResult<
  Fields extends MyAgentsFields,
> = Array<InferResult<CustomAgentResourceSchema, Fields>>;

export type MyAgentsResult<Fields extends MyAgentsFields> = | { success: true; data: InferMyAgentsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read CustomAgent records
 *
 * @ashActionType :read
 */
export async function myAgents<Fields extends MyAgentsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: CustomAgentFilterInput;
  sort?: SortString<CustomAgentSortField> | SortString<CustomAgentSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyAgentsResult<Fields>> {
  const payload = {
    action: "my_agents",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyAgentsResult<Fields>>(
    payload,
    config
  );
}


export type RemoveAgentAttachmentInput = {
  attachmentId: UUID;
};

export type InferRemoveAgentAttachmentResult = Record<string, any>;

export type RemoveAgentAttachmentResult = | { success: true; data: InferRemoveAgentAttachmentResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function removeAgentAttachment(
  config: {
  tenant?: string;
  input: RemoveAgentAttachmentInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveAgentAttachmentResult> {
  const payload = {
    action: "remove_agent_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<RemoveAgentAttachmentResult>(
    payload,
    config
  );
}


export type SetAgentAttachmentModeInput = {
  attachmentId: UUID;
  mode: string;
};

export type InferSetAgentAttachmentModeResult = Record<string, any>;

export type SetAgentAttachmentModeResult = | { success: true; data: InferSetAgentAttachmentModeResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function setAgentAttachmentMode(
  config: {
  tenant?: string;
  input: SetAgentAttachmentModeInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetAgentAttachmentModeResult> {
  const payload = {
    action: "set_agent_attachment_mode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SetAgentAttachmentModeResult>(
    payload,
    config
  );
}


export type SetAgentIntegrationToolInput = {
  integrationId: UUID;
  tool: string;
  enabled: boolean;
};

export type InferSetAgentIntegrationToolResult = Record<string, any>;

export type SetAgentIntegrationToolResult = | { success: true; data: InferSetAgentIntegrationToolResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function setAgentIntegrationTool(
  config: {
  tenant?: string;
  input: SetAgentIntegrationToolInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetAgentIntegrationToolResult> {
  const payload = {
    action: "set_agent_integration_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SetAgentIntegrationToolResult>(
    payload,
    config
  );
}


export type SetAgentResourceAccessInput = {
  agentId: UUID;
  resourceType: string;
  resourceId: UUID;
  granted: boolean;
};

export type InferSetAgentResourceAccessResult = Record<string, any>;

export type SetAgentResourceAccessResult = | { success: true; data: InferSetAgentResourceAccessResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function setAgentResourceAccess(
  config: {
  tenant?: string;
  input: SetAgentResourceAccessInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetAgentResourceAccessResult> {
  const payload = {
    action: "set_agent_resource_access",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SetAgentResourceAccessResult>(
    payload,
    config
  );
}


export type ShareAgentToTeamFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];

export type InferShareAgentToTeamResult<
  Fields extends ShareAgentToTeamFields | undefined,
> = InferResult<CustomAgentResourceSchema, Fields>;

export type ShareAgentToTeamResult<Fields extends ShareAgentToTeamFields | undefined = undefined> = | { success: true; data: InferShareAgentToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CustomAgent
 *
 * @ashActionType :update
 */
export async function shareAgentToTeam<Fields extends ShareAgentToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareAgentToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_agent_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareAgentToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TriggerAgentRunInput = {
  agentId: UUID;
};

export type InferTriggerAgentRunResult = Record<string, any>;

export type TriggerAgentRunResult = | { success: true; data: InferTriggerAgentRunResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function triggerAgentRun(
  config: {
  tenant?: string;
  input: TriggerAgentRunInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TriggerAgentRunResult> {
  const payload = {
    action: "trigger_agent_run",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<TriggerAgentRunResult>(
    payload,
    config
  );
}


export type UnshareAgentFromTeamFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];

export type InferUnshareAgentFromTeamResult<
  Fields extends UnshareAgentFromTeamFields | undefined,
> = InferResult<CustomAgentResourceSchema, Fields>;

export type UnshareAgentFromTeamResult<Fields extends UnshareAgentFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareAgentFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CustomAgent
 *
 * @ashActionType :update
 */
export async function unshareAgentFromTeam<Fields extends UnshareAgentFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareAgentFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_agent_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareAgentFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateAgentMemoryInput = {
  memoryId: UUID;
  summary?: string | null;
  kind: string;
  confidence: number;
};

export type InferUpdateAgentMemoryResult = Record<string, any>;

export type UpdateAgentMemoryResult = | { success: true; data: InferUpdateAgentMemoryResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CustomAgent
 *
 * @ashActionType :action
 */
export async function updateAgentMemory(
  config: {
  tenant?: string;
  input: UpdateAgentMemoryInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateAgentMemoryResult> {
  const payload = {
    action: "update_agent_memory",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<UpdateAgentMemoryResult>(
    payload,
    config
  );
}


export type UpdateCustomAgentInput = {
  name?: string;
  handle?: string;
  description?: string | null;
  icon?: string | null;
  imagePath?: string | null;
  instructions?: string | null;
  slashCommands?: Array<MagusAgentsSlashCommandInputSchema> | null;
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation" | null;
  disabledToolCategories?: Array<"code" | "files" | "integrations" | "memory" | "skills" | "tasks" | "web"> | null;
  preLoadedSkills?: Array<string> | null;
  samplingSettings?: Record<string, any> | null;
  maxIterations?: number | null;
  canReadGlobalMemories?: boolean | null;
  canWriteGlobalMemories?: boolean | null;
  canAccessGlobalFiles?: boolean | null;
  canAccessKnowledge?: boolean | null;
  modelId?: UUID | null;
  imageModelId?: UUID | null;
  videoModelId?: UUID | null;
  isPaused?: boolean | null;
  maxDailyRuns?: number | null;
  maxTokensPerRun?: number | null;
  heartbeatEnabled?: boolean | null;
  heartbeatInstructions?: string | null;
  heartbeatDefaultIntervalMinutes?: number | null;
  nextScheduledAt?: UtcDateTimeUsec | null;
};

export type UpdateCustomAgentFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];

export type InferUpdateCustomAgentResult<
  Fields extends UpdateCustomAgentFields | undefined,
> = InferResult<CustomAgentResourceSchema, Fields>;

export type UpdateCustomAgentResult<Fields extends UpdateCustomAgentFields | undefined = undefined> = | { success: true; data: InferUpdateCustomAgentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CustomAgent
 *
 * @ashActionType :update
 */
export async function updateCustomAgent<Fields extends UpdateCustomAgentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateCustomAgentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateCustomAgentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_custom_agent",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateCustomAgentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceAgentsInput = {
  workspaceId: UUID;
};

export type WorkspaceAgentsFields = UnifiedFieldSelection<CustomAgentResourceSchema>[];
export type InferWorkspaceAgentsResult<
  Fields extends WorkspaceAgentsFields,
> = Array<InferResult<CustomAgentResourceSchema, Fields>>;

export type WorkspaceAgentsResult<Fields extends WorkspaceAgentsFields> = | { success: true; data: InferWorkspaceAgentsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read CustomAgent records
 *
 * @ashActionType :read
 */
export async function workspaceAgents<Fields extends WorkspaceAgentsFields>(
  config: {
  tenant?: string;
  input: WorkspaceAgentsInput;
  fields: Fields;
  filter?: CustomAgentFilterInput;
  sort?: SortString<CustomAgentSortField> | SortString<CustomAgentSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceAgentsResult<Fields>> {
  const payload = {
    action: "workspace_agents",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceAgentsResult<Fields>>(
    payload,
    config
  );
}


export type CreateBrainInput = {
  title: string;
  description?: string | null;
  icon?: string | null;
  color?: string | null;
  workspaceId?: UUID | null;
  instructions?: string | null;
};

export type CreateBrainFields = UnifiedFieldSelection<BrainResourceSchema>[];

export type InferCreateBrainResult<
  Fields extends CreateBrainFields | undefined,
> = InferResult<BrainResourceSchema, Fields>;

export type CreateBrainResult<Fields extends CreateBrainFields | undefined = undefined> = | { success: true; data: InferCreateBrainResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new BrainResource
 *
 * @ashActionType :create
 */
export async function createBrain<Fields extends CreateBrainFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateBrainInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateBrainResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_brain",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateBrainResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyBrainsFields = UnifiedFieldSelection<BrainResourceSchema>[];
export type InferMyBrainsResult<
  Fields extends MyBrainsFields,
> = Array<InferResult<BrainResourceSchema, Fields>>;

export type MyBrainsResult<Fields extends MyBrainsFields> = | { success: true; data: InferMyBrainsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read BrainResource records
 *
 * @ashActionType :read
 */
export async function myBrains<Fields extends MyBrainsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: BrainFilterInput;
  sort?: SortString<BrainSortField> | SortString<BrainSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyBrainsResult<Fields>> {
  const payload = {
    action: "my_brains",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyBrainsResult<Fields>>(
    payload,
    config
  );
}


export type ShareBrainToTeamFields = UnifiedFieldSelection<BrainResourceSchema>[];

export type InferShareBrainToTeamResult<
  Fields extends ShareBrainToTeamFields | undefined,
> = InferResult<BrainResourceSchema, Fields>;

export type ShareBrainToTeamResult<Fields extends ShareBrainToTeamFields | undefined = undefined> = | { success: true; data: InferShareBrainToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing BrainResource
 *
 * @ashActionType :update
 */
export async function shareBrainToTeam<Fields extends ShareBrainToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareBrainToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_brain_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareBrainToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnshareBrainFromTeamFields = UnifiedFieldSelection<BrainResourceSchema>[];

export type InferUnshareBrainFromTeamResult<
  Fields extends UnshareBrainFromTeamFields | undefined,
> = InferResult<BrainResourceSchema, Fields>;

export type UnshareBrainFromTeamResult<Fields extends UnshareBrainFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareBrainFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing BrainResource
 *
 * @ashActionType :update
 */
export async function unshareBrainFromTeam<Fields extends UnshareBrainFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareBrainFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_brain_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareBrainFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateBrainInput = {
  title?: string;
  description?: string | null;
  icon?: string | null;
  color?: string | null;
  instructions?: string | null;
};

export type UpdateBrainFields = UnifiedFieldSelection<BrainResourceSchema>[];

export type InferUpdateBrainResult<
  Fields extends UpdateBrainFields | undefined,
> = InferResult<BrainResourceSchema, Fields>;

export type UpdateBrainResult<Fields extends UpdateBrainFields | undefined = undefined> = | { success: true; data: InferUpdateBrainResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing BrainResource
 *
 * @ashActionType :update
 */
export async function updateBrain<Fields extends UpdateBrainFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateBrainInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateBrainResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_brain",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateBrainResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceBrainsInput = {
  workspaceId: UUID;
};

export type WorkspaceBrainsFields = UnifiedFieldSelection<BrainResourceSchema>[];
export type InferWorkspaceBrainsResult<
  Fields extends WorkspaceBrainsFields,
> = Array<InferResult<BrainResourceSchema, Fields>>;

export type WorkspaceBrainsResult<Fields extends WorkspaceBrainsFields> = | { success: true; data: InferWorkspaceBrainsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read BrainResource records
 *
 * @ashActionType :read
 */
export async function workspaceBrains<Fields extends WorkspaceBrainsFields>(
  config: {
  tenant?: string;
  input: WorkspaceBrainsInput;
  fields: Fields;
  filter?: BrainFilterInput;
  sort?: SortString<BrainSortField> | SortString<BrainSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceBrainsResult<Fields>> {
  const payload = {
    action: "workspace_brains",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceBrainsResult<Fields>>(
    payload,
    config
  );
}


export type BrainPageChildrenInput = {
  parentPageId: UUID;
};

export type BrainPageChildrenFields = UnifiedFieldSelection<BrainPageResourceSchema>[];
export type InferBrainPageChildrenResult<
  Fields extends BrainPageChildrenFields,
> = Array<InferResult<BrainPageResourceSchema, Fields>>;

export type BrainPageChildrenResult<Fields extends BrainPageChildrenFields> = | { success: true; data: InferBrainPageChildrenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function brainPageChildren<Fields extends BrainPageChildrenFields>(
  config: {
  tenant?: string;
  input: BrainPageChildrenInput;
  fields: Fields;
  filter?: BrainPageFilterInput;
  sort?: SortString<BrainPageSortField> | SortString<BrainPageSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainPageChildrenResult<Fields>> {
  const payload = {
    action: "brain_page_children",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<BrainPageChildrenResult<Fields>>(
    payload,
    config
  );
}


export type BrainPageGuideInput = {
  pageId: UUID;
};

export type InferBrainPageGuideResult = Record<string, any>;

export type BrainPageGuideResult = | { success: true; data: InferBrainPageGuideResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Page
 *
 * @ashActionType :action
 */
export async function brainPageGuide(
  config: {
  tenant?: string;
  input: BrainPageGuideInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainPageGuideResult> {
  const payload = {
    action: "brain_page_guide",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<BrainPageGuideResult>(
    payload,
    config
  );
}


export type BrainPageVersionBodyInput = {
  pageId: UUID;
  versionId: UUID;
};

export type InferBrainPageVersionBodyResult = string;

export type BrainPageVersionBodyResult = | { success: true; data: InferBrainPageVersionBodyResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Page
 *
 * @ashActionType :action
 */
export async function brainPageVersionBody(
  config: {
  tenant?: string;
  input: BrainPageVersionBodyInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainPageVersionBodyResult> {
  const payload = {
    action: "brain_page_version_body",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<BrainPageVersionBodyResult>(
    payload,
    config
  );
}


export type BrainPageVersionDiffInput = {
  pageId: UUID;
  versionId: UUID;
};

export type InferBrainPageVersionDiffResult = Record<string, any>;

export type BrainPageVersionDiffResult = | { success: true; data: InferBrainPageVersionDiffResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Page
 *
 * @ashActionType :action
 */
export async function brainPageVersionDiff(
  config: {
  tenant?: string;
  input: BrainPageVersionDiffInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainPageVersionDiffResult> {
  const payload = {
    action: "brain_page_version_diff",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<BrainPageVersionDiffResult>(
    payload,
    config
  );
}


export type BrainPagesInput = {
  brainId: UUID;
};

export type BrainPagesFields = UnifiedFieldSelection<BrainPageResourceSchema>[];
export type InferBrainPagesResult<
  Fields extends BrainPagesFields,
> = Array<InferResult<BrainPageResourceSchema, Fields>>;

export type BrainPagesResult<Fields extends BrainPagesFields> = | { success: true; data: InferBrainPagesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function brainPages<Fields extends BrainPagesFields>(
  config: {
  tenant?: string;
  input: BrainPagesInput;
  fields: Fields;
  filter?: BrainPageFilterInput;
  sort?: SortString<BrainPageSortField> | SortString<BrainPageSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainPagesResult<Fields>> {
  const payload = {
    action: "brain_pages",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<BrainPagesResult<Fields>>(
    payload,
    config
  );
}


export type BrainTemplatesInput = {
  brainId: UUID;
};

export type BrainTemplatesFields = UnifiedFieldSelection<BrainPageResourceSchema>[];
export type InferBrainTemplatesResult<
  Fields extends BrainTemplatesFields,
> = Array<InferResult<BrainPageResourceSchema, Fields>>;

export type BrainTemplatesResult<Fields extends BrainTemplatesFields> = | { success: true; data: InferBrainTemplatesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function brainTemplates<Fields extends BrainTemplatesFields>(
  config: {
  tenant?: string;
  input: BrainTemplatesInput;
  fields: Fields;
  filter?: BrainPageFilterInput;
  sort?: SortString<BrainPageSortField> | SortString<BrainPageSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainTemplatesResult<Fields>> {
  const payload = {
    action: "brain_templates",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<BrainTemplatesResult<Fields>>(
    payload,
    config
  );
}


export type CreateBrainPageInput = {
  title?: string | null;
  icon?: string | null;
  parentPageId?: UUID | null;
  kind?: "page" | "template";
  brainId: UUID;
};

export type CreateBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferCreateBrainPageResult<
  Fields extends CreateBrainPageFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type CreateBrainPageResult<Fields extends CreateBrainPageFields | undefined = undefined> = | { success: true; data: InferCreateBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Page
 *
 * @ashActionType :create
 */
export async function createBrainPage<Fields extends CreateBrainPageFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateBrainPageInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateBrainPageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateBrainPageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];
export type InferGetBrainPageResult<
  Fields extends GetBrainPageFields,
> = InferResult<BrainPageResourceSchema, Fields>;

export type GetBrainPageResult<Fields extends GetBrainPageFields> = | { success: true; data: InferGetBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function getBrainPage<Fields extends GetBrainPageFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetBrainPageResult<Fields>> {
  const payload = {
    action: "get_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetBrainPageResult<Fields>>(
    payload,
    config
  );
}


export type ListBrainPageVersionsInput = {
  pageId: UUID;
};

export type InferListBrainPageVersionsResult = Array<Record<string, any>>;

export type ListBrainPageVersionsResult = | { success: true; data: InferListBrainPageVersionsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Page
 *
 * @ashActionType :action
 */
export async function listBrainPageVersions(
  config: {
  tenant?: string;
  input: ListBrainPageVersionsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListBrainPageVersionsResult> {
  const payload = {
    action: "list_brain_page_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ListBrainPageVersionsResult>(
    payload,
    config
  );
}


export type MoveBrainPageInput = {
  parentPageId?: UUID | null;
};

export type MoveBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferMoveBrainPageResult<
  Fields extends MoveBrainPageFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type MoveBrainPageResult<Fields extends MoveBrainPageFields | undefined = undefined> = | { success: true; data: InferMoveBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Page
 *
 * @ashActionType :update
 */
export async function moveBrainPage<Fields extends MoveBrainPageFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: MoveBrainPageInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MoveBrainPageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "move_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MoveBrainPageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RenameBrainPageInput = {
  title?: string | null;
};

export type RenameBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferRenameBrainPageResult<
  Fields extends RenameBrainPageFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type RenameBrainPageResult<Fields extends RenameBrainPageFields | undefined = undefined> = | { success: true; data: InferRenameBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Page
 *
 * @ashActionType :update
 */
export async function renameBrainPage<Fields extends RenameBrainPageFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: RenameBrainPageInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RenameBrainPageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "rename_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RenameBrainPageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RestoreBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferRestoreBrainPageResult<
  Fields extends RestoreBrainPageFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type RestoreBrainPageResult<Fields extends RestoreBrainPageFields | undefined = undefined> = | { success: true; data: InferRestoreBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Page
 *
 * @ashActionType :update
 */
export async function restoreBrainPage<Fields extends RestoreBrainPageFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RestoreBrainPageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "restore_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RestoreBrainPageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RootBrainPagesInput = {
  brainId: UUID;
};

export type RootBrainPagesFields = UnifiedFieldSelection<BrainPageResourceSchema>[];
export type InferRootBrainPagesResult<
  Fields extends RootBrainPagesFields,
> = Array<InferResult<BrainPageResourceSchema, Fields>>;

export type RootBrainPagesResult<Fields extends RootBrainPagesFields> = | { success: true; data: InferRootBrainPagesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function rootBrainPages<Fields extends RootBrainPagesFields>(
  config: {
  tenant?: string;
  input: RootBrainPagesInput;
  fields: Fields;
  filter?: BrainPageFilterInput;
  sort?: SortString<BrainPageSortField> | SortString<BrainPageSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RootBrainPagesResult<Fields>> {
  const payload = {
    action: "root_brain_pages",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<RootBrainPagesResult<Fields>>(
    payload,
    config
  );
}


export type SaveBrainPageProsemirrorInput = {
  pageId: UUID;
  prosemirror: Record<string, any>;
  baseVersion: number;
};

export type InferSaveBrainPageProsemirrorResult = Record<string, any>;

export type SaveBrainPageProsemirrorResult = | { success: true; data: InferSaveBrainPageProsemirrorResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Page
 *
 * @ashActionType :action
 */
export async function saveBrainPageProsemirror(
  config: {
  tenant?: string;
  input: SaveBrainPageProsemirrorInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SaveBrainPageProsemirrorResult> {
  const payload = {
    action: "save_brain_page_prosemirror",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SaveBrainPageProsemirrorResult>(
    payload,
    config
  );
}


export type TrashBrainPageFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferTrashBrainPageResult<
  Fields extends TrashBrainPageFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type TrashBrainPageResult<Fields extends TrashBrainPageFields | undefined = undefined> = | { success: true; data: InferTrashBrainPageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Page
 *
 * @ashActionType :update
 */
export async function trashBrainPage<Fields extends TrashBrainPageFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TrashBrainPageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "trash_brain_page",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TrashBrainPageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TrashedBrainPagesInput = {
  workspaceId?: UUID | null;
};

export type TrashedBrainPagesFields = UnifiedFieldSelection<BrainPageResourceSchema>[];


export type InferTrashedBrainPagesResult<
  Fields extends TrashedBrainPagesFields | undefined,
  Page extends TrashedBrainPagesConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<BrainPageResourceSchema, Fields>>, {
  results: Array<InferResult<BrainPageResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
}>;

export type TrashedBrainPagesConfig = {
  tenant?: string;
  input?: TrashedBrainPagesInput;
  fields: TrashedBrainPagesFields;
  filter?: BrainPageFilterInput;
  sort?: SortString<BrainPageSortField> | SortString<BrainPageSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type TrashedBrainPagesResult<Fields extends TrashedBrainPagesFields, Page extends TrashedBrainPagesConfig["page"] = undefined> = | { success: true; data: InferTrashedBrainPagesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Page records
 *
 * @ashActionType :read
 */
export async function trashedBrainPages<Fields extends TrashedBrainPagesFields, Config extends TrashedBrainPagesConfig = TrashedBrainPagesConfig>(
  config: Config & { fields: Fields }
): Promise<TrashedBrainPagesResult<Fields, Config["page"]>> {
  const payload = {
    action: "trashed_brain_pages",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<TrashedBrainPagesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


export type UpdateBrainPageBodyInput = {
  body?: string | null;
  baseVersion: number;
};

export type UpdateBrainPageBodyFields = UnifiedFieldSelection<BrainPageResourceSchema>[];

export type InferUpdateBrainPageBodyResult<
  Fields extends UpdateBrainPageBodyFields | undefined,
> = InferResult<BrainPageResourceSchema, Fields>;

export type UpdateBrainPageBodyResult<Fields extends UpdateBrainPageBodyFields | undefined = undefined> = | { success: true; data: InferUpdateBrainPageBodyResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Page
 *
 * @ashActionType :update
 */
export async function updateBrainPageBody<Fields extends UpdateBrainPageBodyFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateBrainPageBodyInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateBrainPageBodyResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_brain_page_body",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateBrainPageBodyResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListPageBacklinksInput = {
  pageId: UUID;
};

export type ListPageBacklinksFields = UnifiedFieldSelection<BrainPageLinkResourceSchema>[];
export type InferListPageBacklinksResult<
  Fields extends ListPageBacklinksFields,
> = Array<InferResult<BrainPageLinkResourceSchema, Fields>>;

export type ListPageBacklinksResult<Fields extends ListPageBacklinksFields> = | { success: true; data: InferListPageBacklinksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PageLink records
 *
 * @ashActionType :read
 */
export async function listPageBacklinks<Fields extends ListPageBacklinksFields>(
  config: {
  tenant?: string;
  input: ListPageBacklinksInput;
  fields: Fields;
  filter?: BrainPageLinkFilterInput;
  sort?: SortString<BrainPageLinkSortField> | SortString<BrainPageLinkSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListPageBacklinksResult<Fields>> {
  const payload = {
    action: "list_page_backlinks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListPageBacklinksResult<Fields>>(
    payload,
    config
  );
}


export type ListPageSourcesInput = {
  pageId: UUID;
};

export type ListPageSourcesFields = UnifiedFieldSelection<BrainPageSourceResourceSchema>[];
export type InferListPageSourcesResult<
  Fields extends ListPageSourcesFields,
> = Array<InferResult<BrainPageSourceResourceSchema, Fields>>;

export type ListPageSourcesResult<Fields extends ListPageSourcesFields> = | { success: true; data: InferListPageSourcesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PageSource records
 *
 * @ashActionType :read
 */
export async function listPageSources<Fields extends ListPageSourcesFields>(
  config: {
  tenant?: string;
  input: ListPageSourcesInput;
  fields: Fields;
  filter?: BrainPageSourceFilterInput;
  sort?: SortString<BrainPageSourceSortField> | SortString<BrainPageSourceSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListPageSourcesResult<Fields>> {
  const payload = {
    action: "list_page_sources",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListPageSourcesResult<Fields>>(
    payload,
    config
  );
}


export type ClearContextWindowInput = {
  conversationId: UUID;
};

export type ClearContextWindowFields = UnifiedFieldSelection<context_windowResourceSchema>[];

export type InferClearContextWindowResult<
  Fields extends ClearContextWindowFields | undefined,
> = InferResult<context_windowResourceSchema, Fields>;

export type ClearContextWindowResult<Fields extends ClearContextWindowFields | undefined = undefined> = | { success: true; data: InferClearContextWindowResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ContextWindow
 *
 * @ashActionType :action
 */
export async function clearContextWindow<Fields extends ClearContextWindowFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: ClearContextWindowInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ClearContextWindowResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "clear_context_window",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ClearContextWindowResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CompactContextWindowInput = {
  conversationId: UUID;
};

export type CompactContextWindowFields = UnifiedFieldSelection<context_windowResourceSchema>[];

export type InferCompactContextWindowResult<
  Fields extends CompactContextWindowFields | undefined,
> = InferResult<context_windowResourceSchema, Fields>;

export type CompactContextWindowResult<Fields extends CompactContextWindowFields | undefined = undefined> = | { success: true; data: InferCompactContextWindowResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ContextWindow
 *
 * @ashActionType :action
 */
export async function compactContextWindow<Fields extends CompactContextWindowFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CompactContextWindowInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CompactContextWindowResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "compact_context_window",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CompactContextWindowResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetContextWindowInput = {
  conversationId: UUID;
};

export type GetContextWindowFields = UnifiedFieldSelection<context_windowResourceSchema>[];
export type InferGetContextWindowResult<
  Fields extends GetContextWindowFields,
> = InferResult<context_windowResourceSchema, Fields>;

export type GetContextWindowResult<Fields extends GetContextWindowFields> = | { success: true; data: InferGetContextWindowResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ContextWindow records
 *
 * @ashActionType :read
 */
export async function getContextWindow<Fields extends GetContextWindowFields>(
  config: {
  tenant?: string;
  input: GetContextWindowInput;
  getBy: {
    conversationId: UUID;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetContextWindowResult<Fields>> {
  const payload = {
    action: "get_context_window",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetContextWindowResult<Fields>>(
    payload,
    config
  );
}


export type SetContextStrategyInput = {
  conversationId: UUID;
  strategy?: "compact" | "rolling" | null;
};

export type SetContextStrategyFields = UnifiedFieldSelection<context_windowResourceSchema>[];

export type InferSetContextStrategyResult<
  Fields extends SetContextStrategyFields | undefined,
> = InferResult<context_windowResourceSchema, Fields>;

export type SetContextStrategyResult<Fields extends SetContextStrategyFields | undefined = undefined> = | { success: true; data: InferSetContextStrategyResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ContextWindow
 *
 * @ashActionType :action
 */
export async function setContextStrategy<Fields extends SetContextStrategyFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SetContextStrategyInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetContextStrategyResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_context_strategy",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetContextStrategyResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ActivateConversationPromptInput = {
  promptId: UUID;
};

export type ActivateConversationPromptFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferActivateConversationPromptResult<
  Fields extends ActivateConversationPromptFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type ActivateConversationPromptResult<Fields extends ActivateConversationPromptFields | undefined = undefined> = | { success: true; data: InferActivateConversationPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function activateConversationPrompt<Fields extends ActivateConversationPromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: ActivateConversationPromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ActivateConversationPromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "activate_conversation_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ActivateConversationPromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ArchiveConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferArchiveConversationResult<
  Fields extends ArchiveConversationFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type ArchiveConversationResult<Fields extends ArchiveConversationFields | undefined = undefined> = | { success: true; data: InferArchiveConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function archiveConversation<Fields extends ArchiveConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ArchiveConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "archive_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ArchiveConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationHistoryInput = {
  query?: string | null;
  workspaceId?: UUID | null;
};

export type ConversationHistoryFields = UnifiedFieldSelection<ConversationResourceSchema>[];


export type InferConversationHistoryResult<
  Fields extends ConversationHistoryFields,
> = {
  results: Array<InferResult<ConversationResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
};

export type ConversationHistoryResult<Fields extends ConversationHistoryFields> = | { success: true; data: InferConversationHistoryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function conversationHistory<Fields extends ConversationHistoryFields>(
  config: {
  tenant?: string;
  input?: ConversationHistoryInput;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  page: {
    limit?: number;
    offset?: number;
    after?: never;
    before?: never;
    count?: boolean;
  };
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationHistoryResult<Fields>> {
  const payload = {
    action: "conversation_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ConversationHistoryResult<Fields>>(
    payload,
    config
  );
}


export type ConversationThreadsInput = {
  conversationId: UUID;
};

export type ConversationThreadsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferConversationThreadsResult<
  Fields extends ConversationThreadsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type ConversationThreadsResult<Fields extends ConversationThreadsFields> = | { success: true; data: InferConversationThreadsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function conversationThreads<Fields extends ConversationThreadsFields>(
  config: {
  tenant?: string;
  input: ConversationThreadsInput;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationThreadsResult<Fields>> {
  const payload = {
    action: "conversation_threads",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationThreadsResult<Fields>>(
    payload,
    config
  );
}


export type ConversationsThreadsInput = {
  conversationIds: Array<UUID>;
};

export type ConversationsThreadsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferConversationsThreadsResult<
  Fields extends ConversationsThreadsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type ConversationsThreadsResult<Fields extends ConversationsThreadsFields> = | { success: true; data: InferConversationsThreadsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function conversationsThreads<Fields extends ConversationsThreadsFields>(
  config: {
  tenant?: string;
  input: ConversationsThreadsInput;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationsThreadsResult<Fields>> {
  const payload = {
    action: "conversations_threads",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationsThreadsResult<Fields>>(
    payload,
    config
  );
}


export type CreateConversationInput = {
  title?: string | null;
  folderId?: UUID | null;
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation";
  selectedModelId?: UUID | null;
  customAgentId?: UUID | null;
  skillContext?: string | null;
  skillTools?: Array<string> | null;
  loadedTools?: Array<string> | null;
  isTaskConversation?: boolean;
  parentConversationId?: UUID | null;
  sandboxConversationId?: UUID | null;
  systemPrompt?: string | null;
  workspaceId?: UUID | null;
};

export type CreateConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferCreateConversationResult<
  Fields extends CreateConversationFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type CreateConversationResult<Fields extends CreateConversationFields | undefined = undefined> = | { success: true; data: InferCreateConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Conversation
 *
 * @ashActionType :create
 */
export async function createConversation<Fields extends CreateConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input?: CreateConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreateThreadInput = {
  title?: string | null;
  parentConversationId: UUID;
  branchedAtMessageId: UUID;
};

export type CreateThreadFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferCreateThreadResult<
  Fields extends CreateThreadFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type CreateThreadResult<Fields extends CreateThreadFields | undefined = undefined> = | { success: true; data: InferCreateThreadResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Conversation
 *
 * @ashActionType :create
 */
export async function createThread<Fields extends CreateThreadFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateThreadInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateThreadResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_thread",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateThreadResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeactivateConversationPromptFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferDeactivateConversationPromptResult<
  Fields extends DeactivateConversationPromptFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type DeactivateConversationPromptResult<Fields extends DeactivateConversationPromptFields | undefined = undefined> = | { success: true; data: InferDeactivateConversationPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function deactivateConversationPrompt<Fields extends DeactivateConversationPromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeactivateConversationPromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "deactivate_conversation_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DeactivateConversationPromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeleteConversationPermanentlyResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Conversation
 *
 * @ashActionType :destroy
 */
export async function deleteConversationPermanently(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteConversationPermanentlyResult> {
  const payload = {
    action: "delete_conversation_permanently",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteConversationPermanentlyResult>(
    payload,
    config
  );
}


export type DisableConversationMultiplayerFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferDisableConversationMultiplayerResult<
  Fields extends DisableConversationMultiplayerFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type DisableConversationMultiplayerResult<Fields extends DisableConversationMultiplayerFields | undefined = undefined> = | { success: true; data: InferDisableConversationMultiplayerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function disableConversationMultiplayer<Fields extends DisableConversationMultiplayerFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DisableConversationMultiplayerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "disable_conversation_multiplayer",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DisableConversationMultiplayerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type EnableConversationMultiplayerFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferEnableConversationMultiplayerResult<
  Fields extends EnableConversationMultiplayerFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type EnableConversationMultiplayerResult<Fields extends EnableConversationMultiplayerFields | undefined = undefined> = | { success: true; data: InferEnableConversationMultiplayerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function enableConversationMultiplayer<Fields extends EnableConversationMultiplayerFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<EnableConversationMultiplayerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "enable_conversation_multiplayer",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<EnableConversationMultiplayerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferGetConversationResult<
  Fields extends GetConversationFields,
> = InferResult<ConversationResourceSchema, Fields>;

export type GetConversationResult<Fields extends GetConversationFields> = | { success: true; data: InferGetConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function getConversation<Fields extends GetConversationFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetConversationResult<Fields>> {
  const payload = {
    action: "get_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetConversationResult<Fields>>(
    payload,
    config
  );
}


export type MoveConversationToFolderInput = {
  folderId?: UUID | null;
};

export type MoveConversationToFolderFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferMoveConversationToFolderResult<
  Fields extends MoveConversationToFolderFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type MoveConversationToFolderResult<Fields extends MoveConversationToFolderFields | undefined = undefined> = | { success: true; data: InferMoveConversationToFolderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function moveConversationToFolder<Fields extends MoveConversationToFolderFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: MoveConversationToFolderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MoveConversationToFolderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "move_conversation_to_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MoveConversationToFolderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyConversationsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferMyConversationsResult<
  Fields extends MyConversationsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type MyConversationsResult<Fields extends MyConversationsFields> = | { success: true; data: InferMyConversationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function myConversations<Fields extends MyConversationsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyConversationsResult<Fields>> {
  const payload = {
    action: "my_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyConversationsResult<Fields>>(
    payload,
    config
  );
}


export type MyFavoriteConversationsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferMyFavoriteConversationsResult<
  Fields extends MyFavoriteConversationsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type MyFavoriteConversationsResult<Fields extends MyFavoriteConversationsFields> = | { success: true; data: InferMyFavoriteConversationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function myFavoriteConversations<Fields extends MyFavoriteConversationsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyFavoriteConversationsResult<Fields>> {
  const payload = {
    action: "my_favorite_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyFavoriteConversationsResult<Fields>>(
    payload,
    config
  );
}


export type PersonalConversationsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferPersonalConversationsResult<
  Fields extends PersonalConversationsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type PersonalConversationsResult<Fields extends PersonalConversationsFields> = | { success: true; data: InferPersonalConversationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function personalConversations<Fields extends PersonalConversationsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PersonalConversationsResult<Fields>> {
  const payload = {
    action: "personal_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<PersonalConversationsResult<Fields>>(
    payload,
    config
  );
}


export type RenameConversationInput = {
  title?: string | null;
};

export type RenameConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferRenameConversationResult<
  Fields extends RenameConversationFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type RenameConversationResult<Fields extends RenameConversationFields | undefined = undefined> = | { success: true; data: InferRenameConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function renameConversation<Fields extends RenameConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: RenameConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RenameConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "rename_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RenameConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ResetConversationSettingsFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferResetConversationSettingsResult<
  Fields extends ResetConversationSettingsFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type ResetConversationSettingsResult<Fields extends ResetConversationSettingsFields | undefined = undefined> = | { success: true; data: InferResetConversationSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function resetConversationSettings<Fields extends ResetConversationSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResetConversationSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "reset_conversation_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResetConversationSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RestoreConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferRestoreConversationResult<
  Fields extends RestoreConversationFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type RestoreConversationResult<Fields extends RestoreConversationFields | undefined = undefined> = | { success: true; data: InferRestoreConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function restoreConversation<Fields extends RestoreConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RestoreConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "restore_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RestoreConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SendNowQueuedInput = {
  conversationId: UUID;
};

export type InferSendNowQueuedResult = string;

export type SendNowQueuedResult = | { success: true; data: InferSendNowQueuedResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function sendNowQueued(
  config: {
  tenant?: string;
  input: SendNowQueuedInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SendNowQueuedResult> {
  const payload = {
    action: "send_now_queued",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SendNowQueuedResult>(
    payload,
    config
  );
}


export type SetConversationImageModelInput = {
  selectedImageModelId?: UUID | null;
};

export type SetConversationImageModelFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferSetConversationImageModelResult<
  Fields extends SetConversationImageModelFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type SetConversationImageModelResult<Fields extends SetConversationImageModelFields | undefined = undefined> = | { success: true; data: InferSetConversationImageModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function setConversationImageModel<Fields extends SetConversationImageModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetConversationImageModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetConversationImageModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_conversation_image_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetConversationImageModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetConversationModeInput = {
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation";
};

export type SetConversationModeFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferSetConversationModeResult<
  Fields extends SetConversationModeFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type SetConversationModeResult<Fields extends SetConversationModeFields | undefined = undefined> = | { success: true; data: InferSetConversationModeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function setConversationMode<Fields extends SetConversationModeFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetConversationModeInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetConversationModeResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_conversation_mode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetConversationModeResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetConversationModelInput = {
  selectedModelId?: UUID | null;
};

export type SetConversationModelFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferSetConversationModelResult<
  Fields extends SetConversationModelFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type SetConversationModelResult<Fields extends SetConversationModelFields | undefined = undefined> = | { success: true; data: InferSetConversationModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function setConversationModel<Fields extends SetConversationModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetConversationModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetConversationModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_conversation_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetConversationModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetConversationVideoModelInput = {
  selectedVideoModelId?: UUID | null;
};

export type SetConversationVideoModelFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferSetConversationVideoModelResult<
  Fields extends SetConversationVideoModelFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type SetConversationVideoModelResult<Fields extends SetConversationVideoModelFields | undefined = undefined> = | { success: true; data: InferSetConversationVideoModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function setConversationVideoModel<Fields extends SetConversationVideoModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetConversationVideoModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetConversationVideoModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_conversation_video_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetConversationVideoModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ShareConversationToTeamFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferShareConversationToTeamResult<
  Fields extends ShareConversationToTeamFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type ShareConversationToTeamResult<Fields extends ShareConversationToTeamFields | undefined = undefined> = | { success: true; data: InferShareConversationToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function shareConversationToTeam<Fields extends ShareConversationToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareConversationToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_conversation_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareConversationToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type StartSkillConversationInput = {
  skillName: string;
  topic?: string | null;
  workspaceId?: UUID | null;
};

export type StartSkillConversationFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferStartSkillConversationResult<
  Fields extends StartSkillConversationFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type StartSkillConversationResult<Fields extends StartSkillConversationFields | undefined = undefined> = | { success: true; data: InferStartSkillConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function startSkillConversation<Fields extends StartSkillConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: StartSkillConversationInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<StartSkillConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "start_skill_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<StartSkillConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TrashedConversationsFields = UnifiedFieldSelection<ConversationResourceSchema>[];


export type InferTrashedConversationsResult<
  Fields extends TrashedConversationsFields | undefined,
  Page extends TrashedConversationsConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<ConversationResourceSchema, Fields>>, {
  results: Array<InferResult<ConversationResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
}>;

export type TrashedConversationsConfig = {
  tenant?: string;
  fields: TrashedConversationsFields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type TrashedConversationsResult<Fields extends TrashedConversationsFields, Page extends TrashedConversationsConfig["page"] = undefined> = | { success: true; data: InferTrashedConversationsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function trashedConversations<Fields extends TrashedConversationsFields, Config extends TrashedConversationsConfig = TrashedConversationsConfig>(
  config: Config & { fields: Fields }
): Promise<TrashedConversationsResult<Fields, Config["page"]>> {
  const payload = {
    action: "trashed_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<TrashedConversationsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


export type UnshareConversationFromTeamFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferUnshareConversationFromTeamResult<
  Fields extends UnshareConversationFromTeamFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type UnshareConversationFromTeamResult<Fields extends UnshareConversationFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareConversationFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function unshareConversationFromTeam<Fields extends UnshareConversationFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareConversationFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_conversation_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareConversationFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateConversationImageSettingsInput = {
  imageGenerationSettings?: Record<string, any> | null;
};

export type UpdateConversationImageSettingsFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferUpdateConversationImageSettingsResult<
  Fields extends UpdateConversationImageSettingsFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type UpdateConversationImageSettingsResult<Fields extends UpdateConversationImageSettingsFields | undefined = undefined> = | { success: true; data: InferUpdateConversationImageSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function updateConversationImageSettings<Fields extends UpdateConversationImageSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: UpdateConversationImageSettingsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateConversationImageSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_conversation_image_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateConversationImageSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateConversationSettingsInput = {
  systemPrompt?: string | null;
  samplingSettings?: Record<string, any> | null;
};

export type UpdateConversationSettingsFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferUpdateConversationSettingsResult<
  Fields extends UpdateConversationSettingsFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type UpdateConversationSettingsResult<Fields extends UpdateConversationSettingsFields | undefined = undefined> = | { success: true; data: InferUpdateConversationSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function updateConversationSettings<Fields extends UpdateConversationSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: UpdateConversationSettingsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateConversationSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_conversation_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateConversationSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateConversationVideoSettingsInput = {
  videoGenerationSettings?: Record<string, any> | null;
};

export type UpdateConversationVideoSettingsFields = UnifiedFieldSelection<ConversationResourceSchema>[];

export type InferUpdateConversationVideoSettingsResult<
  Fields extends UpdateConversationVideoSettingsFields | undefined,
> = InferResult<ConversationResourceSchema, Fields>;

export type UpdateConversationVideoSettingsResult<Fields extends UpdateConversationVideoSettingsFields | undefined = undefined> = | { success: true; data: InferUpdateConversationVideoSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function updateConversationVideoSettings<Fields extends UpdateConversationVideoSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: UpdateConversationVideoSettingsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateConversationVideoSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_conversation_video_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateConversationVideoSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceConversationsInput = {
  workspaceId: UUID;
};

export type WorkspaceConversationsFields = UnifiedFieldSelection<ConversationResourceSchema>[];
export type InferWorkspaceConversationsResult<
  Fields extends WorkspaceConversationsFields,
> = Array<InferResult<ConversationResourceSchema, Fields>>;

export type WorkspaceConversationsResult<Fields extends WorkspaceConversationsFields> = | { success: true; data: InferWorkspaceConversationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function workspaceConversations<Fields extends WorkspaceConversationsFields>(
  config: {
  tenant?: string;
  input: WorkspaceConversationsInput;
  fields: Fields;
  filter?: ConversationFilterInput;
  sort?: SortString<ConversationSortField> | SortString<ConversationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceConversationsResult<Fields>> {
  const payload = {
    action: "workspace_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceConversationsResult<Fields>>(
    payload,
    config
  );
}


export type OpenCompanionChatInput = {
  resourceType: "brain_page" | "file";
  resourceId: UUID;
};

export type InferOpenCompanionChatResult = Record<string, any>;

export type OpenCompanionChatResult = | { success: true; data: InferOpenCompanionChatResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ConversationCompanion
 *
 * @ashActionType :action
 */
export async function openCompanionChat(
  config: {
  tenant?: string;
  input: OpenCompanionChatInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<OpenCompanionChatResult> {
  const payload = {
    action: "open_companion_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<OpenCompanionChatResult>(
    payload,
    config
  );
}


export type FavoriteConversationInput = {
  conversationId: UUID;
};

export type FavoriteConversationFields = UnifiedFieldSelection<ConversationFavoriteResourceSchema>[];

export type InferFavoriteConversationResult<
  Fields extends FavoriteConversationFields | undefined,
> = InferResult<ConversationFavoriteResourceSchema, Fields>;

export type FavoriteConversationResult<Fields extends FavoriteConversationFields | undefined = undefined> = | { success: true; data: InferFavoriteConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ConversationFavorite
 *
 * @ashActionType :create
 */
export async function favoriteConversation<Fields extends FavoriteConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: FavoriteConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FavoriteConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "favorite_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<FavoriteConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyConversationFavoritesFields = UnifiedFieldSelection<ConversationFavoriteResourceSchema>[];
export type InferMyConversationFavoritesResult<
  Fields extends MyConversationFavoritesFields,
> = Array<InferResult<ConversationFavoriteResourceSchema, Fields>>;

export type MyConversationFavoritesResult<Fields extends MyConversationFavoritesFields> = | { success: true; data: InferMyConversationFavoritesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ConversationFavorite records
 *
 * @ashActionType :read
 */
export async function myConversationFavorites<Fields extends MyConversationFavoritesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ConversationFavoriteFilterInput;
  sort?: SortString<ConversationFavoriteSortField> | SortString<ConversationFavoriteSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyConversationFavoritesResult<Fields>> {
  const payload = {
    action: "my_conversation_favorites",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyConversationFavoritesResult<Fields>>(
    payload,
    config
  );
}


export type RemoveConversationFavoriteInput = {
  conversationId: UUID;
};

export type InferRemoveConversationFavoriteResult = "ok";

export type RemoveConversationFavoriteResult = | { success: true; data: InferRemoveConversationFavoriteResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ConversationFavorite
 *
 * @ashActionType :action
 */
export async function removeConversationFavorite(
  config: {
  tenant?: string;
  input: RemoveConversationFavoriteInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveConversationFavoriteResult> {
  const payload = {
    action: "remove_conversation_favorite",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<RemoveConversationFavoriteResult>(
    payload,
    config
  );
}


export type UnfavoriteConversationResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a ConversationFavorite
 *
 * @ashActionType :destroy
 */
export async function unfavoriteConversation(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnfavoriteConversationResult> {
  const payload = {
    action: "unfavorite_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<UnfavoriteConversationResult>(
    payload,
    config
  );
}


export type CancelConversationInvitationResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a ConversationInvitation
 *
 * @ashActionType :destroy
 */
export async function cancelConversationInvitation(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CancelConversationInvitationResult> {
  const payload = {
    action: "cancel_conversation_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<CancelConversationInvitationResult>(
    payload,
    config
  );
}


export type InviteToConversationInput = {
  email: string;
  role?: "member" | "observer";
  conversationId: UUID;
};

export type InviteToConversationFields = UnifiedFieldSelection<ConversationInvitationResourceSchema>[];

export type InferInviteToConversationResult<
  Fields extends InviteToConversationFields | undefined,
> = InferResult<ConversationInvitationResourceSchema, Fields>;

export type InviteToConversationResult<Fields extends InviteToConversationFields | undefined = undefined> = | { success: true; data: InferInviteToConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ConversationInvitation
 *
 * @ashActionType :create
 */
export async function inviteToConversation<Fields extends InviteToConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: InviteToConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InviteToConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "invite_to_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<InviteToConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type PendingConversationInvitationsInput = {
  conversationId: UUID;
};

export type PendingConversationInvitationsFields = UnifiedFieldSelection<ConversationInvitationResourceSchema>[];
export type InferPendingConversationInvitationsResult<
  Fields extends PendingConversationInvitationsFields,
> = Array<InferResult<ConversationInvitationResourceSchema, Fields>>;

export type PendingConversationInvitationsResult<Fields extends PendingConversationInvitationsFields> = | { success: true; data: InferPendingConversationInvitationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ConversationInvitation records
 *
 * @ashActionType :read
 */
export async function pendingConversationInvitations<Fields extends PendingConversationInvitationsFields>(
  config: {
  tenant?: string;
  input: PendingConversationInvitationsInput;
  fields: Fields;
  filter?: ConversationInvitationFilterInput;
  sort?: SortString<ConversationInvitationSortField> | SortString<ConversationInvitationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PendingConversationInvitationsResult<Fields>> {
  const payload = {
    action: "pending_conversation_invitations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<PendingConversationInvitationsResult<Fields>>(
    payload,
    config
  );
}


export type ConversationInviteLinksInput = {
  conversationId: UUID;
};

export type ConversationInviteLinksFields = UnifiedFieldSelection<ConversationInviteLinkResourceSchema>[];
export type InferConversationInviteLinksResult<
  Fields extends ConversationInviteLinksFields,
> = Array<InferResult<ConversationInviteLinkResourceSchema, Fields>>;

export type ConversationInviteLinksResult<Fields extends ConversationInviteLinksFields> = | { success: true; data: InferConversationInviteLinksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ConversationInviteLink records
 *
 * @ashActionType :read
 */
export async function conversationInviteLinks<Fields extends ConversationInviteLinksFields>(
  config: {
  tenant?: string;
  input: ConversationInviteLinksInput;
  fields: Fields;
  filter?: ConversationInviteLinkFilterInput;
  sort?: SortString<ConversationInviteLinkSortField> | SortString<ConversationInviteLinkSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationInviteLinksResult<Fields>> {
  const payload = {
    action: "conversation_invite_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationInviteLinksResult<Fields>>(
    payload,
    config
  );
}


export type CreateConversationInviteLinkInput = {
  expiresAt?: UtcDateTimeUsec | null;
  maxUses?: number | null;
  role?: "member" | "observer";
  conversationId: UUID;
  password?: string | null;
};

export type CreateConversationInviteLinkFields = UnifiedFieldSelection<ConversationInviteLinkResourceSchema>[];

export type InferCreateConversationInviteLinkResult<
  Fields extends CreateConversationInviteLinkFields | undefined,
> = InferResult<ConversationInviteLinkResourceSchema, Fields>;

export type CreateConversationInviteLinkResult<Fields extends CreateConversationInviteLinkFields | undefined = undefined> = | { success: true; data: InferCreateConversationInviteLinkResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ConversationInviteLink
 *
 * @ashActionType :create
 */
export async function createConversationInviteLink<Fields extends CreateConversationInviteLinkFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateConversationInviteLinkInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateConversationInviteLinkResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_conversation_invite_link",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateConversationInviteLinkResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeactivateConversationInviteLinkFields = UnifiedFieldSelection<ConversationInviteLinkResourceSchema>[];

export type InferDeactivateConversationInviteLinkResult<
  Fields extends DeactivateConversationInviteLinkFields | undefined,
> = InferResult<ConversationInviteLinkResourceSchema, Fields>;

export type DeactivateConversationInviteLinkResult<Fields extends DeactivateConversationInviteLinkFields | undefined = undefined> = | { success: true; data: InferDeactivateConversationInviteLinkResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ConversationInviteLink
 *
 * @ashActionType :update
 */
export async function deactivateConversationInviteLink<Fields extends DeactivateConversationInviteLinkFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeactivateConversationInviteLinkResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "deactivate_conversation_invite_link",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DeactivateConversationInviteLinkResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ChangeMemberRoleInput = {
  role?: "member" | "observer" | "owner";
};

export type ChangeMemberRoleFields = UnifiedFieldSelection<ConversationMemberResourceSchema>[];

export type InferChangeMemberRoleResult<
  Fields extends ChangeMemberRoleFields | undefined,
> = InferResult<ConversationMemberResourceSchema, Fields>;

export type ChangeMemberRoleResult<Fields extends ChangeMemberRoleFields | undefined = undefined> = | { success: true; data: InferChangeMemberRoleResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ConversationMember
 *
 * @ashActionType :update
 */
export async function changeMemberRole<Fields extends ChangeMemberRoleFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: ChangeMemberRoleInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ChangeMemberRoleResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "change_member_role",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ChangeMemberRoleResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationMembersInput = {
  conversationId: UUID;
};

export type ConversationMembersFields = UnifiedFieldSelection<ConversationMemberResourceSchema>[];
export type InferConversationMembersResult<
  Fields extends ConversationMembersFields,
> = Array<InferResult<ConversationMemberResourceSchema, Fields>>;

export type ConversationMembersResult<Fields extends ConversationMembersFields> = | { success: true; data: InferConversationMembersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ConversationMember records
 *
 * @ashActionType :read
 */
export async function conversationMembers<Fields extends ConversationMembersFields>(
  config: {
  tenant?: string;
  input: ConversationMembersInput;
  fields: Fields;
  filter?: ConversationMemberFilterInput;
  sort?: SortString<ConversationMemberSortField> | SortString<ConversationMemberSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationMembersResult<Fields>> {
  const payload = {
    action: "conversation_members",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationMembersResult<Fields>>(
    payload,
    config
  );
}


export type MuteConversationMemberFields = UnifiedFieldSelection<ConversationMemberResourceSchema>[];

export type InferMuteConversationMemberResult<
  Fields extends MuteConversationMemberFields | undefined,
> = InferResult<ConversationMemberResourceSchema, Fields>;

export type MuteConversationMemberResult<Fields extends MuteConversationMemberFields | undefined = undefined> = | { success: true; data: InferMuteConversationMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ConversationMember
 *
 * @ashActionType :update
 */
export async function muteConversationMember<Fields extends MuteConversationMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MuteConversationMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "mute_conversation_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MuteConversationMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RemoveConversationMemberResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a ConversationMember
 *
 * @ashActionType :destroy
 */
export async function removeConversationMember(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveConversationMemberResult> {
  const payload = {
    action: "remove_conversation_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<RemoveConversationMemberResult>(
    payload,
    config
  );
}


export type UnmuteConversationMemberFields = UnifiedFieldSelection<ConversationMemberResourceSchema>[];

export type InferUnmuteConversationMemberResult<
  Fields extends UnmuteConversationMemberFields | undefined,
> = InferResult<ConversationMemberResourceSchema, Fields>;

export type UnmuteConversationMemberResult<Fields extends UnmuteConversationMemberFields | undefined = undefined> = | { success: true; data: InferUnmuteConversationMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ConversationMember
 *
 * @ashActionType :update
 */
export async function unmuteConversationMember<Fields extends UnmuteConversationMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnmuteConversationMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unmute_conversation_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnmuteConversationMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationShareLinksInput = {
  conversationId: UUID;
};

export type ConversationShareLinksFields = UnifiedFieldSelection<ConversationShareLinkResourceSchema>[];
export type InferConversationShareLinksResult<
  Fields extends ConversationShareLinksFields,
> = Array<InferResult<ConversationShareLinkResourceSchema, Fields>>;

export type ConversationShareLinksResult<Fields extends ConversationShareLinksFields> = | { success: true; data: InferConversationShareLinksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ConversationShareLink records
 *
 * @ashActionType :read
 */
export async function conversationShareLinks<Fields extends ConversationShareLinksFields>(
  config: {
  tenant?: string;
  input: ConversationShareLinksInput;
  fields: Fields;
  filter?: ConversationShareLinkFilterInput;
  sort?: SortString<ConversationShareLinkSortField> | SortString<ConversationShareLinkSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationShareLinksResult<Fields>> {
  const payload = {
    action: "conversation_share_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationShareLinksResult<Fields>>(
    payload,
    config
  );
}


export type CreateShareLinkInput = {
  accessType?: "authenticated" | "public";
  label?: string | null;
  conversationId: UUID;
};

export type CreateShareLinkFields = UnifiedFieldSelection<ConversationShareLinkResourceSchema>[];

export type InferCreateShareLinkResult<
  Fields extends CreateShareLinkFields | undefined,
> = InferResult<ConversationShareLinkResourceSchema, Fields>;

export type CreateShareLinkResult<Fields extends CreateShareLinkFields | undefined = undefined> = | { success: true; data: InferCreateShareLinkResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ConversationShareLink
 *
 * @ashActionType :create
 */
export async function createShareLink<Fields extends CreateShareLinkFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateShareLinkInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateShareLinkResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_share_link",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateShareLinkResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RevokeShareLinkFields = UnifiedFieldSelection<ConversationShareLinkResourceSchema>[];

export type InferRevokeShareLinkResult<
  Fields extends RevokeShareLinkFields | undefined,
> = InferResult<ConversationShareLinkResourceSchema, Fields>;

export type RevokeShareLinkResult<Fields extends RevokeShareLinkFields | undefined = undefined> = | { success: true; data: InferRevokeShareLinkResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ConversationShareLink
 *
 * @ashActionType :update
 */
export async function revokeShareLink<Fields extends RevokeShareLinkFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RevokeShareLinkResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "revoke_share_link",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RevokeShareLinkResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreateFolderInput = {
  name: string;
  parentId?: UUID | null;
  position?: number | null;
  workspaceId?: UUID | null;
  kind?: "conversations" | "files" | "mixed";
};

export type CreateFolderFields = UnifiedFieldSelection<FolderResourceSchema>[];

export type InferCreateFolderResult<
  Fields extends CreateFolderFields | undefined,
> = InferResult<FolderResourceSchema, Fields>;

export type CreateFolderResult<Fields extends CreateFolderFields | undefined = undefined> = | { success: true; data: InferCreateFolderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Folder
 *
 * @ashActionType :create
 */
export async function createFolder<Fields extends CreateFolderFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateFolderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateFolderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateFolderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeleteFolderResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Folder
 *
 * @ashActionType :destroy
 */
export async function deleteFolder(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteFolderResult> {
  const payload = {
    action: "delete_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteFolderResult>(
    payload,
    config
  );
}


export type FolderChildrenInput = {
  parentId: UUID;
  kinds?: Array<"conversations" | "files" | "mixed"> | null;
};

export type FolderChildrenFields = UnifiedFieldSelection<FolderResourceSchema>[];
export type InferFolderChildrenResult<
  Fields extends FolderChildrenFields,
> = Array<InferResult<FolderResourceSchema, Fields>>;

export type FolderChildrenResult<Fields extends FolderChildrenFields> = | { success: true; data: InferFolderChildrenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Folder records
 *
 * @ashActionType :read
 */
export async function folderChildren<Fields extends FolderChildrenFields>(
  config: {
  tenant?: string;
  input: FolderChildrenInput;
  fields: Fields;
  filter?: FolderFilterInput;
  sort?: SortString<FolderSortField> | SortString<FolderSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FolderChildrenResult<Fields>> {
  const payload = {
    action: "folder_children",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<FolderChildrenResult<Fields>>(
    payload,
    config
  );
}


export type GetFolderFields = UnifiedFieldSelection<FolderResourceSchema>[];
export type InferGetFolderResult<
  Fields extends GetFolderFields,
> = InferResult<FolderResourceSchema, Fields>;

export type GetFolderResult<Fields extends GetFolderFields> = | { success: true; data: InferGetFolderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Folder records
 *
 * @ashActionType :read
 */
export async function getFolder<Fields extends GetFolderFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetFolderResult<Fields>> {
  const payload = {
    action: "get_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetFolderResult<Fields>>(
    payload,
    config
  );
}


export type MoveFolderInput = {
  parentId?: UUID | null;
};

export type MoveFolderFields = UnifiedFieldSelection<FolderResourceSchema>[];

export type InferMoveFolderResult<
  Fields extends MoveFolderFields | undefined,
> = InferResult<FolderResourceSchema, Fields>;

export type MoveFolderResult<Fields extends MoveFolderFields | undefined = undefined> = | { success: true; data: InferMoveFolderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Folder
 *
 * @ashActionType :update
 */
export async function moveFolder<Fields extends MoveFolderFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: MoveFolderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MoveFolderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "move_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MoveFolderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyFoldersInput = {
  kinds?: Array<"conversations" | "files" | "mixed"> | null;
};

export type MyFoldersFields = UnifiedFieldSelection<FolderResourceSchema>[];
export type InferMyFoldersResult<
  Fields extends MyFoldersFields,
> = Array<InferResult<FolderResourceSchema, Fields>>;

export type MyFoldersResult<Fields extends MyFoldersFields> = | { success: true; data: InferMyFoldersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Folder records
 *
 * @ashActionType :read
 */
export async function myFolders<Fields extends MyFoldersFields>(
  config: {
  tenant?: string;
  input?: MyFoldersInput;
  fields: Fields;
  filter?: FolderFilterInput;
  sort?: SortString<FolderSortField> | SortString<FolderSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyFoldersResult<Fields>> {
  const payload = {
    action: "my_folders",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyFoldersResult<Fields>>(
    payload,
    config
  );
}


export type RenameFolderInput = {
  name?: string;
  position?: number | null;
};

export type RenameFolderFields = UnifiedFieldSelection<FolderResourceSchema>[];

export type InferRenameFolderResult<
  Fields extends RenameFolderFields | undefined,
> = InferResult<FolderResourceSchema, Fields>;

export type RenameFolderResult<Fields extends RenameFolderFields | undefined = undefined> = | { success: true; data: InferRenameFolderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Folder
 *
 * @ashActionType :update
 */
export async function renameFolder<Fields extends RenameFolderFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: RenameFolderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RenameFolderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "rename_folder",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RenameFolderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ShareFolderToTeamFields = UnifiedFieldSelection<FolderResourceSchema>[];

export type InferShareFolderToTeamResult<
  Fields extends ShareFolderToTeamFields | undefined,
> = InferResult<FolderResourceSchema, Fields>;

export type ShareFolderToTeamResult<Fields extends ShareFolderToTeamFields | undefined = undefined> = | { success: true; data: InferShareFolderToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Folder
 *
 * @ashActionType :update
 */
export async function shareFolderToTeam<Fields extends ShareFolderToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareFolderToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_folder_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareFolderToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnshareFolderFromTeamFields = UnifiedFieldSelection<FolderResourceSchema>[];

export type InferUnshareFolderFromTeamResult<
  Fields extends UnshareFolderFromTeamFields | undefined,
> = InferResult<FolderResourceSchema, Fields>;

export type UnshareFolderFromTeamResult<Fields extends UnshareFolderFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareFolderFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Folder
 *
 * @ashActionType :update
 */
export async function unshareFolderFromTeam<Fields extends UnshareFolderFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareFolderFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_folder_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareFolderFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceFoldersInput = {
  workspaceId: UUID;
  kinds?: Array<"conversations" | "files" | "mixed"> | null;
};

export type WorkspaceFoldersFields = UnifiedFieldSelection<FolderResourceSchema>[];
export type InferWorkspaceFoldersResult<
  Fields extends WorkspaceFoldersFields,
> = Array<InferResult<FolderResourceSchema, Fields>>;

export type WorkspaceFoldersResult<Fields extends WorkspaceFoldersFields> = | { success: true; data: InferWorkspaceFoldersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Folder records
 *
 * @ashActionType :read
 */
export async function workspaceFolders<Fields extends WorkspaceFoldersFields>(
  config: {
  tenant?: string;
  input: WorkspaceFoldersInput;
  fields: Fields;
  filter?: FolderFilterInput;
  sort?: SortString<FolderSortField> | SortString<FolderSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceFoldersResult<Fields>> {
  const payload = {
    action: "workspace_folders",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceFoldersResult<Fields>>(
    payload,
    config
  );
}


export type DeleteMessageResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Message
 *
 * @ashActionType :destroy
 */
export async function deleteMessage(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteMessageResult> {
  const payload = {
    action: "delete_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteMessageResult>(
    payload,
    config
  );
}


export type EnqueueMessageInput = {
  text: string;
  mode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation";
  selectedModelId?: UUID | null;
  metadata?: Record<string, any>;
  conversationId?: UUID | null;
  folderId?: UUID | null;
  customAgentId?: UUID | null;
  workspaceId?: UUID | null;
  resources?: Array<Record<string, any>> | null;
};

export type EnqueueMessageFields = UnifiedFieldSelection<MessageResourceSchema>[];

export type InferEnqueueMessageResult<
  Fields extends EnqueueMessageFields | undefined,
> = InferResult<MessageResourceSchema, Fields>;

export type EnqueueMessageResult<Fields extends EnqueueMessageFields | undefined = undefined> = | { success: true; data: InferEnqueueMessageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Message
 *
 * @ashActionType :create
 */
export async function enqueueMessage<Fields extends EnqueueMessageFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: EnqueueMessageInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<EnqueueMessageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "enqueue_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<EnqueueMessageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MessageHistoryInput = {
  conversationId: UUID;
};

export type MessageHistoryFields = UnifiedFieldSelection<MessageResourceSchema>[];


export type InferMessageHistoryResult<
  Fields extends MessageHistoryFields | undefined,
  Page extends MessageHistoryConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<MessageResourceSchema, Fields>>, {
  results: Array<InferResult<MessageResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
}>;

export type MessageHistoryConfig = {
  tenant?: string;
  input: MessageHistoryInput;
  fields: MessageHistoryFields;
  filter?: MessageFilterInput;
  sort?: SortString<MessageSortField> | SortString<MessageSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type MessageHistoryResult<Fields extends MessageHistoryFields, Page extends MessageHistoryConfig["page"] = undefined> = | { success: true; data: InferMessageHistoryResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Message records
 *
 * @ashActionType :read
 */
export async function messageHistory<Fields extends MessageHistoryFields, Config extends MessageHistoryConfig = MessageHistoryConfig>(
  config: Config & { fields: Fields }
): Promise<MessageHistoryResult<Fields, Config["page"]>> {
  const payload = {
    action: "message_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<MessageHistoryResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


export type MessagesSinceInput = {
  conversationId: UUID;
  since: UtcDateTimeUsec;
};

export type MessagesSinceFields = UnifiedFieldSelection<MessageResourceSchema>[];
export type InferMessagesSinceResult<
  Fields extends MessagesSinceFields,
> = Array<InferResult<MessageResourceSchema, Fields>>;

export type MessagesSinceResult<Fields extends MessagesSinceFields> = | { success: true; data: InferMessagesSinceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Message records
 *
 * @ashActionType :read
 */
export async function messagesSince<Fields extends MessagesSinceFields>(
  config: {
  tenant?: string;
  input: MessagesSinceInput;
  fields: Fields;
  filter?: MessageFilterInput;
  sort?: SortString<MessageSortField> | SortString<MessageSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MessagesSinceResult<Fields>> {
  const payload = {
    action: "messages_since",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MessagesSinceResult<Fields>>(
    payload,
    config
  );
}


export type RemoveQueuedResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Message
 *
 * @ashActionType :destroy
 */
export async function removeQueued(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveQueuedResult> {
  const payload = {
    action: "remove_queued",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<RemoveQueuedResult>(
    payload,
    config
  );
}


export type SendUserMessageInput = {
  text: string;
  mode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation";
  selectedModelId?: UUID | null;
  metadata?: Record<string, any>;
  conversationId?: UUID | null;
  folderId?: UUID | null;
  customAgentId?: UUID | null;
  systemPromptId?: UUID | null;
  workspaceId?: UUID | null;
  resources?: Array<Record<string, any>> | null;
};

export type SendUserMessageFields = UnifiedFieldSelection<MessageResourceSchema>[];

export type InferSendUserMessageResult<
  Fields extends SendUserMessageFields | undefined,
> = InferResult<MessageResourceSchema, Fields>;

export type SendUserMessageResult<Fields extends SendUserMessageFields | undefined = undefined> = | { success: true; data: InferSendUserMessageResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Message
 *
 * @ashActionType :create
 */
export async function sendUserMessage<Fields extends SendUserMessageFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SendUserMessageInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SendUserMessageResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "send_user_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SendUserMessageResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ToggleMessageDisabledFields = UnifiedFieldSelection<MessageResourceSchema>[];

export type InferToggleMessageDisabledResult<
  Fields extends ToggleMessageDisabledFields | undefined,
> = InferResult<MessageResourceSchema, Fields>;

export type ToggleMessageDisabledResult<Fields extends ToggleMessageDisabledFields | undefined = undefined> = | { success: true; data: InferToggleMessageDisabledResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Message
 *
 * @ashActionType :update
 */
export async function toggleMessageDisabled<Fields extends ToggleMessageDisabledFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ToggleMessageDisabledResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "toggle_message_disabled",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ToggleMessageDisabledResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreateOwnedModelInput = {
  name: string;
  provider?: string | null;
  modelProviderId?: UUID | null;
  contextWindow?: number | null;
  inputCostValue?: Decimal | null;
  outputCostValue?: Decimal | null;
  inputCostUnit?: "per_image" | "per_megapixel" | "per_million_tokens" | "per_second" | "per_video";
  outputCostUnit?: "per_image" | "per_megapixel" | "per_million_tokens" | "per_second" | "per_video";
  outputModalities?: Array<string>;
  inputModalities?: Array<string>;
  supportsTools?: boolean;
  supportsReasoning?: boolean;
  shortDescription?: string | null;
  modelId: string;
};

export type CreateOwnedModelFields = UnifiedFieldSelection<ModelResourceSchema>[];

export type InferCreateOwnedModelResult<
  Fields extends CreateOwnedModelFields | undefined,
> = InferResult<ModelResourceSchema, Fields>;

export type CreateOwnedModelResult<Fields extends CreateOwnedModelFields | undefined = undefined> = | { success: true; data: InferCreateOwnedModelResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Model
 *
 * @ashActionType :create
 */
export async function createOwnedModel<Fields extends CreateOwnedModelFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOwnedModelInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOwnedModelResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_owned_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOwnedModelResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyOwnedModelResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Model
 *
 * @ashActionType :destroy
 */
export async function destroyOwnedModel(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyOwnedModelResult> {
  const payload = {
    action: "destroy_owned_model",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyOwnedModelResult>(
    payload,
    config
  );
}


export type ListActiveModelsFields = UnifiedFieldSelection<ModelResourceSchema>[];
export type InferListActiveModelsResult<
  Fields extends ListActiveModelsFields,
> = Array<InferResult<ModelResourceSchema, Fields>>;

export type ListActiveModelsResult<Fields extends ListActiveModelsFields> = | { success: true; data: InferListActiveModelsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Model records
 *
 * @ashActionType :read
 */
export async function listActiveModels<Fields extends ListActiveModelsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ModelFilterInput;
  sort?: SortString<ModelSortField> | SortString<ModelSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListActiveModelsResult<Fields>> {
  const payload = {
    action: "list_active_models",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListActiveModelsResult<Fields>>(
    payload,
    config
  );
}


export type ListImageGenerationModelsFields = UnifiedFieldSelection<ModelResourceSchema>[];
export type InferListImageGenerationModelsResult<
  Fields extends ListImageGenerationModelsFields,
> = Array<InferResult<ModelResourceSchema, Fields>>;

export type ListImageGenerationModelsResult<Fields extends ListImageGenerationModelsFields> = | { success: true; data: InferListImageGenerationModelsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Model records
 *
 * @ashActionType :read
 */
export async function listImageGenerationModels<Fields extends ListImageGenerationModelsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ModelFilterInput;
  sort?: SortString<ModelSortField> | SortString<ModelSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListImageGenerationModelsResult<Fields>> {
  const payload = {
    action: "list_image_generation_models",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListImageGenerationModelsResult<Fields>>(
    payload,
    config
  );
}


export type ListOwnedModelsFields = UnifiedFieldSelection<ModelResourceSchema>[];
export type InferListOwnedModelsResult<
  Fields extends ListOwnedModelsFields,
> = Array<InferResult<ModelResourceSchema, Fields>>;

export type ListOwnedModelsResult<Fields extends ListOwnedModelsFields> = | { success: true; data: InferListOwnedModelsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Model records
 *
 * @ashActionType :read
 */
export async function listOwnedModels<Fields extends ListOwnedModelsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ModelFilterInput;
  sort?: SortString<ModelSortField> | SortString<ModelSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListOwnedModelsResult<Fields>> {
  const payload = {
    action: "list_owned_models",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListOwnedModelsResult<Fields>>(
    payload,
    config
  );
}


export type ListVideoGenerationModelsFields = UnifiedFieldSelection<ModelResourceSchema>[];
export type InferListVideoGenerationModelsResult<
  Fields extends ListVideoGenerationModelsFields,
> = Array<InferResult<ModelResourceSchema, Fields>>;

export type ListVideoGenerationModelsResult<Fields extends ListVideoGenerationModelsFields> = | { success: true; data: InferListVideoGenerationModelsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Model records
 *
 * @ashActionType :read
 */
export async function listVideoGenerationModels<Fields extends ListVideoGenerationModelsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ModelFilterInput;
  sort?: SortString<ModelSortField> | SortString<ModelSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListVideoGenerationModelsResult<Fields>> {
  const payload = {
    action: "list_video_generation_models",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListVideoGenerationModelsResult<Fields>>(
    payload,
    config
  );
}


export type MyFolderStatesFields = UnifiedFieldSelection<UserFolderStateResourceSchema>[];
export type InferMyFolderStatesResult<
  Fields extends MyFolderStatesFields,
> = Array<InferResult<UserFolderStateResourceSchema, Fields>>;

export type MyFolderStatesResult<Fields extends MyFolderStatesFields> = | { success: true; data: InferMyFolderStatesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read UserFolderState records
 *
 * @ashActionType :read
 */
export async function myFolderStates<Fields extends MyFolderStatesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: UserFolderStateFilterInput;
  sort?: SortString<UserFolderStateSortField> | SortString<UserFolderStateSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyFolderStatesResult<Fields>> {
  const payload = {
    action: "my_folder_states",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyFolderStatesResult<Fields>>(
    payload,
    config
  );
}


export type UpsertFolderExpandedInput = {
  folderId: UUID;
  isExpanded?: boolean;
};

export type UpsertFolderExpandedFields = UnifiedFieldSelection<UserFolderStateResourceSchema>[];

export type InferUpsertFolderExpandedResult<
  Fields extends UpsertFolderExpandedFields | undefined,
> = InferResult<UserFolderStateResourceSchema, Fields>;

export type UpsertFolderExpandedResult<Fields extends UpsertFolderExpandedFields | undefined = undefined> = | { success: true; data: InferUpsertFolderExpandedResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new UserFolderState
 *
 * @ashActionType :create
 */
export async function upsertFolderExpanded<Fields extends UpsertFolderExpandedFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: UpsertFolderExpandedInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpsertFolderExpandedResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "upsert_folder_expanded",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpsertFolderExpandedResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyModelPreferencesFields = UnifiedFieldSelection<UserModelPreferenceResourceSchema>[];
export type InferMyModelPreferencesResult<
  Fields extends MyModelPreferencesFields,
> = Array<InferResult<UserModelPreferenceResourceSchema, Fields>>;

export type MyModelPreferencesResult<Fields extends MyModelPreferencesFields> = | { success: true; data: InferMyModelPreferencesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read UserModelPreference records
 *
 * @ashActionType :read
 */
export async function myModelPreferences<Fields extends MyModelPreferencesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: UserModelPreferenceFilterInput;
  sort?: SortString<UserModelPreferenceSortField> | SortString<UserModelPreferenceSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyModelPreferencesResult<Fields>> {
  const payload = {
    action: "my_model_preferences",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyModelPreferencesResult<Fields>>(
    payload,
    config
  );
}


export type SetModelFavoriteInput = {
  modelId: UUID;
  favorite?: boolean;
};

export type SetModelFavoriteFields = UnifiedFieldSelection<UserModelPreferenceResourceSchema>[];

export type InferSetModelFavoriteResult<
  Fields extends SetModelFavoriteFields | undefined,
> = InferResult<UserModelPreferenceResourceSchema, Fields>;

export type SetModelFavoriteResult<Fields extends SetModelFavoriteFields | undefined = undefined> = | { success: true; data: InferSetModelFavoriteResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new UserModelPreference
 *
 * @ashActionType :create
 */
export async function setModelFavorite<Fields extends SetModelFavoriteFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SetModelFavoriteInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetModelFavoriteResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_model_favorite",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetModelFavoriteResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetModelHiddenInput = {
  modelId: UUID;
  hidden?: boolean;
};

export type SetModelHiddenFields = UnifiedFieldSelection<UserModelPreferenceResourceSchema>[];

export type InferSetModelHiddenResult<
  Fields extends SetModelHiddenFields | undefined,
> = InferResult<UserModelPreferenceResourceSchema, Fields>;

export type SetModelHiddenResult<Fields extends SetModelHiddenFields | undefined = undefined> = | { success: true; data: InferSetModelHiddenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new UserModelPreference
 *
 * @ashActionType :create
 */
export async function setModelHidden<Fields extends SetModelHiddenFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SetModelHiddenInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetModelHiddenResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_model_hidden",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetModelHiddenResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetModelPositionInput = {
  modelId: UUID;
  position?: number | null;
};

export type SetModelPositionFields = UnifiedFieldSelection<UserModelPreferenceResourceSchema>[];

export type InferSetModelPositionResult<
  Fields extends SetModelPositionFields | undefined,
> = InferResult<UserModelPreferenceResourceSchema, Fields>;

export type SetModelPositionResult<Fields extends SetModelPositionFields | undefined = undefined> = | { success: true; data: InferSetModelPositionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new UserModelPreference
 *
 * @ashActionType :create
 */
export async function setModelPosition<Fields extends SetModelPositionFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SetModelPositionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetModelPositionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_model_position",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetModelPositionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationDraftsInput = {
  conversationId: UUID;
};

export type ConversationDraftsFields = UnifiedFieldSelection<DraftResourceSchema>[];
export type InferConversationDraftsResult<
  Fields extends ConversationDraftsFields,
> = Array<InferResult<DraftResourceSchema, Fields>>;

export type ConversationDraftsResult<Fields extends ConversationDraftsFields> = | { success: true; data: InferConversationDraftsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Draft records
 *
 * @ashActionType :read
 */
export async function conversationDrafts<Fields extends ConversationDraftsFields>(
  config: {
  tenant?: string;
  input: ConversationDraftsInput;
  fields: Fields;
  filter?: DraftFilterInput;
  sort?: SortString<DraftSortField> | SortString<DraftSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationDraftsResult<Fields>> {
  const payload = {
    action: "conversation_drafts",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationDraftsResult<Fields>>(
    payload,
    config
  );
}


export type DeleteDraftResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Draft
 *
 * @ashActionType :destroy
 */
export async function deleteDraft(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteDraftResult> {
  const payload = {
    action: "delete_draft",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteDraftResult>(
    payload,
    config
  );
}


export type DraftVersionsInput = {
  draftId: UUID;
};

export type InferDraftVersionsResult = Array<Record<string, any>>;

export type DraftVersionsResult = | { success: true; data: InferDraftVersionsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Draft
 *
 * @ashActionType :action
 */
export async function draftVersions(
  config: {
  tenant?: string;
  input: DraftVersionsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DraftVersionsResult> {
  const payload = {
    action: "draft_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DraftVersionsResult>(
    payload,
    config
  );
}


export type ExportDraftInput = {
  draftId: UUID;
  conversationId: UUID;
  exportFormat: "docx" | "latex" | "markdown" | "pdf";
};

export type InferExportDraftResult = Record<string, any>;

export type ExportDraftResult = | { success: true; data: InferExportDraftResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Draft
 *
 * @ashActionType :action
 */
export async function exportDraft(
  config: {
  tenant?: string;
  input: ExportDraftInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ExportDraftResult> {
  const payload = {
    action: "export_draft",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ExportDraftResult>(
    payload,
    config
  );
}


export type GetDraftFields = UnifiedFieldSelection<DraftResourceSchema>[];
export type InferGetDraftResult<
  Fields extends GetDraftFields,
> = InferResult<DraftResourceSchema, Fields>;

export type GetDraftResult<Fields extends GetDraftFields> = | { success: true; data: InferGetDraftResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Draft records
 *
 * @ashActionType :read
 */
export async function getDraft<Fields extends GetDraftFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetDraftResult<Fields>> {
  const payload = {
    action: "get_draft",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetDraftResult<Fields>>(
    payload,
    config
  );
}


export type RenameDraftInput = {
  title: string;
};

export type RenameDraftFields = UnifiedFieldSelection<DraftResourceSchema>[];

export type InferRenameDraftResult<
  Fields extends RenameDraftFields | undefined,
> = InferResult<DraftResourceSchema, Fields>;

export type RenameDraftResult<Fields extends RenameDraftFields | undefined = undefined> = | { success: true; data: InferRenameDraftResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Draft
 *
 * @ashActionType :update
 */
export async function renameDraft<Fields extends RenameDraftFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: RenameDraftInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RenameDraftResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "rename_draft",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RenameDraftResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RestoreDraftVersionInput = {
  versionId: UUIDv7;
};

export type RestoreDraftVersionFields = UnifiedFieldSelection<DraftResourceSchema>[];

export type InferRestoreDraftVersionResult<
  Fields extends RestoreDraftVersionFields | undefined,
> = InferResult<DraftResourceSchema, Fields>;

export type RestoreDraftVersionResult<Fields extends RestoreDraftVersionFields | undefined = undefined> = | { success: true; data: InferRestoreDraftVersionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Draft
 *
 * @ashActionType :update
 */
export async function restoreDraftVersion<Fields extends RestoreDraftVersionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: RestoreDraftVersionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RestoreDraftVersionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "restore_draft_version",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RestoreDraftVersionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateDraftContentInput = {
  contentJson: Record<string, any>;
};

export type UpdateDraftContentFields = UnifiedFieldSelection<DraftResourceSchema>[];

export type InferUpdateDraftContentResult<
  Fields extends UpdateDraftContentFields | undefined,
> = InferResult<DraftResourceSchema, Fields>;

export type UpdateDraftContentResult<Fields extends UpdateDraftContentFields | undefined = undefined> = | { success: true; data: InferUpdateDraftContentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Draft
 *
 * @ashActionType :update
 */
export async function updateDraftContent<Fields extends UpdateDraftContentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateDraftContentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateDraftContentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_draft_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateDraftContentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DismissAnnouncementInput = {
  key: string;
};

export type InferDismissAnnouncementResult = "ok";

export type DismissAnnouncementResult = | { success: true; data: InferDismissAnnouncementResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Announcement
 *
 * @ashActionType :action
 */
export async function dismissAnnouncement(
  config: {
  tenant?: string;
  input: DismissAnnouncementInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DismissAnnouncementResult> {
  const payload = {
    action: "dismiss_announcement",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DismissAnnouncementResult>(
    payload,
    config
  );
}


export type InferUnseenAnnouncementsResult = Array<Record<string, any>>;

export type UnseenAnnouncementsResult = | { success: true; data: InferUnseenAnnouncementsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Announcement
 *
 * @ashActionType :action
 */
export async function unseenAnnouncements(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnseenAnnouncementsResult> {
  const payload = {
    action: "unseen_announcements",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<UnseenAnnouncementsResult>(
    payload,
    config
  );
}


export type InferOnboardingCardsResult = Record<string, any>;

export type OnboardingCardsResult = | { success: true; data: InferOnboardingCardsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on FeatureUsageEvent
 *
 * @ashActionType :action
 */
export async function onboardingCards(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<OnboardingCardsResult> {
  const payload = {
    action: "onboarding_cards",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<OnboardingCardsResult>(
    payload,
    config
  );
}


export type CollectionFilesInput = {
  knowledgeCollectionId: UUID;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type CollectionFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferCollectionFilesResult<
  Fields extends CollectionFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type CollectionFilesResult<Fields extends CollectionFilesFields> = | { success: true; data: InferCollectionFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function collectionFiles<Fields extends CollectionFilesFields>(
  config: {
  tenant?: string;
  input: CollectionFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CollectionFilesResult<Fields>> {
  const payload = {
    action: "collection_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<CollectionFilesResult<Fields>>(
    payload,
    config
  );
}


export type ConversationFilesInput = {
  conversationId: UUID;
};

export type ConversationFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferConversationFilesResult<
  Fields extends ConversationFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type ConversationFilesResult<Fields extends ConversationFilesFields> = | { success: true; data: InferConversationFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function conversationFiles<Fields extends ConversationFilesFields>(
  config: {
  tenant?: string;
  input: ConversationFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationFilesResult<Fields>> {
  const payload = {
    action: "conversation_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationFilesResult<Fields>>(
    payload,
    config
  );
}


export type DeleteFileResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a File
 *
 * @ashActionType :destroy
 */
export async function deleteFile(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteFileResult> {
  const payload = {
    action: "delete_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteFileResult>(
    payload,
    config
  );
}


export type FilesForDisplayInput = {
  ids: Array<UUID>;
};

export type InferFilesForDisplayResult = Array<Record<string, any>>;

export type FilesForDisplayResult = | { success: true; data: InferFilesForDisplayResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on File
 *
 * @ashActionType :action
 */
export async function filesForDisplay(
  config: {
  tenant?: string;
  input: FilesForDisplayInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FilesForDisplayResult> {
  const payload = {
    action: "files_for_display",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<FilesForDisplayResult>(
    payload,
    config
  );
}


export type FolderFilesInput = {
  folderId: UUID;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type FolderFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferFolderFilesResult<
  Fields extends FolderFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type FolderFilesResult<Fields extends FolderFilesFields> = | { success: true; data: InferFolderFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function folderFiles<Fields extends FolderFilesFields>(
  config: {
  tenant?: string;
  input: FolderFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FolderFilesResult<Fields>> {
  const payload = {
    action: "folder_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<FolderFilesResult<Fields>>(
    payload,
    config
  );
}


export type GetFileFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferGetFileResult<
  Fields extends GetFileFields,
> = InferResult<FileResourceSchema, Fields>;

export type GetFileResult<Fields extends GetFileFields> = | { success: true; data: InferGetFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function getFile<Fields extends GetFileFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetFileResult<Fields>> {
  const payload = {
    action: "get_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetFileResult<Fields>>(
    payload,
    config
  );
}


export type MoveFileInput = {
  conversationId?: UUID | null;
  folderId?: UUID | null;
};

export type MoveFileFields = UnifiedFieldSelection<FileResourceSchema>[];

export type InferMoveFileResult<
  Fields extends MoveFileFields | undefined,
> = InferResult<FileResourceSchema, Fields>;

export type MoveFileResult<Fields extends MoveFileFields | undefined = undefined> = | { success: true; data: InferMoveFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing File
 *
 * @ashActionType :update
 */
export async function moveFile<Fields extends MoveFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: MoveFileInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MoveFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "move_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MoveFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyLibraryFilesInput = {
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type MyLibraryFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferMyLibraryFilesResult<
  Fields extends MyLibraryFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type MyLibraryFilesResult<Fields extends MyLibraryFilesFields> = | { success: true; data: InferMyLibraryFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function myLibraryFiles<Fields extends MyLibraryFilesFields>(
  config: {
  tenant?: string;
  input?: MyLibraryFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyLibraryFilesResult<Fields>> {
  const payload = {
    action: "my_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyLibraryFilesResult<Fields>>(
    payload,
    config
  );
}


export type RecentFilesInput = {
  workspaceId?: UUID | null;
  since: UtcDateTimeUsec;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type RecentFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferRecentFilesResult<
  Fields extends RecentFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type RecentFilesResult<Fields extends RecentFilesFields> = | { success: true; data: InferRecentFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function recentFiles<Fields extends RecentFilesFields>(
  config: {
  tenant?: string;
  input: RecentFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RecentFilesResult<Fields>> {
  const payload = {
    action: "recent_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<RecentFilesResult<Fields>>(
    payload,
    config
  );
}


export type RenameFileInput = {
  name?: string;
  isTemplate?: boolean;
};

export type RenameFileFields = UnifiedFieldSelection<FileResourceSchema>[];

export type InferRenameFileResult<
  Fields extends RenameFileFields | undefined,
> = InferResult<FileResourceSchema, Fields>;

export type RenameFileResult<Fields extends RenameFileFields | undefined = undefined> = | { success: true; data: InferRenameFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing File
 *
 * @ashActionType :update
 */
export async function renameFile<Fields extends RenameFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: RenameFileInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RenameFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "rename_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RenameFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ShareFileToTeamFields = UnifiedFieldSelection<FileResourceSchema>[];

export type InferShareFileToTeamResult<
  Fields extends ShareFileToTeamFields | undefined,
> = InferResult<FileResourceSchema, Fields>;

export type ShareFileToTeamResult<Fields extends ShareFileToTeamFields | undefined = undefined> = | { success: true; data: InferShareFileToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing File
 *
 * @ashActionType :update
 */
export async function shareFileToTeam<Fields extends ShareFileToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareFileToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_file_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareFileToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SharedWithMeFilesInput = {
  workspaceId: UUID;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type SharedWithMeFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferSharedWithMeFilesResult<
  Fields extends SharedWithMeFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type SharedWithMeFilesResult<Fields extends SharedWithMeFilesFields> = | { success: true; data: InferSharedWithMeFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function sharedWithMeFiles<Fields extends SharedWithMeFilesFields>(
  config: {
  tenant?: string;
  input: SharedWithMeFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SharedWithMeFilesResult<Fields>> {
  const payload = {
    action: "shared_with_me_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<SharedWithMeFilesResult<Fields>>(
    payload,
    config
  );
}


export type TemplateFilesInput = {
  query?: string | null;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type TemplateFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferTemplateFilesResult<
  Fields extends TemplateFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type TemplateFilesResult<Fields extends TemplateFilesFields> = | { success: true; data: InferTemplateFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function templateFiles<Fields extends TemplateFilesFields>(
  config: {
  tenant?: string;
  input?: TemplateFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TemplateFilesResult<Fields>> {
  const payload = {
    action: "template_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<TemplateFilesResult<Fields>>(
    payload,
    config
  );
}


export type TrashFileFields = UnifiedFieldSelection<FileResourceSchema>[];

export type InferTrashFileResult<
  Fields extends TrashFileFields | undefined,
> = InferResult<FileResourceSchema, Fields>;

export type TrashFileResult<Fields extends TrashFileFields | undefined = undefined> = | { success: true; data: InferTrashFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing File
 *
 * @ashActionType :update
 */
export async function trashFile<Fields extends TrashFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TrashFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "trash_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TrashFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TrashFilesInput = {
  workspaceId?: UUID | null;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type TrashFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferTrashFilesResult<
  Fields extends TrashFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type TrashFilesResult<Fields extends TrashFilesFields> = | { success: true; data: InferTrashFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function trashFiles<Fields extends TrashFilesFields>(
  config: {
  tenant?: string;
  input?: TrashFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TrashFilesResult<Fields>> {
  const payload = {
    action: "trash_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<TrashFilesResult<Fields>>(
    payload,
    config
  );
}


export type UnshareFileFromTeamFields = UnifiedFieldSelection<FileResourceSchema>[];

export type InferUnshareFileFromTeamResult<
  Fields extends UnshareFileFromTeamFields | undefined,
> = InferResult<FileResourceSchema, Fields>;

export type UnshareFileFromTeamResult<Fields extends UnshareFileFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareFileFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing File
 *
 * @ashActionType :update
 */
export async function unshareFileFromTeam<Fields extends UnshareFileFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareFileFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_file_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareFileFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceLibraryFilesInput = {
  workspaceId: UUID;
  browserType?: string | null;
  browserModified?: string | null;
  browserSource?: string | null;
};

export type WorkspaceLibraryFilesFields = UnifiedFieldSelection<FileResourceSchema>[];
export type InferWorkspaceLibraryFilesResult<
  Fields extends WorkspaceLibraryFilesFields,
> = Array<InferResult<FileResourceSchema, Fields>>;

export type WorkspaceLibraryFilesResult<Fields extends WorkspaceLibraryFilesFields> = | { success: true; data: InferWorkspaceLibraryFilesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read File records
 *
 * @ashActionType :read
 */
export async function workspaceLibraryFiles<Fields extends WorkspaceLibraryFilesFields>(
  config: {
  tenant?: string;
  input: WorkspaceLibraryFilesInput;
  fields: Fields;
  filter?: FileFilterInput;
  sort?: SortString<FileSortField> | SortString<FileSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceLibraryFilesResult<Fields>> {
  const payload = {
    action: "workspace_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceLibraryFilesResult<Fields>>(
    payload,
    config
  );
}


export type ListUserIntegrationsInput = {
  userId: UUID;
};

export type ListUserIntegrationsFields = UnifiedFieldSelection<UserIntegrationResourceSchema>[];
export type InferListUserIntegrationsResult<
  Fields extends ListUserIntegrationsFields,
> = Array<InferResult<UserIntegrationResourceSchema, Fields>>;

export type ListUserIntegrationsResult<Fields extends ListUserIntegrationsFields> = | { success: true; data: InferListUserIntegrationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read UserIntegration records
 *
 * @ashActionType :read
 */
export async function listUserIntegrations<Fields extends ListUserIntegrationsFields>(
  config: {
  tenant?: string;
  input: ListUserIntegrationsInput;
  fields: Fields;
  filter?: UserIntegrationFilterInput;
  sort?: SortString<UserIntegrationSortField> | SortString<UserIntegrationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListUserIntegrationsResult<Fields>> {
  const payload = {
    action: "list_user_integrations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListUserIntegrationsResult<Fields>>(
    payload,
    config
  );
}


export type UpdateIntegrationConfigInput = {
  config?: Record<string, any> | null;
  externalId?: string | null;
  conversationMode?: "multi" | "single" | null;
  asyncReplyEnabled?: boolean | null;
};

export type UpdateIntegrationConfigFields = UnifiedFieldSelection<UserIntegrationResourceSchema>[];

export type InferUpdateIntegrationConfigResult<
  Fields extends UpdateIntegrationConfigFields | undefined,
> = InferResult<UserIntegrationResourceSchema, Fields>;

export type UpdateIntegrationConfigResult<Fields extends UpdateIntegrationConfigFields | undefined = undefined> = | { success: true; data: InferUpdateIntegrationConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing UserIntegration
 *
 * @ashActionType :update
 */
export async function updateIntegrationConfig<Fields extends UpdateIntegrationConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: UpdateIntegrationConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateIntegrationConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_integration_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateIntegrationConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyKnowledgeCollectionsFields = UnifiedFieldSelection<KnowledgeCollectionResourceSchema>[];
export type InferMyKnowledgeCollectionsResult<
  Fields extends MyKnowledgeCollectionsFields,
> = Array<InferResult<KnowledgeCollectionResourceSchema, Fields>>;

export type MyKnowledgeCollectionsResult<Fields extends MyKnowledgeCollectionsFields> = | { success: true; data: InferMyKnowledgeCollectionsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read KnowledgeCollection records
 *
 * @ashActionType :read
 */
export async function myKnowledgeCollections<Fields extends MyKnowledgeCollectionsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: KnowledgeCollectionFilterInput;
  sort?: SortString<KnowledgeCollectionSortField> | SortString<KnowledgeCollectionSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyKnowledgeCollectionsResult<Fields>> {
  const payload = {
    action: "my_knowledge_collections",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyKnowledgeCollectionsResult<Fields>>(
    payload,
    config
  );
}


export type WorkspaceKnowledgeCollectionsInput = {
  workspaceId: UUID;
};

export type WorkspaceKnowledgeCollectionsFields = UnifiedFieldSelection<KnowledgeCollectionResourceSchema>[];
export type InferWorkspaceKnowledgeCollectionsResult<
  Fields extends WorkspaceKnowledgeCollectionsFields,
> = Array<InferResult<KnowledgeCollectionResourceSchema, Fields>>;

export type WorkspaceKnowledgeCollectionsResult<Fields extends WorkspaceKnowledgeCollectionsFields> = | { success: true; data: InferWorkspaceKnowledgeCollectionsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read KnowledgeCollection records
 *
 * @ashActionType :read
 */
export async function workspaceKnowledgeCollections<Fields extends WorkspaceKnowledgeCollectionsFields>(
  config: {
  tenant?: string;
  input: WorkspaceKnowledgeCollectionsInput;
  fields: Fields;
  filter?: KnowledgeCollectionFilterInput;
  sort?: SortString<KnowledgeCollectionSortField> | SortString<KnowledgeCollectionSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceKnowledgeCollectionsResult<Fields>> {
  const payload = {
    action: "workspace_knowledge_collections",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceKnowledgeCollectionsResult<Fields>>(
    payload,
    config
  );
}


export type ConnectKnowledgeSourceInput = {
  provider: string;
  authConfig: Record<string, any>;
  name?: string | null;
  workspaceId?: UUID | null;
};

export type InferConnectKnowledgeSourceResult = Record<string, any>;

export type ConnectKnowledgeSourceResult = | { success: true; data: InferConnectKnowledgeSourceResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on KnowledgeSource
 *
 * @ashActionType :action
 */
export async function connectKnowledgeSource(
  config: {
  tenant?: string;
  input: ConnectKnowledgeSourceInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConnectKnowledgeSourceResult> {
  const payload = {
    action: "connect_knowledge_source",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ConnectKnowledgeSourceResult>(
    payload,
    config
  );
}


export type CreateKnowledgeCollectionsInput = {
  sourceId: UUID;
  folders: Array<Record<string, any>>;
};

export type InferCreateKnowledgeCollectionsResult = Record<string, any>;

export type CreateKnowledgeCollectionsResult = | { success: true; data: InferCreateKnowledgeCollectionsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on KnowledgeSource
 *
 * @ashActionType :action
 */
export async function createKnowledgeCollections(
  config: {
  tenant?: string;
  input: CreateKnowledgeCollectionsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateKnowledgeCollectionsResult> {
  const payload = {
    action: "create_knowledge_collections",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<CreateKnowledgeCollectionsResult>(
    payload,
    config
  );
}


export type DisconnectKnowledgeSourceResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a KnowledgeSource
 *
 * @ashActionType :destroy
 */
export async function disconnectKnowledgeSource(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DisconnectKnowledgeSourceResult> {
  const payload = {
    action: "disconnect_knowledge_source",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DisconnectKnowledgeSourceResult>(
    payload,
    config
  );
}


export type KnowledgeSourceFoldersInput = {
  sourceId: UUID;
  parentId?: string | null;
};

export type InferKnowledgeSourceFoldersResult = Array<Record<string, any>>;

export type KnowledgeSourceFoldersResult = | { success: true; data: InferKnowledgeSourceFoldersResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on KnowledgeSource
 *
 * @ashActionType :action
 */
export async function knowledgeSourceFolders(
  config: {
  tenant?: string;
  input: KnowledgeSourceFoldersInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<KnowledgeSourceFoldersResult> {
  const payload = {
    action: "knowledge_source_folders",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<KnowledgeSourceFoldersResult>(
    payload,
    config
  );
}


export type ListKnowledgeSourcesFields = UnifiedFieldSelection<KnowledgeSourceResourceSchema>[];
export type InferListKnowledgeSourcesResult<
  Fields extends ListKnowledgeSourcesFields,
> = Array<InferResult<KnowledgeSourceResourceSchema, Fields>>;

export type ListKnowledgeSourcesResult<Fields extends ListKnowledgeSourcesFields> = | { success: true; data: InferListKnowledgeSourcesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read KnowledgeSource records
 *
 * @ashActionType :read
 */
export async function listKnowledgeSources<Fields extends ListKnowledgeSourcesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: KnowledgeSourceFilterInput;
  sort?: SortString<KnowledgeSourceSortField> | SortString<KnowledgeSourceSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListKnowledgeSourcesResult<Fields>> {
  const payload = {
    action: "list_knowledge_sources",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListKnowledgeSourcesResult<Fields>>(
    payload,
    config
  );
}


export type AddPromptTagsInput = {
  tagIds: Array<UUID>;
};

export type AddPromptTagsFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferAddPromptTagsResult<
  Fields extends AddPromptTagsFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type AddPromptTagsResult<Fields extends AddPromptTagsFields | undefined = undefined> = | { success: true; data: InferAddPromptTagsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function addPromptTags<Fields extends AddPromptTagsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: AddPromptTagsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AddPromptTagsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "add_prompt_tags",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AddPromptTagsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreatePromptInput = {
  name: string;
  content: string;
  type: "system" | "user";
  metadata?: Record<string, any> | null;
  variables?: Record<string, any> | null;
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation" | null;
  modelId?: UUID | null;
  description?: string | null;
  userMessageTemplate?: string | null;
  additionalInformation?: string | null;
  language?: "ar" | "de" | "en" | "es" | "fr" | "ja" | "ko" | "pt" | "ru" | "zh" | null;
  workspaceId?: UUID | null;
};

export type CreatePromptFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferCreatePromptResult<
  Fields extends CreatePromptFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type CreatePromptResult<Fields extends CreatePromptFields | undefined = undefined> = | { success: true; data: InferCreatePromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Prompt
 *
 * @ashActionType :create
 */
export async function createPrompt<Fields extends CreatePromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreatePromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreatePromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreatePromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyPromptResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Prompt
 *
 * @ashActionType :destroy
 */
export async function destroyPrompt(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyPromptResult> {
  const payload = {
    action: "destroy_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyPromptResult>(
    payload,
    config
  );
}


export type GetPromptFields = UnifiedFieldSelection<PromptResourceSchema>[];
export type InferGetPromptResult<
  Fields extends GetPromptFields,
> = InferResult<PromptResourceSchema, Fields>;

export type GetPromptResult<Fields extends GetPromptFields> = | { success: true; data: InferGetPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Prompt records
 *
 * @ashActionType :read
 */
export async function getPrompt<Fields extends GetPromptFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUID;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPromptResult<Fields>> {
  const payload = {
    action: "get_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetPromptResult<Fields>>(
    payload,
    config
  );
}


export type IncrementPromptUseCountFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferIncrementPromptUseCountResult<
  Fields extends IncrementPromptUseCountFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type IncrementPromptUseCountResult<Fields extends IncrementPromptUseCountFields | undefined = undefined> = | { success: true; data: InferIncrementPromptUseCountResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function incrementPromptUseCount<Fields extends IncrementPromptUseCountFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<IncrementPromptUseCountResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "increment_prompt_use_count",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<IncrementPromptUseCountResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyFavoritePromptsFields = UnifiedFieldSelection<PromptResourceSchema>[];
export type InferMyFavoritePromptsResult<
  Fields extends MyFavoritePromptsFields,
> = Array<InferResult<PromptResourceSchema, Fields>>;

export type MyFavoritePromptsResult<Fields extends MyFavoritePromptsFields> = | { success: true; data: InferMyFavoritePromptsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Prompt records
 *
 * @ashActionType :read
 */
export async function myFavoritePrompts<Fields extends MyFavoritePromptsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: PromptFilterInput;
  sort?: SortString<PromptSortField> | SortString<PromptSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyFavoritePromptsResult<Fields>> {
  const payload = {
    action: "my_favorite_prompts",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyFavoritePromptsResult<Fields>>(
    payload,
    config
  );
}


export type MyPromptsFields = UnifiedFieldSelection<PromptResourceSchema>[];
export type InferMyPromptsResult<
  Fields extends MyPromptsFields,
> = Array<InferResult<PromptResourceSchema, Fields>>;

export type MyPromptsResult<Fields extends MyPromptsFields> = | { success: true; data: InferMyPromptsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Prompt records
 *
 * @ashActionType :read
 */
export async function myPrompts<Fields extends MyPromptsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: PromptFilterInput;
  sort?: SortString<PromptSortField> | SortString<PromptSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyPromptsResult<Fields>> {
  const payload = {
    action: "my_prompts",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyPromptsResult<Fields>>(
    payload,
    config
  );
}


export type PublishPromptInput = {
  isPublic?: boolean | null;
};

export type PublishPromptFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferPublishPromptResult<
  Fields extends PublishPromptFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type PublishPromptResult<Fields extends PublishPromptFields | undefined = undefined> = | { success: true; data: InferPublishPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function publishPrompt<Fields extends PublishPromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: PublishPromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PublishPromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "publish_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<PublishPromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RemovePromptTagInput = {
  tagId: UUID;
};

export type RemovePromptTagFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferRemovePromptTagResult<
  Fields extends RemovePromptTagFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type RemovePromptTagResult<Fields extends RemovePromptTagFields | undefined = undefined> = | { success: true; data: InferRemovePromptTagResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function removePromptTag<Fields extends RemovePromptTagFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: RemovePromptTagInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemovePromptTagResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "remove_prompt_tag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RemovePromptTagResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SharePromptToTeamFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferSharePromptToTeamResult<
  Fields extends SharePromptToTeamFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type SharePromptToTeamResult<Fields extends SharePromptToTeamFields | undefined = undefined> = | { success: true; data: InferSharePromptToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function sharePromptToTeam<Fields extends SharePromptToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SharePromptToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_prompt_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SharePromptToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnpublishPromptFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferUnpublishPromptResult<
  Fields extends UnpublishPromptFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type UnpublishPromptResult<Fields extends UnpublishPromptFields | undefined = undefined> = | { success: true; data: InferUnpublishPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function unpublishPrompt<Fields extends UnpublishPromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnpublishPromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unpublish_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnpublishPromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnsharePromptFromTeamFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferUnsharePromptFromTeamResult<
  Fields extends UnsharePromptFromTeamFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type UnsharePromptFromTeamResult<Fields extends UnsharePromptFromTeamFields | undefined = undefined> = | { success: true; data: InferUnsharePromptFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function unsharePromptFromTeam<Fields extends UnsharePromptFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnsharePromptFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_prompt_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnsharePromptFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdatePromptInput = {
  name?: string;
  content?: string;
  type?: "system" | "user";
  metadata?: Record<string, any> | null;
  variables?: Record<string, any> | null;
  chatMode?: "chat" | "image_generation" | "reasoning" | "search" | "video_generation" | null;
  modelId?: UUID | null;
  description?: string | null;
  userMessageTemplate?: string | null;
  additionalInformation?: string | null;
  language?: "ar" | "de" | "en" | "es" | "fr" | "ja" | "ko" | "pt" | "ru" | "zh" | null;
};

export type UpdatePromptFields = UnifiedFieldSelection<PromptResourceSchema>[];

export type InferUpdatePromptResult<
  Fields extends UpdatePromptFields | undefined,
> = InferResult<PromptResourceSchema, Fields>;

export type UpdatePromptResult<Fields extends UpdatePromptFields | undefined = undefined> = | { success: true; data: InferUpdatePromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Prompt
 *
 * @ashActionType :update
 */
export async function updatePrompt<Fields extends UpdatePromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdatePromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdatePromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdatePromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspacePromptsInput = {
  workspaceId: UUID;
};

export type WorkspacePromptsFields = UnifiedFieldSelection<PromptResourceSchema>[];
export type InferWorkspacePromptsResult<
  Fields extends WorkspacePromptsFields,
> = Array<InferResult<PromptResourceSchema, Fields>>;

export type WorkspacePromptsResult<Fields extends WorkspacePromptsFields> = | { success: true; data: InferWorkspacePromptsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Prompt records
 *
 * @ashActionType :read
 */
export async function workspacePrompts<Fields extends WorkspacePromptsFields>(
  config: {
  tenant?: string;
  input: WorkspacePromptsInput;
  fields: Fields;
  filter?: PromptFilterInput;
  sort?: SortString<PromptSortField> | SortString<PromptSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspacePromptsResult<Fields>> {
  const payload = {
    action: "workspace_prompts",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspacePromptsResult<Fields>>(
    payload,
    config
  );
}


export type FavoritePromptInput = {
  promptId: UUID;
};

export type FavoritePromptFields = UnifiedFieldSelection<PromptFavoriteResourceSchema>[];

export type InferFavoritePromptResult<
  Fields extends FavoritePromptFields | undefined,
> = InferResult<PromptFavoriteResourceSchema, Fields>;

export type FavoritePromptResult<Fields extends FavoritePromptFields | undefined = undefined> = | { success: true; data: InferFavoritePromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new PromptFavorite
 *
 * @ashActionType :create
 */
export async function favoritePrompt<Fields extends FavoritePromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: FavoritePromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FavoritePromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "favorite_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<FavoritePromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MyPromptFavoritesFields = UnifiedFieldSelection<PromptFavoriteResourceSchema>[];
export type InferMyPromptFavoritesResult<
  Fields extends MyPromptFavoritesFields,
> = Array<InferResult<PromptFavoriteResourceSchema, Fields>>;

export type MyPromptFavoritesResult<Fields extends MyPromptFavoritesFields> = | { success: true; data: InferMyPromptFavoritesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PromptFavorite records
 *
 * @ashActionType :read
 */
export async function myPromptFavorites<Fields extends MyPromptFavoritesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: PromptFavoriteFilterInput;
  sort?: SortString<PromptFavoriteSortField> | SortString<PromptFavoriteSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyPromptFavoritesResult<Fields>> {
  const payload = {
    action: "my_prompt_favorites",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyPromptFavoritesResult<Fields>>(
    payload,
    config
  );
}


export type UnfavoritePromptResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a PromptFavorite
 *
 * @ashActionType :destroy
 */
export async function unfavoritePrompt(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnfavoritePromptResult> {
  const payload = {
    action: "unfavorite_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<UnfavoritePromptResult>(
    payload,
    config
  );
}


export type DestroyTagResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Tag
 *
 * @ashActionType :destroy
 */
export async function destroyTag(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyTagResult> {
  const payload = {
    action: "destroy_tag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyTagResult>(
    payload,
    config
  );
}


export type GetOrCreateTagInput = {
  name: string;
  workspaceId?: UUID | null;
};

export type GetOrCreateTagFields = UnifiedFieldSelection<TagResourceSchema>[];

export type InferGetOrCreateTagResult<
  Fields extends GetOrCreateTagFields | undefined,
> = InferResult<TagResourceSchema, Fields>;

export type GetOrCreateTagResult<Fields extends GetOrCreateTagFields | undefined = undefined> = | { success: true; data: InferGetOrCreateTagResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Tag
 *
 * @ashActionType :create
 */
export async function getOrCreateTag<Fields extends GetOrCreateTagFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: GetOrCreateTagInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOrCreateTagResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "get_or_create_tag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOrCreateTagResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListTagsFields = UnifiedFieldSelection<TagResourceSchema>[];
export type InferListTagsResult<
  Fields extends ListTagsFields,
> = Array<InferResult<TagResourceSchema, Fields>>;

export type ListTagsResult<Fields extends ListTagsFields> = | { success: true; data: InferListTagsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Tag records
 *
 * @ashActionType :read
 */
export async function listTags<Fields extends ListTagsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: TagFilterInput;
  sort?: SortString<TagSortField> | SortString<TagSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListTagsResult<Fields>> {
  const payload = {
    action: "list_tags",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListTagsResult<Fields>>(
    payload,
    config
  );
}


export type CreateMcpServerInput = {
  name: string;
  handle: string;
  url: string;
  transport?: "sse" | "streamable_http";
  mcpPath?: string;
  enabled?: boolean;
  authType?: "none" | "oauth" | "static_header";
  workspaceId?: UUID | null;
};

export type CreateMcpServerFields = UnifiedFieldSelection<MCPServerResourceSchema>[];

export type InferCreateMcpServerResult<
  Fields extends CreateMcpServerFields | undefined,
> = InferResult<MCPServerResourceSchema, Fields>;

export type CreateMcpServerResult<Fields extends CreateMcpServerFields | undefined = undefined> = | { success: true; data: InferCreateMcpServerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Server
 *
 * @ashActionType :create
 */
export async function createMcpServer<Fields extends CreateMcpServerFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateMcpServerInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateMcpServerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateMcpServerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyMcpServerResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Server
 *
 * @ashActionType :destroy
 */
export async function destroyMcpServer(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyMcpServerResult> {
  const payload = {
    action: "destroy_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyMcpServerResult>(
    payload,
    config
  );
}


export type DiscoverMcpServerInput = {
  mcpServerId: UUID;
};

export type DiscoverMcpServerFields = UnifiedFieldSelection<MCPServerResourceSchema>[];

export type InferDiscoverMcpServerResult<
  Fields extends DiscoverMcpServerFields | undefined,
> = InferResult<MCPServerResourceSchema, Fields>;

export type DiscoverMcpServerResult<Fields extends DiscoverMcpServerFields | undefined = undefined> = | { success: true; data: InferDiscoverMcpServerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Server
 *
 * @ashActionType :action
 */
export async function discoverMcpServer<Fields extends DiscoverMcpServerFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: DiscoverMcpServerInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DiscoverMcpServerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "discover_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DiscoverMcpServerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetMcpServerFields = UnifiedFieldSelection<MCPServerResourceSchema>[];
export type InferGetMcpServerResult<
  Fields extends GetMcpServerFields,
> = InferResult<MCPServerResourceSchema, Fields>;

export type GetMcpServerResult<Fields extends GetMcpServerFields> = | { success: true; data: InferGetMcpServerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Server records
 *
 * @ashActionType :read
 */
export async function getMcpServer<Fields extends GetMcpServerFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMcpServerResult<Fields>> {
  const payload = {
    action: "get_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetMcpServerResult<Fields>>(
    payload,
    config
  );
}


export type ListMcpServersFields = UnifiedFieldSelection<MCPServerResourceSchema>[];
export type InferListMcpServersResult<
  Fields extends ListMcpServersFields,
> = Array<InferResult<MCPServerResourceSchema, Fields>>;

export type ListMcpServersResult<Fields extends ListMcpServersFields> = | { success: true; data: InferListMcpServersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Server records
 *
 * @ashActionType :read
 */
export async function listMcpServers<Fields extends ListMcpServersFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: MCPServerFilterInput;
  sort?: SortString<MCPServerSortField> | SortString<MCPServerSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListMcpServersResult<Fields>> {
  const payload = {
    action: "list_mcp_servers",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListMcpServersResult<Fields>>(
    payload,
    config
  );
}


export type ToggleMcpServerFields = UnifiedFieldSelection<MCPServerResourceSchema>[];

export type InferToggleMcpServerResult<
  Fields extends ToggleMcpServerFields | undefined,
> = InferResult<MCPServerResourceSchema, Fields>;

export type ToggleMcpServerResult<Fields extends ToggleMcpServerFields | undefined = undefined> = | { success: true; data: InferToggleMcpServerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Server
 *
 * @ashActionType :update
 */
export async function toggleMcpServer<Fields extends ToggleMcpServerFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ToggleMcpServerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "toggle_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ToggleMcpServerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateMcpServerInput = {
  name?: string;
  url?: string;
  transport?: "sse" | "streamable_http";
  mcpPath?: string;
  enabled?: boolean;
  authType?: "none" | "oauth" | "static_header";
};

export type UpdateMcpServerFields = UnifiedFieldSelection<MCPServerResourceSchema>[];

export type InferUpdateMcpServerResult<
  Fields extends UpdateMcpServerFields | undefined,
> = InferResult<MCPServerResourceSchema, Fields>;

export type UpdateMcpServerResult<Fields extends UpdateMcpServerFields | undefined = undefined> = | { success: true; data: InferUpdateMcpServerResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Server
 *
 * @ashActionType :update
 */
export async function updateMcpServer<Fields extends UpdateMcpServerFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateMcpServerInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateMcpServerResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_mcp_server",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateMcpServerResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DisconnectMcpCredentialFields = UnifiedFieldSelection<MCPServerCredentialResourceSchema>[];

export type InferDisconnectMcpCredentialResult<
  Fields extends DisconnectMcpCredentialFields | undefined,
> = InferResult<MCPServerCredentialResourceSchema, Fields>;

export type DisconnectMcpCredentialResult<Fields extends DisconnectMcpCredentialFields | undefined = undefined> = | { success: true; data: InferDisconnectMcpCredentialResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ServerCredential
 *
 * @ashActionType :update
 */
export async function disconnectMcpCredential<Fields extends DisconnectMcpCredentialFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DisconnectMcpCredentialResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "disconnect_mcp_credential",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DisconnectMcpCredentialResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetMcpCredentialInput = {
  mcpServerId: UUID;
};

export type GetMcpCredentialFields = UnifiedFieldSelection<MCPServerCredentialResourceSchema>[];
export type InferGetMcpCredentialResult<
  Fields extends GetMcpCredentialFields,
> = InferResult<MCPServerCredentialResourceSchema, Fields>;

export type GetMcpCredentialResult<Fields extends GetMcpCredentialFields> = | { success: true; data: InferGetMcpCredentialResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ServerCredential records
 *
 * @ashActionType :read
 */
export async function getMcpCredential<Fields extends GetMcpCredentialFields>(
  config: {
  tenant?: string;
  input: GetMcpCredentialInput;
  getBy: {
    mcpServerId: UUID;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMcpCredentialResult<Fields>> {
  const payload = {
    action: "get_mcp_credential",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetMcpCredentialResult<Fields>>(
    payload,
    config
  );
}


export type SetMcpCredentialStatusInput = {
  status?: "connected" | "disconnected" | "error" | "needs_auth";
};

export type SetMcpCredentialStatusFields = UnifiedFieldSelection<MCPServerCredentialResourceSchema>[];

export type InferSetMcpCredentialStatusResult<
  Fields extends SetMcpCredentialStatusFields | undefined,
> = InferResult<MCPServerCredentialResourceSchema, Fields>;

export type SetMcpCredentialStatusResult<Fields extends SetMcpCredentialStatusFields | undefined = undefined> = | { success: true; data: InferSetMcpCredentialStatusResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ServerCredential
 *
 * @ashActionType :update
 */
export async function setMcpCredentialStatus<Fields extends SetMcpCredentialStatusFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetMcpCredentialStatusInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetMcpCredentialStatusResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_mcp_credential_status",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetMcpCredentialStatusResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpsertMcpStaticHeadersInput = {
  mcpServerId: UUID;
  staticHeaders: Record<string, any>;
};

export type UpsertMcpStaticHeadersFields = UnifiedFieldSelection<MCPServerCredentialResourceSchema>[];

export type InferUpsertMcpStaticHeadersResult<
  Fields extends UpsertMcpStaticHeadersFields | undefined,
> = InferResult<MCPServerCredentialResourceSchema, Fields>;

export type UpsertMcpStaticHeadersResult<Fields extends UpsertMcpStaticHeadersFields | undefined = undefined> = | { success: true; data: InferUpsertMcpStaticHeadersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ServerCredential
 *
 * @ashActionType :create
 */
export async function upsertMcpStaticHeaders<Fields extends UpsertMcpStaticHeadersFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: UpsertMcpStaticHeadersInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpsertMcpStaticHeadersResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "upsert_mcp_static_headers",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpsertMcpStaticHeadersResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeactivateUserMemoryFields = UnifiedFieldSelection<MemoryResourceSchema>[];

export type InferDeactivateUserMemoryResult<
  Fields extends DeactivateUserMemoryFields | undefined,
> = InferResult<MemoryResourceSchema, Fields>;

export type DeactivateUserMemoryResult<Fields extends DeactivateUserMemoryFields | undefined = undefined> = | { success: true; data: InferDeactivateUserMemoryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Memory
 *
 * @ashActionType :update
 */
export async function deactivateUserMemory<Fields extends DeactivateUserMemoryFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeactivateUserMemoryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "deactivate_user_memory",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DeactivateUserMemoryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListUserMemoriesInput = {
  workspaceId?: UUID | null;
};

export type ListUserMemoriesFields = UnifiedFieldSelection<MemoryResourceSchema>[];
export type InferListUserMemoriesResult<
  Fields extends ListUserMemoriesFields,
> = Array<InferResult<MemoryResourceSchema, Fields>>;

export type ListUserMemoriesResult<Fields extends ListUserMemoriesFields> = | { success: true; data: InferListUserMemoriesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Memory records
 *
 * @ashActionType :read
 */
export async function listUserMemories<Fields extends ListUserMemoriesFields>(
  config: {
  tenant?: string;
  input?: ListUserMemoriesInput;
  fields: Fields;
  filter?: MemoryFilterInput;
  sort?: SortString<MemorySortField> | SortString<MemorySortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListUserMemoriesResult<Fields>> {
  const payload = {
    action: "list_user_memories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListUserMemoriesResult<Fields>>(
    payload,
    config
  );
}


export type ClearUserProfileFields = UnifiedFieldSelection<UserProfileResourceSchema>[];

export type InferClearUserProfileResult<
  Fields extends ClearUserProfileFields | undefined,
> = InferResult<UserProfileResourceSchema, Fields>;

export type ClearUserProfileResult<Fields extends ClearUserProfileFields | undefined = undefined> = | { success: true; data: InferClearUserProfileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing UserProfile
 *
 * @ashActionType :update
 */
export async function clearUserProfile<Fields extends ClearUserProfileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ClearUserProfileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "clear_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ClearUserProfileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetUserProfileInput = {
  userId: UUID;
  workspaceId?: UUID | null;
};

export type GetUserProfileFields = UnifiedFieldSelection<UserProfileResourceSchema>[];
export type InferGetUserProfileResult<
  Fields extends GetUserProfileFields,
> = InferResult<UserProfileResourceSchema, Fields>;

export type GetUserProfileResult<Fields extends GetUserProfileFields> = | { success: true; data: InferGetUserProfileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read UserProfile records
 *
 * @ashActionType :read
 */
export async function getUserProfile<Fields extends GetUserProfileFields>(
  config: {
  tenant?: string;
  input: GetUserProfileInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetUserProfileResult<Fields>> {
  const payload = {
    action: "get_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetUserProfileResult<Fields>>(
    payload,
    config
  );
}


export type CreateOwnedProviderInput = {
  name: string;
  reqLlmId: string;
  baseUrl?: string | null;
  apiKey?: string | null;
};

export type CreateOwnedProviderFields = UnifiedFieldSelection<ModelProviderResourceSchema>[];

export type InferCreateOwnedProviderResult<
  Fields extends CreateOwnedProviderFields | undefined,
> = InferResult<ModelProviderResourceSchema, Fields>;

export type CreateOwnedProviderResult<Fields extends CreateOwnedProviderFields | undefined = undefined> = | { success: true; data: InferCreateOwnedProviderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Provider
 *
 * @ashActionType :create
 */
export async function createOwnedProvider<Fields extends CreateOwnedProviderFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOwnedProviderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOwnedProviderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_owned_provider",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOwnedProviderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyOwnedProviderResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Provider
 *
 * @ashActionType :destroy
 */
export async function destroyOwnedProvider(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyOwnedProviderResult> {
  const payload = {
    action: "destroy_owned_provider",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyOwnedProviderResult>(
    payload,
    config
  );
}


export type ListOwnedProvidersFields = UnifiedFieldSelection<ModelProviderResourceSchema>[];
export type InferListOwnedProvidersResult<
  Fields extends ListOwnedProvidersFields,
> = Array<InferResult<ModelProviderResourceSchema, Fields>>;

export type ListOwnedProvidersResult<Fields extends ListOwnedProvidersFields> = | { success: true; data: InferListOwnedProvidersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Provider records
 *
 * @ashActionType :read
 */
export async function listOwnedProviders<Fields extends ListOwnedProvidersFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: ModelProviderFilterInput;
  sort?: SortString<ModelProviderSortField> | SortString<ModelProviderSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListOwnedProvidersResult<Fields>> {
  const payload = {
    action: "list_owned_providers",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListOwnedProvidersResult<Fields>>(
    payload,
    config
  );
}


export type ListRemoteModelsInput = {
  providerId: UUID;
};

export type InferListRemoteModelsResult = Record<string, any>;

export type ListRemoteModelsResult = | { success: true; data: InferListRemoteModelsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Provider
 *
 * @ashActionType :action
 */
export async function listRemoteModels(
  config: {
  tenant?: string;
  input: ListRemoteModelsInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListRemoteModelsResult> {
  const payload = {
    action: "list_remote_models",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ListRemoteModelsResult>(
    payload,
    config
  );
}


export type UpdateOwnedProviderInput = {
  name?: string;
  baseUrl?: string | null;
  apiKey?: string | null;
  enabled?: boolean;
};

export type UpdateOwnedProviderFields = UnifiedFieldSelection<ModelProviderResourceSchema>[];

export type InferUpdateOwnedProviderResult<
  Fields extends UpdateOwnedProviderFields | undefined,
> = InferResult<ModelProviderResourceSchema, Fields>;

export type UpdateOwnedProviderResult<Fields extends UpdateOwnedProviderFields | undefined = undefined> = | { success: true; data: InferUpdateOwnedProviderResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Provider
 *
 * @ashActionType :update
 */
export async function updateOwnedProvider<Fields extends UpdateOwnedProviderFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateOwnedProviderInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOwnedProviderResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_owned_provider",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOwnedProviderResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ValidateProviderCredentialFields = UnifiedFieldSelection<ModelProviderResourceSchema>[];

export type InferValidateProviderCredentialResult<
  Fields extends ValidateProviderCredentialFields | undefined,
> = InferResult<ModelProviderResourceSchema, Fields>;

export type ValidateProviderCredentialResult<Fields extends ValidateProviderCredentialFields | undefined = undefined> = | { success: true; data: InferValidateProviderCredentialResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Provider
 *
 * @ashActionType :update
 */
export async function validateProviderCredential<Fields extends ValidateProviderCredentialFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidateProviderCredentialResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "validate_provider_credential",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ValidateProviderCredentialResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type InferMarkAllNotificationsReadResult = number;

export type MarkAllNotificationsReadResult = | { success: true; data: InferMarkAllNotificationsReadResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Notification
 *
 * @ashActionType :action
 */
export async function markAllNotificationsRead(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MarkAllNotificationsReadResult> {
  const payload = {
    action: "mark_all_notifications_read",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<MarkAllNotificationsReadResult>(
    payload,
    config
  );
}


export type MarkNotificationReadFields = UnifiedFieldSelection<NotificationResourceSchema>[];

export type InferMarkNotificationReadResult<
  Fields extends MarkNotificationReadFields | undefined,
> = InferResult<NotificationResourceSchema, Fields>;

export type MarkNotificationReadResult<Fields extends MarkNotificationReadFields | undefined = undefined> = | { success: true; data: InferMarkNotificationReadResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Notification
 *
 * @ashActionType :update
 */
export async function markNotificationRead<Fields extends MarkNotificationReadFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MarkNotificationReadResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "mark_notification_read",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<MarkNotificationReadResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnreadNotificationsFields = UnifiedFieldSelection<NotificationResourceSchema>[];
export type InferUnreadNotificationsResult<
  Fields extends UnreadNotificationsFields,
> = Array<InferResult<NotificationResourceSchema, Fields>>;

export type UnreadNotificationsResult<Fields extends UnreadNotificationsFields> = | { success: true; data: InferUnreadNotificationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Notification records
 *
 * @ashActionType :read
 */
export async function unreadNotifications<Fields extends UnreadNotificationsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: NotificationFilterInput;
  sort?: SortString<NotificationSortField> | SortString<NotificationSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnreadNotificationsResult<Fields>> {
  const payload = {
    action: "unread_notifications",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<UnreadNotificationsResult<Fields>>(
    payload,
    config
  );
}


export type ArchiveOrganizationFields = UnifiedFieldSelection<OrganizationResourceSchema>[];

export type InferArchiveOrganizationResult<
  Fields extends ArchiveOrganizationFields | undefined,
> = InferResult<OrganizationResourceSchema, Fields>;

export type ArchiveOrganizationResult<Fields extends ArchiveOrganizationFields | undefined = undefined> = | { success: true; data: InferArchiveOrganizationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Organization
 *
 * @ashActionType :update
 */
export async function archiveOrganization<Fields extends ArchiveOrganizationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ArchiveOrganizationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "archive_organization",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ArchiveOrganizationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreateOrganizationInput = {
  name: string;
  slug: string;
};

export type CreateOrganizationFields = UnifiedFieldSelection<OrganizationResourceSchema>[];

export type InferCreateOrganizationResult<
  Fields extends CreateOrganizationFields | undefined,
> = InferResult<OrganizationResourceSchema, Fields>;

export type CreateOrganizationResult<Fields extends CreateOrganizationFields | undefined = undefined> = | { success: true; data: InferCreateOrganizationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Organization
 *
 * @ashActionType :create
 */
export async function createOrganization<Fields extends CreateOrganizationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOrganizationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOrganizationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_organization",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOrganizationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetOrganizationFields = UnifiedFieldSelection<OrganizationResourceSchema>[];
export type InferGetOrganizationResult<
  Fields extends GetOrganizationFields,
> = InferResult<OrganizationResourceSchema, Fields>;

export type GetOrganizationResult<Fields extends GetOrganizationFields> = | { success: true; data: InferGetOrganizationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Organization records
 *
 * @ashActionType :read
 */
export async function getOrganization<Fields extends GetOrganizationFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUIDv7;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOrganizationResult<Fields>> {
  const payload = {
    action: "get_organization",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOrganizationResult<Fields>>(
    payload,
    config
  );
}


export type GetOrganizationBySlugFields = UnifiedFieldSelection<OrganizationResourceSchema>[];
export type InferGetOrganizationBySlugResult<
  Fields extends GetOrganizationBySlugFields,
> = InferResult<OrganizationResourceSchema, Fields>;

export type GetOrganizationBySlugResult<Fields extends GetOrganizationBySlugFields> = | { success: true; data: InferGetOrganizationBySlugResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Organization records
 *
 * @ashActionType :read
 */
export async function getOrganizationBySlug<Fields extends GetOrganizationBySlugFields>(
  config: {
  tenant?: string;
  getBy: {
    slug: string;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOrganizationBySlugResult<Fields>> {
  const payload = {
    action: "get_organization_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOrganizationBySlugResult<Fields>>(
    payload,
    config
  );
}


export type OrgBillingOverviewInput = {
  organizationId: UUID;
};

export type InferOrgBillingOverviewResult = Record<string, any>;

export type OrgBillingOverviewResult = | { success: true; data: InferOrgBillingOverviewResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Organization
 *
 * @ashActionType :action
 */
export async function orgBillingOverview(
  config: {
  tenant?: string;
  input: OrgBillingOverviewInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<OrgBillingOverviewResult> {
  const payload = {
    action: "org_billing_overview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<OrgBillingOverviewResult>(
    payload,
    config
  );
}


export type OrgUsageOverviewInput = {
  organizationId: UUID;
};

export type InferOrgUsageOverviewResult = Record<string, any>;

export type OrgUsageOverviewResult = | { success: true; data: InferOrgUsageOverviewResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Organization
 *
 * @ashActionType :action
 */
export async function orgUsageOverview(
  config: {
  tenant?: string;
  input: OrgUsageOverviewInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<OrgUsageOverviewResult> {
  const payload = {
    action: "org_usage_overview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<OrgUsageOverviewResult>(
    payload,
    config
  );
}


export type UpdateOrganizationInput = {
  name?: string;
};

export type UpdateOrganizationFields = UnifiedFieldSelection<OrganizationResourceSchema>[];

export type InferUpdateOrganizationResult<
  Fields extends UpdateOrganizationFields | undefined,
> = InferResult<OrganizationResourceSchema, Fields>;

export type UpdateOrganizationResult<Fields extends UpdateOrganizationFields | undefined = undefined> = | { success: true; data: InferUpdateOrganizationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Organization
 *
 * @ashActionType :update
 */
export async function updateOrganization<Fields extends UpdateOrganizationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateOrganizationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOrganizationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_organization",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOrganizationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ChangeOrgMemberRoleInput = {
  role: "member" | "owner";
};

export type ChangeOrgMemberRoleFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferChangeOrgMemberRoleResult<
  Fields extends ChangeOrgMemberRoleFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type ChangeOrgMemberRoleResult<Fields extends ChangeOrgMemberRoleFields | undefined = undefined> = | { success: true; data: InferChangeOrgMemberRoleResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function changeOrgMemberRole<Fields extends ChangeOrgMemberRoleFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: ChangeOrgMemberRoleInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ChangeOrgMemberRoleResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "change_org_member_role",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ChangeOrgMemberRoleResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetOrgMemberByTokenInput = {
  inviteToken: string;
};

export type GetOrgMemberByTokenFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];
export type InferGetOrgMemberByTokenResult<
  Fields extends GetOrgMemberByTokenFields,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type GetOrgMemberByTokenResult<Fields extends GetOrgMemberByTokenFields> = | { success: true; data: InferGetOrgMemberByTokenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OrganizationMember records
 *
 * @ashActionType :read
 */
export async function getOrgMemberByToken<Fields extends GetOrgMemberByTokenFields>(
  config: {
  tenant?: string;
  input: GetOrgMemberByTokenInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOrgMemberByTokenResult<Fields>> {
  const payload = {
    action: "get_org_member_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOrgMemberByTokenResult<Fields>>(
    payload,
    config
  );
}


export type InviteOrgMemberInput = {
  organizationId: UUID;
  inviteEmail: string;
};

export type InviteOrgMemberFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferInviteOrgMemberResult<
  Fields extends InviteOrgMemberFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type InviteOrgMemberResult<Fields extends InviteOrgMemberFields | undefined = undefined> = | { success: true; data: InferInviteOrgMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new OrganizationMember
 *
 * @ashActionType :create
 */
export async function inviteOrgMember<Fields extends InviteOrgMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: InviteOrgMemberInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InviteOrgMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "invite_org_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<InviteOrgMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type LeaveOrgFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferLeaveOrgResult<
  Fields extends LeaveOrgFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type LeaveOrgResult<Fields extends LeaveOrgFields | undefined = undefined> = | { success: true; data: InferLeaveOrgResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function leaveOrg<Fields extends LeaveOrgFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<LeaveOrgResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "leave_org",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<LeaveOrgResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListOrgMembersInput = {
  organizationId: UUID;
};

export type ListOrgMembersFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];
export type InferListOrgMembersResult<
  Fields extends ListOrgMembersFields,
> = Array<InferResult<OrganizationMemberResourceSchema, Fields>>;

export type ListOrgMembersResult<Fields extends ListOrgMembersFields> = | { success: true; data: InferListOrgMembersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OrganizationMember records
 *
 * @ashActionType :read
 */
export async function listOrgMembers<Fields extends ListOrgMembersFields>(
  config: {
  tenant?: string;
  input: ListOrgMembersInput;
  fields: Fields;
  filter?: OrganizationMemberFilterInput;
  sort?: SortString<OrganizationMemberSortField> | SortString<OrganizationMemberSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListOrgMembersResult<Fields>> {
  const payload = {
    action: "list_org_members",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListOrgMembersResult<Fields>>(
    payload,
    config
  );
}


export type MyOrganizationFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];
export type InferMyOrganizationResult<
  Fields extends MyOrganizationFields,
> = Array<InferResult<OrganizationMemberResourceSchema, Fields>>;

export type MyOrganizationResult<Fields extends MyOrganizationFields> = | { success: true; data: InferMyOrganizationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OrganizationMember records
 *
 * @ashActionType :read
 */
export async function myOrganization<Fields extends MyOrganizationFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: OrganizationMemberFilterInput;
  sort?: SortString<OrganizationMemberSortField> | SortString<OrganizationMemberSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyOrganizationResult<Fields>> {
  const payload = {
    action: "my_organization",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyOrganizationResult<Fields>>(
    payload,
    config
  );
}


export type RemoveOrgMemberFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferRemoveOrgMemberResult<
  Fields extends RemoveOrgMemberFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type RemoveOrgMemberResult<Fields extends RemoveOrgMemberFields | undefined = undefined> = | { success: true; data: InferRemoveOrgMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function removeOrgMember<Fields extends RemoveOrgMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveOrgMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "remove_org_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RemoveOrgMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ResendOrgInviteFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferResendOrgInviteResult<
  Fields extends ResendOrgInviteFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type ResendOrgInviteResult<Fields extends ResendOrgInviteFields | undefined = undefined> = | { success: true; data: InferResendOrgInviteResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function resendOrgInvite<Fields extends ResendOrgInviteFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResendOrgInviteResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "resend_org_invite",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResendOrgInviteResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetMemberSpendCapInput = {
  spendCapCents?: number | null;
};

export type SetMemberSpendCapFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferSetMemberSpendCapResult<
  Fields extends SetMemberSpendCapFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type SetMemberSpendCapResult<Fields extends SetMemberSpendCapFields | undefined = undefined> = | { success: true; data: InferSetMemberSpendCapResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function setMemberSpendCap<Fields extends SetMemberSpendCapFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: SetMemberSpendCapInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetMemberSpendCapResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_member_spend_cap",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetMemberSpendCapResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TransferOrgOwnershipFields = UnifiedFieldSelection<OrganizationMemberResourceSchema>[];

export type InferTransferOrgOwnershipResult<
  Fields extends TransferOrgOwnershipFields | undefined,
> = InferResult<OrganizationMemberResourceSchema, Fields>;

export type TransferOrgOwnershipResult<Fields extends TransferOrgOwnershipFields | undefined = undefined> = | { success: true; data: InferTransferOrgOwnershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OrganizationMember
 *
 * @ashActionType :update
 */
export async function transferOrgOwnership<Fields extends TransferOrgOwnershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TransferOrgOwnershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "transfer_org_ownership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TransferOrgOwnershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type BrainTasksInput = {
  brainId: UUID;
};

export type BrainTasksFields = UnifiedFieldSelection<TaskResourceSchema>[];
export type InferBrainTasksResult<
  Fields extends BrainTasksFields,
> = Array<InferResult<TaskResourceSchema, Fields>>;

export type BrainTasksResult<Fields extends BrainTasksFields> = | { success: true; data: InferBrainTasksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Task records
 *
 * @ashActionType :read
 */
export async function brainTasks<Fields extends BrainTasksFields>(
  config: {
  tenant?: string;
  input: BrainTasksInput;
  fields: Fields;
  filter?: TaskFilterInput;
  sort?: SortString<TaskSortField> | SortString<TaskSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainTasksResult<Fields>> {
  const payload = {
    action: "brain_tasks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<BrainTasksResult<Fields>>(
    payload,
    config
  );
}


export type ClaimPlanTaskInput = {
  assignedToUserId?: UUID | null;
  assignedToAgent?: string | null;
};

export type ClaimPlanTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferClaimPlanTaskResult<
  Fields extends ClaimPlanTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type ClaimPlanTaskResult<Fields extends ClaimPlanTaskFields | undefined = undefined> = | { success: true; data: InferClaimPlanTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function claimPlanTask<Fields extends ClaimPlanTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: ClaimPlanTaskInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ClaimPlanTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "claim_plan_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ClaimPlanTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CompleteTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferCompleteTaskResult<
  Fields extends CompleteTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type CompleteTaskResult<Fields extends CompleteTaskFields | undefined = undefined> = | { success: true; data: InferCompleteTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function completeTask<Fields extends CompleteTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CompleteTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "complete_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CompleteTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationTasksInput = {
  conversationId: UUID;
};

export type ConversationTasksFields = UnifiedFieldSelection<TaskResourceSchema>[];
export type InferConversationTasksResult<
  Fields extends ConversationTasksFields,
> = Array<InferResult<TaskResourceSchema, Fields>>;

export type ConversationTasksResult<Fields extends ConversationTasksFields> = | { success: true; data: InferConversationTasksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Task records
 *
 * @ashActionType :read
 */
export async function conversationTasks<Fields extends ConversationTasksFields>(
  config: {
  tenant?: string;
  input: ConversationTasksInput;
  fields: Fields;
  filter?: TaskFilterInput;
  sort?: SortString<TaskSortField> | SortString<TaskSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationTasksResult<Fields>> {
  const payload = {
    action: "conversation_tasks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationTasksResult<Fields>>(
    payload,
    config
  );
}


export type CreateConversationTaskInput = {
  title: string;
  description?: string | null;
  status?: "archived" | "blocked" | "cancelled" | "done" | "in_progress" | "open";
  position?: number | null;
  priority?: "high" | "low" | "normal" | "urgent";
  assignedToAgent?: string | null;
  metadata?: Record<string, any> | null;
  parentId?: UUID | null;
  assignedToUserId?: UUID | null;
  assignedToCustomAgentId?: UUID | null;
  assignedByCustomAgentId?: UUID | null;
  blockedReason?: string | null;
  waitingOnUser?: boolean | null;
  dueAt?: UtcDateTimeUsec | null;
  recurrence?: Record<string, any> | null;
  conversationId: UUID;
};

export type CreateConversationTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferCreateConversationTaskResult<
  Fields extends CreateConversationTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type CreateConversationTaskResult<Fields extends CreateConversationTaskFields | undefined = undefined> = | { success: true; data: InferCreateConversationTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Task
 *
 * @ashActionType :create
 */
export async function createConversationTask<Fields extends CreateConversationTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateConversationTaskInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateConversationTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_conversation_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateConversationTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreatePlanTaskInput = {
  title: string;
  description?: string | null;
  status?: "archived" | "blocked" | "cancelled" | "done" | "in_progress" | "open";
  position?: number | null;
  priority?: "high" | "low" | "normal" | "urgent";
  metadata?: Record<string, any> | null;
  parentId?: UUID | null;
  assignedToUserId?: UUID | null;
  assignedToAgent?: string | null;
  blockedReason?: string | null;
  dueAt?: UtcDateTimeUsec | null;
  createdByLabel?: string | null;
  brainPageId: UUID;
};

export type CreatePlanTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferCreatePlanTaskResult<
  Fields extends CreatePlanTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type CreatePlanTaskResult<Fields extends CreatePlanTaskFields | undefined = undefined> = | { success: true; data: InferCreatePlanTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Task
 *
 * @ashActionType :create
 */
export async function createPlanTask<Fields extends CreatePlanTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreatePlanTaskInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreatePlanTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_plan_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreatePlanTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroyConversationTaskResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Task
 *
 * @ashActionType :destroy
 */
export async function destroyConversationTask(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyConversationTaskResult> {
  const payload = {
    action: "destroy_conversation_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyConversationTaskResult>(
    payload,
    config
  );
}


export type DismissTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferDismissTaskResult<
  Fields extends DismissTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type DismissTaskResult<Fields extends DismissTaskFields | undefined = undefined> = | { success: true; data: InferDismissTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function dismissTask<Fields extends DismissTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DismissTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "dismiss_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DismissTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListOpenTasksInput = {
  userId: UUID;
};

export type ListOpenTasksFields = UnifiedFieldSelection<TaskResourceSchema>[];
export type InferListOpenTasksResult<
  Fields extends ListOpenTasksFields,
> = Array<InferResult<TaskResourceSchema, Fields>>;

export type ListOpenTasksResult<Fields extends ListOpenTasksFields> = | { success: true; data: InferListOpenTasksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Task records
 *
 * @ashActionType :read
 */
export async function listOpenTasks<Fields extends ListOpenTasksFields>(
  config: {
  tenant?: string;
  input: ListOpenTasksInput;
  fields: Fields;
  filter?: TaskFilterInput;
  sort?: SortString<TaskSortField> | SortString<TaskSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListOpenTasksResult<Fields>> {
  const payload = {
    action: "list_open_tasks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListOpenTasksResult<Fields>>(
    payload,
    config
  );
}


export type PlanTasksInput = {
  brainPageId: UUID;
};

export type PlanTasksFields = UnifiedFieldSelection<TaskResourceSchema>[];
export type InferPlanTasksResult<
  Fields extends PlanTasksFields,
> = Array<InferResult<TaskResourceSchema, Fields>>;

export type PlanTasksResult<Fields extends PlanTasksFields> = | { success: true; data: InferPlanTasksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Task records
 *
 * @ashActionType :read
 */
export async function planTasks<Fields extends PlanTasksFields>(
  config: {
  tenant?: string;
  input: PlanTasksInput;
  fields: Fields;
  filter?: TaskFilterInput;
  sort?: SortString<TaskSortField> | SortString<TaskSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PlanTasksResult<Fields>> {
  const payload = {
    action: "plan_tasks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<PlanTasksResult<Fields>>(
    payload,
    config
  );
}


export type ReadyPlanTasksInput = {
  brainPageId: UUID;
};

export type ReadyPlanTasksFields = UnifiedFieldSelection<TaskResourceSchema>[];
export type InferReadyPlanTasksResult<
  Fields extends ReadyPlanTasksFields,
> = Array<InferResult<TaskResourceSchema, Fields>>;

export type ReadyPlanTasksResult<Fields extends ReadyPlanTasksFields> = | { success: true; data: InferReadyPlanTasksResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Task records
 *
 * @ashActionType :read
 */
export async function readyPlanTasks<Fields extends ReadyPlanTasksFields>(
  config: {
  tenant?: string;
  input: ReadyPlanTasksInput;
  fields: Fields;
  filter?: TaskFilterInput;
  sort?: SortString<TaskSortField> | SortString<TaskSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ReadyPlanTasksResult<Fields>> {
  const payload = {
    action: "ready_plan_tasks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ReadyPlanTasksResult<Fields>>(
    payload,
    config
  );
}


export type ReleasePlanTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferReleasePlanTaskResult<
  Fields extends ReleasePlanTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type ReleasePlanTaskResult<Fields extends ReleasePlanTaskFields | undefined = undefined> = | { success: true; data: InferReleasePlanTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function releasePlanTask<Fields extends ReleasePlanTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ReleasePlanTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "release_plan_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ReleasePlanTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateConversationTaskInput = {
  title?: string;
  description?: string | null;
  status?: "archived" | "blocked" | "cancelled" | "done" | "in_progress" | "open";
  position?: number | null;
  priority?: "high" | "low" | "normal" | "urgent";
  assignedToAgent?: string | null;
  metadata?: Record<string, any> | null;
  parentId?: UUID | null;
  assignedToUserId?: UUID | null;
  assignedToCustomAgentId?: UUID | null;
  assignedByCustomAgentId?: UUID | null;
  blockedReason?: string | null;
  waitingOnUser?: boolean | null;
  resultSummary?: string | null;
  dueAt?: UtcDateTimeUsec | null;
  recurrence?: Record<string, any> | null;
};

export type UpdateConversationTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferUpdateConversationTaskResult<
  Fields extends UpdateConversationTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type UpdateConversationTaskResult<Fields extends UpdateConversationTaskFields | undefined = undefined> = | { success: true; data: InferUpdateConversationTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function updateConversationTask<Fields extends UpdateConversationTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateConversationTaskInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateConversationTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_conversation_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateConversationTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdatePlanTaskInput = {
  title?: string;
  description?: string | null;
  status?: "archived" | "blocked" | "cancelled" | "done" | "in_progress" | "open";
  position?: number | null;
  priority?: "high" | "low" | "normal" | "urgent";
  assignedToAgent?: string | null;
  metadata?: Record<string, any> | null;
  parentId?: UUID | null;
  assignedToUserId?: UUID | null;
  assignedToCustomAgentId?: UUID | null;
  assignedByCustomAgentId?: UUID | null;
  blockedReason?: string | null;
  waitingOnUser?: boolean | null;
  resultSummary?: string | null;
  dueAt?: UtcDateTimeUsec | null;
  recurrence?: Record<string, any> | null;
};

export type UpdatePlanTaskFields = UnifiedFieldSelection<TaskResourceSchema>[];

export type InferUpdatePlanTaskResult<
  Fields extends UpdatePlanTaskFields | undefined,
> = InferResult<TaskResourceSchema, Fields>;

export type UpdatePlanTaskResult<Fields extends UpdatePlanTaskFields | undefined = undefined> = | { success: true; data: InferUpdatePlanTaskResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Task
 *
 * @ashActionType :update
 */
export async function updatePlanTask<Fields extends UpdatePlanTaskFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdatePlanTaskInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdatePlanTaskResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_plan_task",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdatePlanTaskResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type AddTaskDependencyInput = {
  taskId: UUID;
  dependsOnId: UUID;
};

export type AddTaskDependencyFields = UnifiedFieldSelection<TaskDependencyResourceSchema>[];

export type InferAddTaskDependencyResult<
  Fields extends AddTaskDependencyFields | undefined,
> = InferResult<TaskDependencyResourceSchema, Fields>;

export type AddTaskDependencyResult<Fields extends AddTaskDependencyFields | undefined = undefined> = | { success: true; data: InferAddTaskDependencyResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new TaskDependency
 *
 * @ashActionType :create
 */
export async function addTaskDependency<Fields extends AddTaskDependencyFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: AddTaskDependencyInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AddTaskDependencyResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "add_task_dependency",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AddTaskDependencyResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type RemoveTaskDependencyResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a TaskDependency
 *
 * @ashActionType :destroy
 */
export async function removeTaskDependency(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RemoveTaskDependencyResult> {
  const payload = {
    action: "remove_task_dependency",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<RemoveTaskDependencyResult>(
    payload,
    config
  );
}


export type TaskDependenciesInput = {
  taskId: UUID;
};

export type TaskDependenciesFields = UnifiedFieldSelection<TaskDependencyResourceSchema>[];
export type InferTaskDependenciesResult<
  Fields extends TaskDependenciesFields,
> = Array<InferResult<TaskDependencyResourceSchema, Fields>>;

export type TaskDependenciesResult<Fields extends TaskDependenciesFields> = | { success: true; data: InferTaskDependenciesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read TaskDependency records
 *
 * @ashActionType :read
 */
export async function taskDependencies<Fields extends TaskDependenciesFields>(
  config: {
  tenant?: string;
  input: TaskDependenciesInput;
  fields: Fields;
  filter?: TaskDependencyFilterInput;
  sort?: SortString<TaskDependencySortField> | SortString<TaskDependencySortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TaskDependenciesResult<Fields>> {
  const payload = {
    action: "task_dependencies",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<TaskDependenciesResult<Fields>>(
    payload,
    config
  );
}


export type BrainTaskEventsInput = {
  brainId: UUID;
};

export type BrainTaskEventsFields = UnifiedFieldSelection<TaskEventResourceSchema>[];
export type InferBrainTaskEventsResult<
  Fields extends BrainTaskEventsFields,
> = Array<InferResult<TaskEventResourceSchema, Fields>>;

export type BrainTaskEventsResult<Fields extends BrainTaskEventsFields> = | { success: true; data: InferBrainTaskEventsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read TaskEvent records
 *
 * @ashActionType :read
 */
export async function brainTaskEvents<Fields extends BrainTaskEventsFields>(
  config: {
  tenant?: string;
  input: BrainTaskEventsInput;
  fields: Fields;
  filter?: TaskEventFilterInput;
  sort?: SortString<TaskEventSortField> | SortString<TaskEventSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BrainTaskEventsResult<Fields>> {
  const payload = {
    action: "brain_task_events",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<BrainTaskEventsResult<Fields>>(
    payload,
    config
  );
}


export type PlanTaskEventsInput = {
  brainPageId: UUID;
};

export type PlanTaskEventsFields = UnifiedFieldSelection<TaskEventResourceSchema>[];
export type InferPlanTaskEventsResult<
  Fields extends PlanTaskEventsFields,
> = Array<InferResult<TaskEventResourceSchema, Fields>>;

export type PlanTaskEventsResult<Fields extends PlanTaskEventsFields> = | { success: true; data: InferPlanTaskEventsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read TaskEvent records
 *
 * @ashActionType :read
 */
export async function planTaskEvents<Fields extends PlanTaskEventsFields>(
  config: {
  tenant?: string;
  input: PlanTaskEventsInput;
  fields: Fields;
  filter?: TaskEventFilterInput;
  sort?: SortString<TaskEventSortField> | SortString<TaskEventSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PlanTaskEventsResult<Fields>> {
  const payload = {
    action: "plan_task_events",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<PlanTaskEventsResult<Fields>>(
    payload,
    config
  );
}


export type CreateSandboxSecretInput = {
  key: string;
  value: string;
  description?: string | null;
};

export type CreateSandboxSecretFields = UnifiedFieldSelection<SandboxSecretResourceSchema>[];

export type InferCreateSandboxSecretResult<
  Fields extends CreateSandboxSecretFields | undefined,
> = InferResult<SandboxSecretResourceSchema, Fields>;

export type CreateSandboxSecretResult<Fields extends CreateSandboxSecretFields | undefined = undefined> = | { success: true; data: InferCreateSandboxSecretResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new SandboxSecret
 *
 * @ashActionType :create
 */
export async function createSandboxSecret<Fields extends CreateSandboxSecretFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateSandboxSecretInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateSandboxSecretResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_sandbox_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateSandboxSecretResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroySandboxSecretResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a SandboxSecret
 *
 * @ashActionType :destroy
 */
export async function destroySandboxSecret(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroySandboxSecretResult> {
  const payload = {
    action: "destroy_sandbox_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroySandboxSecretResult>(
    payload,
    config
  );
}


export type MySandboxSecretsFields = UnifiedFieldSelection<SandboxSecretResourceSchema>[];
export type InferMySandboxSecretsResult<
  Fields extends MySandboxSecretsFields,
> = Array<InferResult<SandboxSecretResourceSchema, Fields>>;

export type MySandboxSecretsResult<Fields extends MySandboxSecretsFields> = | { success: true; data: InferMySandboxSecretsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read SandboxSecret records
 *
 * @ashActionType :read
 */
export async function mySandboxSecrets<Fields extends MySandboxSecretsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: SandboxSecretFilterInput;
  sort?: SortString<SandboxSecretSortField> | SortString<SandboxSecretSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MySandboxSecretsResult<Fields>> {
  const payload = {
    action: "my_sandbox_secrets",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MySandboxSecretsResult<Fields>>(
    payload,
    config
  );
}


export type UpdateSandboxSecretInput = {
  value?: string;
  description?: string | null;
};

export type UpdateSandboxSecretFields = UnifiedFieldSelection<SandboxSecretResourceSchema>[];

export type InferUpdateSandboxSecretResult<
  Fields extends UpdateSandboxSecretFields | undefined,
> = InferResult<SandboxSecretResourceSchema, Fields>;

export type UpdateSandboxSecretResult<Fields extends UpdateSandboxSecretFields | undefined = undefined> = | { success: true; data: InferUpdateSandboxSecretResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing SandboxSecret
 *
 * @ashActionType :update
 */
export async function updateSandboxSecret<Fields extends UpdateSandboxSecretFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateSandboxSecretInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateSandboxSecretResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_sandbox_secret",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateSandboxSecretResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CreateSkillInput = {
  name: string;
  displayName?: string | null;
  description: string;
  body?: string | null;
  requestedTools?: Array<string> | null;
  requiredSecrets?: Array<Record<string, any>> | null;
  runtimeHints?: Record<string, any> | null;
  metadata?: Record<string, any> | null;
  version?: string | null;
  license?: string | null;
  compatibility?: string | null;
  icon?: string | null;
  color?: string | null;
  sourceFormat?: "agents_md" | "goose" | "other" | "skill_md";
  sourceUrl?: string | null;
  workspaceId?: UUID | null;
};

export type CreateSkillFields = UnifiedFieldSelection<SkillResourceSchema>[];

export type InferCreateSkillResult<
  Fields extends CreateSkillFields | undefined,
> = InferResult<SkillResourceSchema, Fields>;

export type CreateSkillResult<Fields extends CreateSkillFields | undefined = undefined> = | { success: true; data: InferCreateSkillResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Skill
 *
 * @ashActionType :create
 */
export async function createSkill<Fields extends CreateSkillFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateSkillInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateSkillResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateSkillResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DestroySkillResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Skill
 *
 * @ashActionType :destroy
 */
export async function destroySkill(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroySkillResult> {
  const payload = {
    action: "destroy_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroySkillResult>(
    payload,
    config
  );
}


export type GetSkillFields = UnifiedFieldSelection<SkillResourceSchema>[];
export type InferGetSkillResult<
  Fields extends GetSkillFields,
> = InferResult<SkillResourceSchema, Fields>;

export type GetSkillResult<Fields extends GetSkillFields> = | { success: true; data: InferGetSkillResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Skill records
 *
 * @ashActionType :read
 */
export async function getSkill<Fields extends GetSkillFields>(
  config: {
  tenant?: string;
  getBy: {
    id: UUID;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetSkillResult<Fields>> {
  const payload = {
    action: "get_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetSkillResult<Fields>>(
    payload,
    config
  );
}


export type MyFavoriteSkillsFields = UnifiedFieldSelection<SkillResourceSchema>[];
export type InferMyFavoriteSkillsResult<
  Fields extends MyFavoriteSkillsFields,
> = Array<InferResult<SkillResourceSchema, Fields>>;

export type MyFavoriteSkillsResult<Fields extends MyFavoriteSkillsFields> = | { success: true; data: InferMyFavoriteSkillsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Skill records
 *
 * @ashActionType :read
 */
export async function myFavoriteSkills<Fields extends MyFavoriteSkillsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: SkillFilterInput;
  sort?: SortString<SkillSortField> | SortString<SkillSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyFavoriteSkillsResult<Fields>> {
  const payload = {
    action: "my_favorite_skills",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyFavoriteSkillsResult<Fields>>(
    payload,
    config
  );
}


export type MySkillsFields = UnifiedFieldSelection<SkillResourceSchema>[];
export type InferMySkillsResult<
  Fields extends MySkillsFields,
> = Array<InferResult<SkillResourceSchema, Fields>>;

export type MySkillsResult<Fields extends MySkillsFields> = | { success: true; data: InferMySkillsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Skill records
 *
 * @ashActionType :read
 */
export async function mySkills<Fields extends MySkillsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: SkillFilterInput;
  sort?: SortString<SkillSortField> | SortString<SkillSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MySkillsResult<Fields>> {
  const payload = {
    action: "my_skills",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MySkillsResult<Fields>>(
    payload,
    config
  );
}


export type ShareSkillToTeamFields = UnifiedFieldSelection<SkillResourceSchema>[];

export type InferShareSkillToTeamResult<
  Fields extends ShareSkillToTeamFields | undefined,
> = InferResult<SkillResourceSchema, Fields>;

export type ShareSkillToTeamResult<Fields extends ShareSkillToTeamFields | undefined = undefined> = | { success: true; data: InferShareSkillToTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Skill
 *
 * @ashActionType :update
 */
export async function shareSkillToTeam<Fields extends ShareSkillToTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ShareSkillToTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "share_skill_to_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ShareSkillToTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UnshareSkillFromTeamFields = UnifiedFieldSelection<SkillResourceSchema>[];

export type InferUnshareSkillFromTeamResult<
  Fields extends UnshareSkillFromTeamFields | undefined,
> = InferResult<SkillResourceSchema, Fields>;

export type UnshareSkillFromTeamResult<Fields extends UnshareSkillFromTeamFields | undefined = undefined> = | { success: true; data: InferUnshareSkillFromTeamResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Skill
 *
 * @ashActionType :update
 */
export async function unshareSkillFromTeam<Fields extends UnshareSkillFromTeamFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnshareSkillFromTeamResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "unshare_skill_from_team",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UnshareSkillFromTeamResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateSkillInput = {
  name?: string;
  displayName?: string | null;
  description?: string;
  body?: string | null;
  requestedTools?: Array<string> | null;
  requiredSecrets?: Array<Record<string, any>> | null;
  runtimeHints?: Record<string, any> | null;
  metadata?: Record<string, any> | null;
  version?: string | null;
  license?: string | null;
  compatibility?: string | null;
  icon?: string | null;
  color?: string | null;
};

export type UpdateSkillFields = UnifiedFieldSelection<SkillResourceSchema>[];

export type InferUpdateSkillResult<
  Fields extends UpdateSkillFields | undefined,
> = InferResult<SkillResourceSchema, Fields>;

export type UpdateSkillResult<Fields extends UpdateSkillFields | undefined = undefined> = | { success: true; data: InferUpdateSkillResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Skill
 *
 * @ashActionType :update
 */
export async function updateSkill<Fields extends UpdateSkillFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateSkillInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateSkillResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateSkillResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceSkillsInput = {
  workspaceId: UUID;
};

export type WorkspaceSkillsFields = UnifiedFieldSelection<SkillResourceSchema>[];
export type InferWorkspaceSkillsResult<
  Fields extends WorkspaceSkillsFields,
> = Array<InferResult<SkillResourceSchema, Fields>>;

export type WorkspaceSkillsResult<Fields extends WorkspaceSkillsFields> = | { success: true; data: InferWorkspaceSkillsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Skill records
 *
 * @ashActionType :read
 */
export async function workspaceSkills<Fields extends WorkspaceSkillsFields>(
  config: {
  tenant?: string;
  input: WorkspaceSkillsInput;
  fields: Fields;
  filter?: SkillFilterInput;
  sort?: SortString<SkillSortField> | SortString<SkillSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceSkillsResult<Fields>> {
  const payload = {
    action: "workspace_skills",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<WorkspaceSkillsResult<Fields>>(
    payload,
    config
  );
}


export type FavoriteSkillInput = {
  skillId: UUID;
};

export type FavoriteSkillFields = UnifiedFieldSelection<SkillFavoriteResourceSchema>[];

export type InferFavoriteSkillResult<
  Fields extends FavoriteSkillFields | undefined,
> = InferResult<SkillFavoriteResourceSchema, Fields>;

export type FavoriteSkillResult<Fields extends FavoriteSkillFields | undefined = undefined> = | { success: true; data: InferFavoriteSkillResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new SkillFavorite
 *
 * @ashActionType :create
 */
export async function favoriteSkill<Fields extends FavoriteSkillFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: FavoriteSkillInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FavoriteSkillResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "favorite_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<FavoriteSkillResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type MySkillFavoritesFields = UnifiedFieldSelection<SkillFavoriteResourceSchema>[];
export type InferMySkillFavoritesResult<
  Fields extends MySkillFavoritesFields,
> = Array<InferResult<SkillFavoriteResourceSchema, Fields>>;

export type MySkillFavoritesResult<Fields extends MySkillFavoritesFields> = | { success: true; data: InferMySkillFavoritesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read SkillFavorite records
 *
 * @ashActionType :read
 */
export async function mySkillFavorites<Fields extends MySkillFavoritesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: SkillFavoriteFilterInput;
  sort?: SortString<SkillFavoriteSortField> | SortString<SkillFavoriteSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MySkillFavoritesResult<Fields>> {
  const payload = {
    action: "my_skill_favorites",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MySkillFavoritesResult<Fields>>(
    payload,
    config
  );
}


export type UnfavoriteSkillResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a SkillFavorite
 *
 * @ashActionType :destroy
 */
export async function unfavoriteSkill(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UnfavoriteSkillResult> {
  const payload = {
    action: "unfavorite_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<UnfavoriteSkillResult>(
    payload,
    config
  );
}


export type MySkillTrustsFields = UnifiedFieldSelection<SkillTrustResourceSchema>[];
export type InferMySkillTrustsResult<
  Fields extends MySkillTrustsFields,
> = Array<InferResult<SkillTrustResourceSchema, Fields>>;

export type MySkillTrustsResult<Fields extends MySkillTrustsFields> = | { success: true; data: InferMySkillTrustsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read SkillTrust records
 *
 * @ashActionType :read
 */
export async function mySkillTrusts<Fields extends MySkillTrustsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: SkillTrustFilterInput;
  sort?: SortString<SkillTrustSortField> | SortString<SkillTrustSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MySkillTrustsResult<Fields>> {
  const payload = {
    action: "my_skill_trusts",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MySkillTrustsResult<Fields>>(
    payload,
    config
  );
}


export type TrustSkillInput = {
  skillId: UUID;
};

export type TrustSkillFields = UnifiedFieldSelection<SkillTrustResourceSchema>[];

export type InferTrustSkillResult<
  Fields extends TrustSkillFields | undefined,
> = InferResult<SkillTrustResourceSchema, Fields>;

export type TrustSkillResult<Fields extends TrustSkillFields | undefined = undefined> = | { success: true; data: InferTrustSkillResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new SkillTrust
 *
 * @ashActionType :create
 */
export async function trustSkill<Fields extends TrustSkillFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: TrustSkillInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TrustSkillResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "trust_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TrustSkillResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UntrustSkillResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a SkillTrust
 *
 * @ashActionType :destroy
 */
export async function untrustSkill(
  config: {
  tenant?: string;
  identity: UUIDv7;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UntrustSkillResult> {
  const payload = {
    action: "untrust_skill",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<UntrustSkillResult>(
    payload,
    config
  );
}


export type InferBillingOverviewResult = Record<string, any>;

export type BillingOverviewResult = | { success: true; data: InferBillingOverviewResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Account
 *
 * @ashActionType :action
 */
export async function billingOverview(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<BillingOverviewResult> {
  const payload = {
    action: "billing_overview",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<BillingOverviewResult>(
    payload,
    config
  );
}


export type InferChatFeatureLimitsResult = Record<string, any>;

export type ChatFeatureLimitsResult = | { success: true; data: InferChatFeatureLimitsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Account
 *
 * @ashActionType :action
 */
export async function chatFeatureLimits(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ChatFeatureLimitsResult> {
  const payload = {
    action: "chat_feature_limits",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<ChatFeatureLimitsResult>(
    payload,
    config
  );
}


export type InferCreditStatusResult = Record<string, any>;

export type CreditStatusResult = | { success: true; data: InferCreditStatusResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Account
 *
 * @ashActionType :action
 */
export async function creditStatus(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreditStatusResult> {
  const payload = {
    action: "credit_status",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<CreditStatusResult>(
    payload,
    config
  );
}


export type InferMoneyUsageStatusResult = Record<string, any>;

export type MoneyUsageStatusResult = | { success: true; data: InferMoneyUsageStatusResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Account
 *
 * @ashActionType :action
 */
export async function moneyUsageStatus(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MoneyUsageStatusResult> {
  const payload = {
    action: "money_usage_status",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<MoneyUsageStatusResult>(
    payload,
    config
  );
}


export type SetBillingPreferencesInput = {
  monthlySpendCapCents?: number | null;
  noSpendCap: boolean;
};

export type InferSetBillingPreferencesResult = Record<string, any>;

export type SetBillingPreferencesResult = | { success: true; data: InferSetBillingPreferencesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Account
 *
 * @ashActionType :action
 */
export async function setBillingPreferences(
  config: {
  tenant?: string;
  input: SetBillingPreferencesInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetBillingPreferencesResult> {
  const payload = {
    action: "set_billing_preferences",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SetBillingPreferencesResult>(
    payload,
    config
  );
}


export type ActivateWorkbenchTabInput = {
  tabId: string;
};

export type ActivateWorkbenchTabFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferActivateWorkbenchTabResult<
  Fields extends ActivateWorkbenchTabFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type ActivateWorkbenchTabResult<Fields extends ActivateWorkbenchTabFields | undefined = undefined> = | { success: true; data: InferActivateWorkbenchTabResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function activateWorkbenchTab<Fields extends ActivateWorkbenchTabFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: ActivateWorkbenchTabInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ActivateWorkbenchTabResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "activate_workbench_tab",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ActivateWorkbenchTabResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type CloseWorkbenchTabInput = {
  tabId: string;
};

export type CloseWorkbenchTabFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferCloseWorkbenchTabResult<
  Fields extends CloseWorkbenchTabFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type CloseWorkbenchTabResult<Fields extends CloseWorkbenchTabFields | undefined = undefined> = | { success: true; data: InferCloseWorkbenchTabResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function closeWorkbenchTab<Fields extends CloseWorkbenchTabFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: CloseWorkbenchTabInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CloseWorkbenchTabResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "close_workbench_tab",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CloseWorkbenchTabResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetOrCreateTabSessionInput = {
  userId: UUID;
  workspaceId?: UUID | null;
};

export type GetOrCreateTabSessionFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferGetOrCreateTabSessionResult<
  Fields extends GetOrCreateTabSessionFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type GetOrCreateTabSessionResult<Fields extends GetOrCreateTabSessionFields | undefined = undefined> = | { success: true; data: InferGetOrCreateTabSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new TabSession
 *
 * @ashActionType :create
 */
export async function getOrCreateTabSession<Fields extends GetOrCreateTabSessionFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: GetOrCreateTabSessionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOrCreateTabSessionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "get_or_create_tab_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOrCreateTabSessionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetTabSessionInput = {
  workspaceId?: UUID | null;
};

export type GetTabSessionFields = UnifiedFieldSelection<TabSessionResourceSchema>[];
export type InferGetTabSessionResult<
  Fields extends GetTabSessionFields,
> = InferResult<TabSessionResourceSchema, Fields>;

export type GetTabSessionResult<Fields extends GetTabSessionFields> = | { success: true; data: InferGetTabSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read TabSession records
 *
 * @ashActionType :read
 */
export async function getTabSession<Fields extends GetTabSessionFields>(
  config: {
  tenant?: string;
  input?: GetTabSessionInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetTabSessionResult<Fields>> {
  const payload = {
    action: "get_tab_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetTabSessionResult<Fields>>(
    payload,
    config
  );
}


export type OpenWorkbenchTabInput = {
  primary: Record<string, any>;
  label?: string | null;
  single?: boolean | null;
};

export type OpenWorkbenchTabFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferOpenWorkbenchTabResult<
  Fields extends OpenWorkbenchTabFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type OpenWorkbenchTabResult<Fields extends OpenWorkbenchTabFields | undefined = undefined> = | { success: true; data: InferOpenWorkbenchTabResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function openWorkbenchTab<Fields extends OpenWorkbenchTabFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: OpenWorkbenchTabInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<OpenWorkbenchTabResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "open_workbench_tab",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<OpenWorkbenchTabResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ReorderWorkbenchTabsInput = {
  order: Array<string>;
};

export type ReorderWorkbenchTabsFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferReorderWorkbenchTabsResult<
  Fields extends ReorderWorkbenchTabsFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type ReorderWorkbenchTabsResult<Fields extends ReorderWorkbenchTabsFields | undefined = undefined> = | { success: true; data: InferReorderWorkbenchTabsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function reorderWorkbenchTabs<Fields extends ReorderWorkbenchTabsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: ReorderWorkbenchTabsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ReorderWorkbenchTabsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "reorder_workbench_tabs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ReorderWorkbenchTabsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ReplaceWorkbenchTabsInput = {
  tabs: Array<Record<string, any>>;
  activeTabId?: string | null;
};

export type ReplaceWorkbenchTabsFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferReplaceWorkbenchTabsResult<
  Fields extends ReplaceWorkbenchTabsFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type ReplaceWorkbenchTabsResult<Fields extends ReplaceWorkbenchTabsFields | undefined = undefined> = | { success: true; data: InferReplaceWorkbenchTabsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function replaceWorkbenchTabs<Fields extends ReplaceWorkbenchTabsFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: ReplaceWorkbenchTabsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ReplaceWorkbenchTabsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "replace_workbench_tabs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ReplaceWorkbenchTabsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetTabSessionModeInput = {
  mode?: "agents" | "brain" | "chat" | "files" | "library" | "prompts" | "skills";
};

export type SetTabSessionModeFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferSetTabSessionModeResult<
  Fields extends SetTabSessionModeFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type SetTabSessionModeResult<Fields extends SetTabSessionModeFields | undefined = undefined> = | { success: true; data: InferSetTabSessionModeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function setTabSessionMode<Fields extends SetTabSessionModeFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SetTabSessionModeInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetTabSessionModeResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_tab_session_mode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetTabSessionModeResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetTabSessionNavFilterInput = {
  navFilter?: "all" | "personal" | "shared";
};

export type SetTabSessionNavFilterFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferSetTabSessionNavFilterResult<
  Fields extends SetTabSessionNavFilterFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type SetTabSessionNavFilterResult<Fields extends SetTabSessionNavFilterFields | undefined = undefined> = | { success: true; data: InferSetTabSessionNavFilterResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function setTabSessionNavFilter<Fields extends SetTabSessionNavFilterFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SetTabSessionNavFilterInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetTabSessionNavFilterResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_tab_session_nav_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetTabSessionNavFilterResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type SetWorkbenchCompanionInput = {
  tabId: string;
  companion?: Record<string, any> | null;
};

export type SetWorkbenchCompanionFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferSetWorkbenchCompanionResult<
  Fields extends SetWorkbenchCompanionFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type SetWorkbenchCompanionResult<Fields extends SetWorkbenchCompanionFields | undefined = undefined> = | { success: true; data: InferSetWorkbenchCompanionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function setWorkbenchCompanion<Fields extends SetWorkbenchCompanionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: SetWorkbenchCompanionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SetWorkbenchCompanionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "set_workbench_companion",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SetWorkbenchCompanionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UpdateTabPrimaryInput = {
  tabId: string;
  primary: Record<string, any>;
};

export type UpdateTabPrimaryFields = UnifiedFieldSelection<TabSessionResourceSchema>[];

export type InferUpdateTabPrimaryResult<
  Fields extends UpdateTabPrimaryFields | undefined,
> = InferResult<TabSessionResourceSchema, Fields>;

export type UpdateTabPrimaryResult<Fields extends UpdateTabPrimaryFields | undefined = undefined> = | { success: true; data: InferUpdateTabPrimaryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing TabSession
 *
 * @ashActionType :update
 */
export async function updateTabPrimary<Fields extends UpdateTabPrimaryFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateTabPrimaryInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateTabPrimaryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_tab_primary",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateTabPrimaryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ConversationJobsInput = {
  conversationId: UUID;
};

export type ConversationJobsFields = UnifiedFieldSelection<JobResourceSchema>[];
export type InferConversationJobsResult<
  Fields extends ConversationJobsFields,
> = Array<InferResult<JobResourceSchema, Fields>>;

export type ConversationJobsResult<Fields extends ConversationJobsFields> = | { success: true; data: InferConversationJobsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Job records
 *
 * @ashActionType :read
 */
export async function conversationJobs<Fields extends ConversationJobsFields>(
  config: {
  tenant?: string;
  input: ConversationJobsInput;
  fields: Fields;
  filter?: JobFilterInput;
  sort?: SortString<JobSortField> | SortString<JobSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ConversationJobsResult<Fields>> {
  const payload = {
    action: "conversation_jobs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ConversationJobsResult<Fields>>(
    payload,
    config
  );
}


export type PauseJobFields = UnifiedFieldSelection<JobResourceSchema>[];

export type InferPauseJobResult<
  Fields extends PauseJobFields | undefined,
> = InferResult<JobResourceSchema, Fields>;

export type PauseJobResult<Fields extends PauseJobFields | undefined = undefined> = | { success: true; data: InferPauseJobResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Job
 *
 * @ashActionType :update
 */
export async function pauseJob<Fields extends PauseJobFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PauseJobResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "pause_job",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<PauseJobResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ResumeJobFields = UnifiedFieldSelection<JobResourceSchema>[];

export type InferResumeJobResult<
  Fields extends ResumeJobFields | undefined,
> = InferResult<JobResourceSchema, Fields>;

export type ResumeJobResult<Fields extends ResumeJobFields | undefined = undefined> = | { success: true; data: InferResumeJobResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Job
 *
 * @ashActionType :update
 */
export async function resumeJob<Fields extends ResumeJobFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResumeJobResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "resume_job",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResumeJobResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type StopJobFields = UnifiedFieldSelection<JobResourceSchema>[];

export type InferStopJobResult<
  Fields extends StopJobFields | undefined,
> = InferResult<JobResourceSchema, Fields>;

export type StopJobResult<Fields extends StopJobFields | undefined = undefined> = | { success: true; data: InferStopJobResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Job
 *
 * @ashActionType :update
 */
export async function stopJob<Fields extends StopJobFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<StopJobResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "stop_job",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<StopJobResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TriggerJobNowFields = UnifiedFieldSelection<JobResourceSchema>[];

export type InferTriggerJobNowResult<
  Fields extends TriggerJobNowFields | undefined,
> = InferResult<JobResourceSchema, Fields>;

export type TriggerJobNowResult<Fields extends TriggerJobNowFields | undefined = undefined> = | { success: true; data: InferTriggerJobNowResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Job
 *
 * @ashActionType :update
 */
export async function triggerJobNow<Fields extends TriggerJobNowFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TriggerJobNowResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "trigger_job_now",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TriggerJobNowResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type UserJobsInput = {
  userId: UUID;
};

export type UserJobsFields = UnifiedFieldSelection<JobResourceSchema>[];
export type InferUserJobsResult<
  Fields extends UserJobsFields,
> = Array<InferResult<JobResourceSchema, Fields>>;

export type UserJobsResult<Fields extends UserJobsFields> = | { success: true; data: InferUserJobsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Job records
 *
 * @ashActionType :read
 */
export async function userJobs<Fields extends UserJobsFields>(
  config: {
  tenant?: string;
  input: UserJobsInput;
  fields: Fields;
  filter?: JobFilterInput;
  sort?: SortString<JobSortField> | SortString<JobSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UserJobsResult<Fields>> {
  const payload = {
    action: "user_jobs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<UserJobsResult<Fields>>(
    payload,
    config
  );
}


export type JobRunsInput = {
  jobId: UUID;
  limit?: number | null;
};

export type JobRunsFields = UnifiedFieldSelection<JobRunResourceSchema>[];
export type InferJobRunsResult<
  Fields extends JobRunsFields,
> = Array<InferResult<JobRunResourceSchema, Fields>>;

export type JobRunsResult<Fields extends JobRunsFields> = | { success: true; data: InferJobRunsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read JobRun records
 *
 * @ashActionType :read
 */
export async function jobRuns<Fields extends JobRunsFields>(
  config: {
  tenant?: string;
  input: JobRunsInput;
  fields: Fields;
  filter?: JobRunFilterInput;
  sort?: SortString<JobRunSortField> | SortString<JobRunSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<JobRunsResult<Fields>> {
  const payload = {
    action: "job_runs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<JobRunsResult<Fields>>(
    payload,
    config
  );
}


export type CreateWorkspaceInput = {
  name: string;
  slug: string;
};

export type CreateWorkspaceFields = UnifiedFieldSelection<WorkspaceResourceSchema>[];

export type InferCreateWorkspaceResult<
  Fields extends CreateWorkspaceFields | undefined,
> = InferResult<WorkspaceResourceSchema, Fields>;

export type CreateWorkspaceResult<Fields extends CreateWorkspaceFields | undefined = undefined> = | { success: true; data: InferCreateWorkspaceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Workspace
 *
 * @ashActionType :create
 */
export async function createWorkspace<Fields extends CreateWorkspaceFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateWorkspaceInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateWorkspaceResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_workspace",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateWorkspaceResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeactivateWorkspaceFields = UnifiedFieldSelection<WorkspaceResourceSchema>[];

export type InferDeactivateWorkspaceResult<
  Fields extends DeactivateWorkspaceFields | undefined,
> = InferResult<WorkspaceResourceSchema, Fields>;

export type DeactivateWorkspaceResult<Fields extends DeactivateWorkspaceFields | undefined = undefined> = | { success: true; data: InferDeactivateWorkspaceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Workspace
 *
 * @ashActionType :update
 */
export async function deactivateWorkspace<Fields extends DeactivateWorkspaceFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeactivateWorkspaceResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "deactivate_workspace",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DeactivateWorkspaceResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type GetWorkspaceBySlugFields = UnifiedFieldSelection<WorkspaceResourceSchema>[];
export type InferGetWorkspaceBySlugResult<
  Fields extends GetWorkspaceBySlugFields,
> = InferResult<WorkspaceResourceSchema, Fields>;

export type GetWorkspaceBySlugResult<Fields extends GetWorkspaceBySlugFields> = | { success: true; data: InferGetWorkspaceBySlugResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Workspace records
 *
 * @ashActionType :read
 */
export async function getWorkspaceBySlug<Fields extends GetWorkspaceBySlugFields>(
  config: {
  tenant?: string;
  getBy: {
    slug: string;
  };
  fields: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetWorkspaceBySlugResult<Fields>> {
  const payload = {
    action: "get_workspace_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    getBy: config.getBy,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetWorkspaceBySlugResult<Fields>>(
    payload,
    config
  );
}


export type MyWorkspacesFields = UnifiedFieldSelection<WorkspaceResourceSchema>[];
export type InferMyWorkspacesResult<
  Fields extends MyWorkspacesFields,
> = Array<InferResult<WorkspaceResourceSchema, Fields>>;

export type MyWorkspacesResult<Fields extends MyWorkspacesFields> = | { success: true; data: InferMyWorkspacesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Workspace records
 *
 * @ashActionType :read
 */
export async function myWorkspaces<Fields extends MyWorkspacesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: WorkspaceFilterInput;
  sort?: SortString<WorkspaceSortField> | SortString<WorkspaceSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<MyWorkspacesResult<Fields>> {
  const payload = {
    action: "my_workspaces",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<MyWorkspacesResult<Fields>>(
    payload,
    config
  );
}


export type UpdateWorkspaceInput = {
  name?: string;
  allowedModelIds?: Array<UUID> | null;
  defaultAgentId?: UUID | null;
  isActive?: boolean;
};

export type UpdateWorkspaceFields = UnifiedFieldSelection<WorkspaceResourceSchema>[];

export type InferUpdateWorkspaceResult<
  Fields extends UpdateWorkspaceFields | undefined,
> = InferResult<WorkspaceResourceSchema, Fields>;

export type UpdateWorkspaceResult<Fields extends UpdateWorkspaceFields | undefined = undefined> = | { success: true; data: InferUpdateWorkspaceResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Workspace
 *
 * @ashActionType :update
 */
export async function updateWorkspace<Fields extends UpdateWorkspaceFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: UpdateWorkspaceInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateWorkspaceResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_workspace",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateWorkspaceResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type WorkspaceMemberUsageInput = {
  workspaceId: UUID;
};

export type InferWorkspaceMemberUsageResult = Array<Record<string, any>>;

export type WorkspaceMemberUsageResult = | { success: true; data: InferWorkspaceMemberUsageResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Workspace
 *
 * @ashActionType :action
 */
export async function workspaceMemberUsage(
  config: {
  tenant?: string;
  input: WorkspaceMemberUsageInput;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<WorkspaceMemberUsageResult> {
  const payload = {
    action: "workspace_member_usage",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<WorkspaceMemberUsageResult>(
    payload,
    config
  );
}


export type ChangeWorkspaceMemberRoleInput = {
  role: "admin" | "member";
};

export type ChangeWorkspaceMemberRoleFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];

export type InferChangeWorkspaceMemberRoleResult<
  Fields extends ChangeWorkspaceMemberRoleFields | undefined,
> = InferResult<WorkspaceMemberResourceSchema, Fields>;

export type ChangeWorkspaceMemberRoleResult<Fields extends ChangeWorkspaceMemberRoleFields | undefined = undefined> = | { success: true; data: InferChangeWorkspaceMemberRoleResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WorkspaceMember
 *
 * @ashActionType :update
 */
export async function changeWorkspaceMemberRole<Fields extends ChangeWorkspaceMemberRoleFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input: ChangeWorkspaceMemberRoleInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ChangeWorkspaceMemberRoleResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "change_workspace_member_role",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ChangeWorkspaceMemberRoleResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type DeactivateWorkspaceMemberInput = {
  forWorkspaceRemoval?: boolean;
};

export type DeactivateWorkspaceMemberFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];

export type InferDeactivateWorkspaceMemberResult<
  Fields extends DeactivateWorkspaceMemberFields | undefined,
> = InferResult<WorkspaceMemberResourceSchema, Fields>;

export type DeactivateWorkspaceMemberResult<Fields extends DeactivateWorkspaceMemberFields | undefined = undefined> = | { success: true; data: InferDeactivateWorkspaceMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WorkspaceMember
 *
 * @ashActionType :update
 */
export async function deactivateWorkspaceMember<Fields extends DeactivateWorkspaceMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  input?: DeactivateWorkspaceMemberInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeactivateWorkspaceMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "deactivate_workspace_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DeactivateWorkspaceMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type InviteWorkspaceMemberInput = {
  workspaceId: UUID;
  inviteEmail: string;
};

export type InviteWorkspaceMemberFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];

export type InferInviteWorkspaceMemberResult<
  Fields extends InviteWorkspaceMemberFields | undefined,
> = InferResult<WorkspaceMemberResourceSchema, Fields>;

export type InviteWorkspaceMemberResult<Fields extends InviteWorkspaceMemberFields | undefined = undefined> = | { success: true; data: InferInviteWorkspaceMemberResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new WorkspaceMember
 *
 * @ashActionType :create
 */
export async function inviteWorkspaceMember<Fields extends InviteWorkspaceMemberFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: InviteWorkspaceMemberInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InviteWorkspaceMemberResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "invite_workspace_member",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<InviteWorkspaceMemberResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type ListWorkspaceMembersInput = {
  workspaceId: UUID;
};

export type ListWorkspaceMembersFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];
export type InferListWorkspaceMembersResult<
  Fields extends ListWorkspaceMembersFields,
> = Array<InferResult<WorkspaceMemberResourceSchema, Fields>>;

export type ListWorkspaceMembersResult<Fields extends ListWorkspaceMembersFields> = | { success: true; data: InferListWorkspaceMembersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read WorkspaceMember records
 *
 * @ashActionType :read
 */
export async function listWorkspaceMembers<Fields extends ListWorkspaceMembersFields>(
  config: {
  tenant?: string;
  input: ListWorkspaceMembersInput;
  fields: Fields;
  filter?: WorkspaceMemberFilterInput;
  sort?: SortString<WorkspaceMemberSortField> | SortString<WorkspaceMemberSortField>[];
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListWorkspaceMembersResult<Fields>> {
  const payload = {
    action: "list_workspace_members",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListWorkspaceMembersResult<Fields>>(
    payload,
    config
  );
}


export type ResendWorkspaceInviteFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];

export type InferResendWorkspaceInviteResult<
  Fields extends ResendWorkspaceInviteFields | undefined,
> = InferResult<WorkspaceMemberResourceSchema, Fields>;

export type ResendWorkspaceInviteResult<Fields extends ResendWorkspaceInviteFields | undefined = undefined> = | { success: true; data: InferResendWorkspaceInviteResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WorkspaceMember
 *
 * @ashActionType :update
 */
export async function resendWorkspaceInvite<Fields extends ResendWorkspaceInviteFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResendWorkspaceInviteResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "resend_workspace_invite",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResendWorkspaceInviteResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


export type TransferWorkspaceOwnershipFields = UnifiedFieldSelection<WorkspaceMemberResourceSchema>[];

export type InferTransferWorkspaceOwnershipResult<
  Fields extends TransferWorkspaceOwnershipFields | undefined,
> = InferResult<WorkspaceMemberResourceSchema, Fields>;

export type TransferWorkspaceOwnershipResult<Fields extends TransferWorkspaceOwnershipFields | undefined = undefined> = | { success: true; data: InferTransferWorkspaceOwnershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WorkspaceMember
 *
 * @ashActionType :update
 */
export async function transferWorkspaceOwnership<Fields extends TransferWorkspaceOwnershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUIDv7;
  fields?: Fields;
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TransferWorkspaceOwnershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "transfer_workspace_ownership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<TransferWorkspaceOwnershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}

