<script lang="ts">
	// The library gallery lives in prompts/+layout.svelte and owns the pane when
	// no prompt is selected, so this index route renders nothing of its own. It
	// exists only so /prompts is a valid page.
</script>
