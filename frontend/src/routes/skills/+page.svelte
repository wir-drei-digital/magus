<script lang="ts">
	// The skills gallery lives in skills/+layout.svelte and owns the pane when
	// no skill is selected, so this index route renders nothing of its own. It
	// exists only so /skills is a valid page.
</script>
