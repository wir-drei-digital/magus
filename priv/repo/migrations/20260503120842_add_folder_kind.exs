defmodule Magus.Repo.Migrations.AddFolderKind do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:folders) do
      add :kind, :text, null: false, default: "conversations"
    end
  end

  def down do
    alter table(:folders) do
      remove :kind
    end
  end
end
