defmodule Magus.Repo.Migrations.AddWorkspaceIdToFolders do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:folders) do
      add :workspace_id,
          references(:workspaces,
            column: :id,
            name: "folders_workspace_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all
          )
    end
  end

  def down do
    drop constraint(:folders, "folders_workspace_id_fkey")

    alter table(:folders) do
      remove :workspace_id
    end
  end
end
