defmodule Magus.Repo.Migrations.WorkbenchUpsertIdentity do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  # The unique index on (user_id, workspace_id) with nulls_distinct: false was
  # already created in the initial tab_sessions migration as
  # tab_sessions_user_workspace_unique. No additional index is needed;
  # Ash resolves ON CONFLICT by column list, not by index name.
  def up do
    :ok
  end

  def down do
    :ok
  end
end
