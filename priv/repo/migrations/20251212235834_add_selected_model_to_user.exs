defmodule Magus.Repo.Migrations.AddSelectedModelToUser do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users) do
      add :selected_model_id,
          references(:models,
            column: :id,
            name: "users_selected_model_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:users, "users_selected_model_id_fkey")

    alter table(:users) do
      remove :selected_model_id
    end
  end
end
