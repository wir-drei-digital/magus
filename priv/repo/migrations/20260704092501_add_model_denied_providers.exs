defmodule Magus.Repo.Migrations.AddModelDeniedProviders do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:models) do
      add :denied_providers, {:array, :text}, null: false, default: []
    end
  end

  def down do
    alter table(:models) do
      remove :denied_providers
    end
  end
end
