defmodule Magus.Repo.Migrations.AddModelToConversation do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:conversations) do
      add :selected_model_id,
          references(:models,
            column: :id,
            name: "conversations_selected_model_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:conversations, "conversations_selected_model_id_fkey")

    alter table(:conversations) do
      remove :selected_model_id
    end
  end
end
